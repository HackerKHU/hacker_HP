[← 스펙 인덱스](README.md)

# 5. 테스트와 오류 처리

무엇을 검증해야 머지할 수 있는지, 오류가 났을 때 어떻게 응답하는지를 잡아준다. **이 문서에 적힌 검증을 통과하지 못한 PR은 머지하지 않는다** (MUST).

> **출시 단계** — MVP에서는 공개 랜딩([3-3 결정 8](3-3-DESIGN-DECISIONS.md#3-3-9-결정-8--공개-랜딩-페이지를-1차-출시에-포함하고-정적으로-구현한다))·인증·회원 관리·공지에 해당하는 테스트를 출시 조건으로 삼는다. 자료·즐겨찾기·S3·사진·회원 제거·권한 변경 테스트는 해당 `Post Launch` 기능을 구현하는 PR부터 적용한다. 구현되지 않은 기능의 테스트 때문에 MVP 출시를 막지 않되, 구현한 기능에 해당하는 MUST 테스트는 미룰 수 없다.

```text
§5-1   테스트 범위       무엇을 테스트하고 무엇을 안 하는가
§5-2   필수 테스트 사례   권한·경계 조건 중심
§5-3   수동 검증         자동화하기 어려운 것
§5-4   오류 응답 규칙     형식과 로깅
```

## 5-1 테스트 범위

동아리 프로젝트다. **커버리지 목표를 세우지 않는다.** 대신 **틀리면 사고가 나는 것**만 반드시 테스트한다.

| 대상 | 테스트 | 이유 |
|---|---|---|
| 권한 검사 | **MUST** | 틀리면 미승인자가 시험 자료를 본다 |
| 소유자 검사 (본인 것만 수정·삭제) | **MUST** | 틀리면 남의 자료가 지워진다 |
| 관리자 직접 정지 차단과 마지막 관리자의 권한 회수·삭제 차단 | **MUST** | 틀리면 권한 회수 없이 관리자가 정지되거나 시스템에 아무도 못 들어간다 |
| 관리자 부트스트랩(이메일+토큰) | **MUST** | 틀리면 이메일만 아는 공격자가 관리자를 탈취한다 |
| Status 전이 (`PENDING`/`SUSPENDED` 차단) | **MUST** | 틀리면 승인제가 무의미해진다 |
| 구글 계정의 도메인·`email_verified` 검증 | **MUST** | 틀리면 외부인이 가입한다 |
| JWT와 세션의 사용자 결합 | **MUST** | 틀리면 남의 세션을 빌려 그 권한을 쓴다 |
| 상태 변경이 기존 세션에 반영되는지 | **MUST** | 틀리면 정지해도 계속 쓰고, 승인해도 계속 막힌다 |
| CSRF 토큰 검증 | **MUST** | 틀리면 다른 사이트가 관리자 대신 회원을 승인·정지한다 |
| presigned URL 발급 조건 (용량·확장자) | **MUST** | 틀리면 임의 파일이 버킷에 쌓인다 |
| 검색·필터 조합 | SHOULD | 틀리면 불편하지만 사고는 아니다 |
| 페이지네이션 경계 | SHOULD | |
| DTO 매핑, getter | 안 한다 | 프레임워크를 테스트하는 것이다 |

**API 계층은 통합 테스트로 검증한다** (SHOULD). Spring Security 필터를 태우지 않는 단위 테스트는 권한 버그를 잡지 못한다. `@SpringBootTest` + Testcontainers(또는 CI의 postgres service container)를 쓴다.

기반은 `apps/api`의 `AbstractIntegrationTest`(Testcontainers·세션·JWT)와 `testsupport`(`Accounts`·`TestSessions`·`Csrf`)다 (2026-08-14, #90). 새 API 이슈는 **그 위에 T 번호 사례만 얹는다.**

- 로그인 상태는 `sessions.as(user, request)` 한 줄이다. **세션은 진짜 저장소에 만든다** — `MockHttpSession`을 쓰려면 세션 자동설정을 꺼야 하고, 그러면 테스트마다 어느 방식인지 골라야 한다.
- 계정은 `Accounts.signedIn/applied/approved/admin/suspended`로 만든다. **가입 단계를 그대로 밟는다** — 상태를 손으로 조립하면 실제로는 만들어질 수 없는 계정이 생긴다.
- **구글 OAuth 콜백 전체 흐름은 통합으로 타지 않는다.** 도메인·`email_verified` 검증(위 표의 MUST)은 `GoogleOidcUserServiceTest`가 서비스 단위로 덮고, 배선은 #82·#48이 실제 구글로 밟는다. 가짜 OIDC 서버를 띄우는 것은 별도 결정이 필요하다.
- **CI에서 도는 것은 #45가 붙인다.** 러너에 Docker가 있어야 Testcontainers가 뜬다.

## 5-2 필수 테스트 사례

### 인증·권한

| # | 조건 | 기대 |
|---|---|---|
| T-01 | 비로그인으로 `GET /notices` | `401 UNAUTHENTICATED` |
| T-02 | `PENDING` 사용자로 `GET /notices` | `403 PENDING_APPROVAL` |
| T-03 | `SUSPENDED` 계정으로 구글 로그인 | 세션 미발급, `/login?error=suspended`로 리다이렉트 |
| T-04 | `USER`로 `POST /notices` | `403 FORBIDDEN` |
| T-05 | `USER`로 `GET /admin/users` | `403 FORBIDDEN` |
| T-06 | 같은 구글 계정으로 두 번째 로그인 | 새 계정을 만들지 않고 기존 계정에 붙는다 (`google_sub`로 조회) |
| T-07 | 신청서의 학번이 다른 계정에 이미 있음 | `409 DUPLICATE_STUDENT_NO` |
| T-08 | `khu.ac.kr`이 아닌 구글 계정으로 로그인 | `/login?error=domain`으로 리다이렉트, **계정이 생성되지 않는다** |
| T-144 | `PENDING`·`SUSPENDED`가 신청·대기에 필요한 경로를 호출 | **통과한다.** `GET /auth/csrf`·`GET /auth/me`·`POST /auth/logout` |
| T-248 | 비로그인이 `GET /auth/me` 호출 | **`204`. 오류가 아니고 본문도 없다** (MUST, #190) |
| T-249 | 결합이 어긋난 쿠키로 `GET /auth/me` 호출 | `204`이되 **양쪽 쿠키를 폐기하고 세션을 지운다** — 열렸다고 결합 검사가 느슨해지지 않는다 |
| T-145 | `SUSPENDED`가 `POST /auth/application` 호출 | `403 SUSPENDED`. **`FORBIDDEN`이 아니다** — 신청 API는 `PENDING`에게만 열린다 |
| T-146 | 매핑된 엔드포인트에 접근 규칙이 없다 | **테스트가 실패한다.** `@PreAuthorize`로 권한을 적거나 `@PublicApi`로 열었다고 선언해야 한다 |
| T-147 | 정지된 `ADMIN`이 관리자 API 호출 | `403 SUSPENDED`. **`FORBIDDEN`이 아니다** — 막은 것은 권한이 아니라 상태다 |
| T-148 | `USER`가 **본문이 깨진** 관리자 쓰기 API 호출 | `403 FORBIDDEN`. **`400`이 아니다** — 본문을 읽기 전에 끊는다 |
| T-149 | 정렬을 지정하지 않은 `GET /admin/users` | 가입 신청일(`applied_at`) 최신순. **신청하지 않은 계정이 맨 뒤에 온다** |
| T-150 | `sort`에 이름·학번·가입 신청일이 아닌 값 | `400 VALIDATION_ERROR`. **조용히 무시하지 않는다** |
| T-151 | `GET /admin/users?status=PENDING&applied=true` | 신청서를 낸 `PENDING`만. 구글 로그인만 해본 계정은 빠진다 |
| T-152 | 검색어에 `_` 또는 `%` | 글자 그대로 찾는다. **전원이 결과에 나오지 않는다** |
| T-153 | `size`가 상한(100)을 넘는 요청 | 100으로 잘린다 |
| T-154 | 회원 목록 응답의 한 행 | `google_sub`·`version`이 없다 |
| T-155 | 여러 `PENDING` 신청자를 한 요청으로 승인 | 전원이 `ACTIVE`가 되고 `approved_at`이 채워진다 |
| T-156 | 성공·실패가 섞인 일괄 승인 | **성공한 건은 그대로 승인된다.** 실패 한 건이 나머지를 되돌리지 않는다 |
| T-157 | 이미 `ACTIVE`이거나 `SUSPENDED`인 id | `NOT_PENDING`으로 집계된다. **`NOT_APPLIED`가 아니다** — 사유를 뭉개면 화면이 거짓 안내를 한다 |
| T-158 | 같은 id를 두 번 담아 승인 | 한 번만 집계된다. 화면이 읽는 건수가 부풀려지지 않는다 |
| T-159 | 빈 배열 또는 101개 이상 | `400 VALIDATION_ERROR`. **같은 id를 101번 담아도 마찬가지다** — 상한은 중복을 거르기 전의 원본 배열에 건다 |
| T-160 | CSRF 토큰 없이 일괄 승인 | `403`. 계정 상태가 바뀌지 않는다 |

T-144는 **막는 것만큼 여는 것이 중요하다**는 점을 지킨다. 신청 API를 막으면 **아무도 신청서를 낼 수 없고**(신청 전 계정도 `PENDING`이다), `GET /auth/me`를 막으면 화면이 상태를 몰라 신청 폼도 정지 안내도 띄우지 못한다. **정지된 사람에게도 열어야 한다** — 막으면 `401`로 보여 "로그아웃됨"과 구별되지 않는다 ([3-3 결정 12](3-3-DESIGN-DECISIONS.md#3-3-13-결정-12--인증은-jwt-인가-상태는-서버-세션으로-나눈다)).

T-146은 **잊는 것**을 막는다. 권한 매트릭스는 표가 원본이고 코드가 그것을 따르는데, 옮겨 적기를 잊어도 코드는 그냥 동작한다 — 인증만 되면 통과하는 엔드포인트가 조용히 생긴다. 클래스가 아니라 **실제로 매핑된 핸들러**를 훑는다. 열려 있는 것은 매핑이지 클래스가 아니다.

T-147은 `@PreAuthorize`에 `ACTIVE` 조건을 반복해 적지 않는 근거다. 매트릭스의 `ADMIN` 열은 "`ADMIN`이면서 `ACTIVE`"지만, 상태는 `AccountStatusFilter`가 인가보다 먼저 본다. **같은 규칙을 두 곳에 두면 한쪽만 고쳐진다.**

T-05가 여기서 처음으로 실제 확인된다. 계약이 정한 지 오래됐지만 `GET /admin/users`가 없어 미뤄져 있었다(#28 PR에 그렇게 적어 뒀다).

T-149~T-151은 **이 목록이 곧 승인·정지의 대상이 되기 때문에** 있다. 순서가 어긋나거나 신청하지 않은 계정이 승인 대기로 섞이면 관리자가 잘못된 사람을 처리한다. T-149의 "맨 뒤"가 특히 그렇다 — PostgreSQL은 `DESC`에서 널을 **맨 앞에** 올리므로, 널 처리를 빠뜨리면 관리자가 첫 화면에서 보는 것이 승인 대상이 아닌 사람들이 된다.

T-150이 **거절**인 이유는 무시가 더 위험하기 때문이다. 정렬이 적용된 줄 알고 틀린 순서의 명단을 신뢰하는 것보다 `400`을 받는 편이 낫다. T-152도 같다 — `_`를 escape하지 않으면 "아무 글자 하나"로 해석되어 검색 결과에 **전원이 나오고**, 관리자는 그것을 검색 결과로 믿는다.

T-154의 완료 조건은 이슈에 "비밀번호 해시가 응답에 없다"로 적혀 있었다. 구글 OAuth뿐이라 비밀번호 컬럼 자체가 없으므로 **같은 취지로 새면 안 되는 것**을 확인한다 — `google_sub`(구글 계정 식별자)와 `version`(동시성 제어용)이다.

T-155~T-160은 T-49와 한 벌이다. **가입 승인은 되돌릴 수 없고 여러 명을 한 번에 처리한다.** T-156이 특히 그렇다 — 실패를 예외로 던지면 관리자가 20명을 골랐을 때 한 명이 신청서를 내지 않았다는 이유로 **아무도 승인되지 않는다.**

T-157이 사유를 가르는 이유는 화면이 그것을 그대로 안내하기 때문이다. 전부 `NOT_APPLIED`로 뭉개면 **신청서를 내고 이미 승인까지 받은 사람에게 "신청서를 내지 않은 계정입니다"라고 말하게 된다.**

T-148은 **인가가 본문보다 먼저인지**를 본다. MVC는 메서드를 부르기 전에 본문을 역직렬화하고 `@Valid`를 돌리므로, `@PreAuthorize`에만 기대면 권한 없는 사람이 깨진 본문을 보냈을 때 `400 INVALID_INPUT`을 받는다 — **권한이 없다는 사실이 본문 모양에 가려진다.** 그래서 관리자 경로는 필터 체인에서도 막는다 (`POST /auth/application`이 T-50 때문에 그런 것과 같은 이유다).

T-145가 그 경계다. 신청 API를 상태와 무관하게 열면 **정지된 사람이 제출했을 때 인가 규칙이 거절해 `FORBIDDEN`이 나간다** — 화면은 정지를 알아채지 못하고 "권한이 없습니다"만 띄운 채 남는다. 대기 중에 정지당한 사람이 딱 이 경로를 밟는다.

T-02·T-32의 코드가 `FORBIDDEN`이 아닌 이유도 같다. **셋 다 같은 상태 코드를 쓰므로 화면이 가르는 근거는 코드뿐이다** (T-116).

### 소유자·안전장치

| # | 조건 | 기대 |
|---|---|---|
| T-09 | A가 올린 자료를 B(`USER`)가 `PATCH` | `403 FORBIDDEN` |
| T-10 | A가 올린 자료를 관리자가 `PATCH` | 성공 |
| T-11 | 활성 관리자가 **1명**일 때, 그 관리자가 자기 자신의 role을 `USER`로 변경 | `403 FORBIDDEN` |
| T-12 | 활성 관리자가 **2명**일 때, 한 명이 자기 자신의 role을 `USER`로 변경 | 성공 (활성 관리자 1명 남음) |
| T-13 | 활성 관리자가 1명일 때, 자기 자신을 `DELETE` | `403 FORBIDDEN` |
| T-14 | `ADMIN` 계정의 status를 `SUSPENDED`로 변경 | 활성 관리자 수·자기 대상 여부와 무관하게 `403 FORBIDDEN` + 정확한 권한 회수 안내 |
| T-15 | 활성 관리자 2명이 **동시에** 각자 자기 자신을 회수/삭제 시도 | 최소 한쪽은 실패한다 — 활성 관리자가 0명이 되면 안 된다 (MUST, [2-2 §2-2-7](2-2-OPERATOR-REQUIREMENTS.md) 원자성 요구사항) |
| T-433 | 여러 관리자가 있을 때 다른 관리자와 자기 자신을 각각 직접 정지 | 모두 `403 FORBIDDEN`, 메시지는 **"관리자 계정은 바로 정지할 수 없습니다. 먼저 관리자 권한을 회수한 뒤 정지해 주세요."** |
| T-434 | 마지막 활성 관리자 직접 정지 | T-433과 같은 코드·문구. 활성 관리자 수에 따른 옛 문구가 먼저 나오지 않는다 |
| T-435 | 기존/비정상 `ADMIN` 상태(`ACTIVE`·`SUSPENDED`·`INACTIVE`·`PENDING`)에 desired `SUSPENDED` | 전부 같은 `403`. 기존 `SUSPENDED ADMIN`을 자동 보정하지 않는다 |
| T-436 | T-433~T-435의 거절 전후 | 대상 status·role, 기존 세션, `admin_actions`가 모두 그대로다 |
| T-437 | `USER`의 `ACTIVE`·`INACTIVE` → `SUSPENDED`, `SUSPENDED` → `ACTIVE`, 같은 상태 재요청 | 기존 성공·멱등·세션 규약을 유지한다 |
| T-438 | `ADMIN`을 `USER`로 회수한 뒤 별도 정지 | 둘 다 성공. 세션이 `USER` 뒤 `SUSPENDED`로 각각 반영되고 감사가 `REVOKE_ADMIN` → `SUSPEND` 두 행이다 |
| T-439 | 인가 뒤 요청자가 정지·강등된 상태에서 `ADMIN` 대상 정지를 마침 | 요청자 재검증의 `SUSPENDED`·`FORBIDDEN`이 관리자 직접 정지 정책보다 먼저다. 대상은 그대로다 |
| T-440 | 없는 대상에 정지 요청 | 기존대로 `404 NOT_FOUND`. 정책이 오류 경계를 바꾸지 않는다 |
| T-441 | 관리자 회원 제거·본인 탈퇴 | 내부 선행 정지를 거쳐 기존처럼 성공한다. 마지막 활성 관리자 보호와 `SUSPEND` → `REMOVE`/`WITHDRAW` 감사도 유지한다 |
| T-442 | 관리자 직접 정지 요청을 동시에 여러 개 보냄 | 전부 정책 거절이고 변화·감사가 없다. 마지막 관리자 동시 보호는 권한 회수·삭제 동시성 사례가 계속 맡는다 |

### 선택 회원 일괄 활성화·정지 (#313)

| # | 조건 | 기대 |
|---|---|---|
| T-443 | 본문 없음·최상위 `null`·`userIds` 누락·`null`·빈 배열 | 모두 `400 VALIDATION_ERROR`, 어떤 행도 바뀌지 않는다 |
| T-444 | 원본 id 101개, 같은 id 101번 | 중복 제거 전 상한으로 둘 다 `400`. 원본 100개는 허용 |
| T-445 | id 요소 `null`·0·음수, `status` 누락·잘못된 값 | 모두 `400 VALIDATION_ERROR` |
| T-446 | 역순·중복·없는 id가 섞인 요청 | 검증 뒤 중복은 첫 등장만 남기고, `processed`·`failed`는 각자 첫 등장 상대 순서를 유지한다. 잠금만 id 오름차순 |
| T-447 | `ACTIVE` 혼합: 신청/미신청 `PENDING`, `INACTIVE`, `SUSPENDED`, `ACTIVE`, 없는 id | 각각 승인·`NOT_APPLIED`·복구·정지 해제·멱등·`NOT_FOUND`. 승인일과 `deactivated_at` 반영 |
| T-448 | `SUSPENDED` 혼합: `USER` ACTIVE/INACTIVE/SUSPENDED, 제출/미제출 PENDING, ACTIVE/SUSPENDED ADMIN, 없는 id | 일반 회원만 정지·멱등. PENDING은 `PENDING_NOT_ALLOWED`, ADMIN은 `ADMIN_SUSPEND_REQUIRES_ROLE_REVOCATION`, 없으면 `NOT_FOUND` |
| T-449 | `ADMIN`을 별도 요청으로 `USER`로 회수한 뒤 일괄 정지 | 성공. 세션과 이력이 `REVOKE_ADMIN` → `SUSPEND`로 나뉘어 남는다 |
| T-450 | 단건 상태 PATCH의 관리자·없는 id·잘못된 상태 | 기존 `403`·`404`·`400` 본문이 그대로고, 일괄의 없는 id만 `200` 항목 실패 |
| T-451 | 한 `ACTIVE` 배치에 `APPROVE`·`REACTIVATE`·`ACTIVATE` 전이 | 성공 전이만 대상별 한 행, 같은 `occurredAt`, target id 순. 실패·멱등은 이력 없음 |
| T-452 | 활성화 대상들의 기존 세션 | 승인·복구·정지 해제 후 다음 요청에서 `ACTIVE`. 이미 ACTIVE인 id도 세션 재반영 대상 |
| T-453 | 정지 대상들의 기존 세션 | 다음 보호 요청이 `401`이 아닌 `403 SUSPENDED` |
| T-454 | 정지 배치의 세션 하나가 반영 실패 | 나머지도 끝까지 시도하고 `500`. 상태·성공 이력은 남으며 같은 요청 재시도가 멱등 세션 복구 |
| T-455 | 활성화 배치의 세션 반영 실패 | 끝까지 시도·실패 기록, `200`, 상태·이력 유지 |
| T-456 | 같은 `USER`를 포함한 두 `SUSPENDED` 배치가 동시 도착 | 둘 다 처리되지만 `SUSPEND` 이력은 한 행뿐 |
| T-457 | `ACTIVE`와 `SUSPENDED` 배치가 같은 id를 동시 처리 | id 오름차순 잠금으로 직렬화되고, 각 응답과 이력은 잠금 후 본 전이와 일치 |
| T-458 | 요청자가 필터 통과 뒤 정지·강등 | 요청자 재검증이 대상 판정보다 먼저 `SUSPENDED`·`FORBIDDEN`으로 전체를 막는다 |
| T-459 | `ADMIN` role 회수/승격과 일괄 정지가 경합 | 잠금 후 최신 role이 `USER`면 정지, `ADMIN`이면 항목 실패 |
| T-460 | USER·비로그인·CSRF 없음·USER의 깨진 본문 | 기존 관리자 API와 같은 `FORBIDDEN`·`UNAUTHENTICATED` 우선순위. 대상은 변화 없음 |
| T-461 | 생성된 OpenAPI | 새 PATCH, required body, `maxItems=100`, 양수 id, target enum, response·reason enum, 커밋 후 세션 실패 `500` 설명이 모두 노출 |

### 관리자 부트스트랩

| # | 조건 | 기대 |
|---|---|---|
| T-16 | 활성 관리자 0명, **신청서 제출을 마친** 계정이 이메일·토큰 모두 일치 | 승격 성공 — `role=ADMIN`, `status=ACTIVE`, `approved_at` 기록됨. `student_no`가 채워져 있다 |
| T-17 | 활성 관리자 0명, 이메일은 일치하나 토큰 불일치 | `403 FORBIDDEN` |
| T-18 | 활성 관리자 0명, `ADMIN_BOOTSTRAP_EMAIL`과 다른 이메일의 사용자가 올바른 토큰으로 호출 | `403 FORBIDDEN` |
| T-19 | 활성 관리자가 이미 1명 이상 존재할 때 올바른 토큰으로 호출 | `403 FORBIDDEN` |
| T-20 | **첫 구글 로그인 직후(신청서 미제출)** 이메일·토큰이 모두 일치하는 계정이 호출 | `403 FORBIDDEN` — 학번 없는 관리자가 만들어지면 안 된다 |
| T-175 | **정지된 계정**이 이메일·토큰 모두 일치로 호출 | `403 FORBIDDEN`. 정지된 사람이 이 경로로 되살아나면 안 된다 |
| T-176 | **이미 승인된 계정**이 호출 (마지막 관리자 복구) | 승격되고 **`approved_at`이 바뀌지 않는다** — 실제 승인일이 사라지면 안 된다 |
| T-177 | 설정값이 비어 있는 서버에서 호출 | `403 FORBIDDEN`. 기동은 정상이고, **설정되지 않았다는 것도 드러나지 않는다** |
| T-178 | `ADMIN_BOOTSTRAP_EMAIL`이 대문자로 설정됨 | 승격된다. 이메일은 비밀이 아니고 운영자가 어떻게 넣든 동작해야 한다 |
| T-179 | 승격에 성공한 계정이 **같은 요청을 다시** 보냄 | `204`. 아무것도 바뀌지 않고 세션만 다시 맞춘다 — 세션 갱신 실패의 복구 수단이다 |
| T-180 | 대소문자만 다른 두 계정이 각각 호출 | **한 명만 승격된다.** 이메일 비교는 대소문자를 가리지 않지만 `UNIQUE`는 가린다 |

T-17~T-20·T-175·T-177의 응답이 **모두 같아야 한다** (MUST). 사유가 갈리면 *"이메일은 맞았고 토큰만 틀렸다"* 를 알아낼 수 있어 토큰 추측의 탐색 공간이 줄어든다. 설정 여부까지 같은 응답인 이유도 같다 — 알 수 있으면 **이 경로가 언제 열리는지를 지켜볼 수 있다.**

T-179가 필요한 이유는 **세션 갱신 실패를 예외로 올리지 않기** 때문이다([3-1 §3-1-5](3-1-DESIGN-ARCHITECTURE.md)). 승격은 커밋됐는데 세션만 옛 값으로 남으면 본인이 할 수 있는 일은 다시 부르는 것뿐인데, "활성 관리자가 이미 있다"로 거절하면 그 재시도가 막혀 **재로그인 전까지 관리자 화면을 못 연다.** 이메일·토큰을 이미 확인한 뒤라 여는 문이 넓어지지도 않는다.

T-180은 **자격을 만족하는 행이 둘일 수 있다**는 데서 온다. `users.email`의 `UNIQUE`는 대소문자를 구분하므로 `a@b`와 `A@B`가 함께 존재할 수 있고, 각자 자기 행만 잠그면 둘 다 "활성 관리자 0명"을 보고 통과한다. 같은 이메일을 쓰는 계정을 **전부** 잠가 이 경로의 요청을 한 줄로 세운다.

T-16은 `AccountStatusFilter`가 이 경로를 `PENDING`에게 열어 두는지도 함께 확인한다. 막혀 있으면 **최초 관리자는 영영 승격할 수 없다** — 이 경로를 부를 수 있는 상태가 `PENDING`뿐이기 때문이다.

### 자료 조회

[2-1 §2-1-1](2-1-USER-STORIES.md)·[3-2 §3-2-4](3-2-DESIGN-CONTRACT.md)가 요구하는 결과다 (#52).

| # | 조건 | 기대 |
|---|---|---|
| T-221 | 기본 조회 | 최신순. 검색어·필터 없이 전체 |
| T-222 | 제목·과목명·교수명 중 어디에 있든 같은 키워드로 검색 | **셋 다 찾힌다** (MUST). 필드를 나눠 받지 않는다 |
| T-223 | 교수명이 없는 자료를 검색 | 그 행이 **빠지지 않는다** — 널을 지나치면 통째로 사라진다 |
| T-224 | 검색어에 `_`·`%` 포함 | 글자 그대로 찾는다. escape하지 않으면 **찾지 않은 자료가 섞인다** |
| T-225 | 검색어 + 필터 여럿 | **AND로 함께** 걸린다 (MUST) |
| T-226 | `category=SUBJECT&examType=MIDTERM` | **`200`에 0건.** 오류가 아니다 |
| T-227 | `sort=title` / 모르는 값 | 제목순 / 기본값(최신순). 모르는 값에 **`500`이 나지 않는다** |
| T-228 | 자료 100건에서 과목명 일부로 검색 | 20건 단위로 페이징된다. **두 페이지에 같은 자료가 겹치지 않는다** (MUST) |
| T-229 | 목록과 상세의 파일 | 목록은 **개수만**, 상세는 **목록**(`id`·이름·크기) |
| T-230 | 어떤 응답이든 | **S3 키가 나오지 않는다** (MUST) |
| T-231 | 탈퇴한 회원이 올린 자료 | 그대로 보이고 업로더 이름이 **`"탈퇴한 회원"`**, id는 없다 |
| T-427 | 학번이 `2021102466`인 부원이 올린 자료 | 업로더 이름이 **`권승원66`** — 이름 + 끝 두 자리 (MUST) |
| T-428 | **학번이 비어 있는** 계정이 올린 자료 | 이름만 나온다. **붙일 것이 없으면 붙이지 않는다** |
| T-429 | 학번이 **한 글자**인 계정 | 그 한 글자만 붙는다 — 있는 만큼 |
| T-430 | 학번에 **숫자가 아닌 글자**가 섞인 계정 | 그래도 뒤 두 글자를 붙인다 — 구별이 목적이지 학번 해석이 아니다 |
| T-431 | 같은 사람의 자료·공지·활동사진·게시글을 함께 조회 | **네 곳의 표시 이름이 같다** (MUST) |
| T-432 | 이름이 같고 **학번 끝 두 자리가 다른** 두 부원 | 표시 이름이 **서로 다르다** — 이 기능의 목적이다 |
| T-433 | 이름이 같고 **끝 두 자리까지 같은** 두 부원 (`2021102466`·`2022102466`) | 표시 이름이 **같다.** 결정 18이 받아들인 한계다 |
| T-434 | 학번이 **BMP 밖 문자로 시작**한다 (`😀A`) | 끝 두 **글자**가 온전하다 (`😀A`). 깨진 서로게이트가 나오지 않는다 (MUST) |
| T-435 | **(웹)** 비로그인으로 `/privacy` 열람 | **학번 뒷자리가 다른 부원에게 보인다**는 안내가 있다 (MUST) |
| T-232 | `GET /notes/filters` | **등록된 값에서만** 만든다. 교수명이 없는 자료는 옵션을 만들지 않는다 |
| T-233 | 비로그인 / `PENDING` 회원 | `401` / `403 PENDING_APPROVAL` |
| T-462 | **(웹)** 320·360·390 CSS px의 자료게시판 갈래 메뉴 | 시험 정리본·과목 정리본·즐겨찾기가 한 줄에 있고 페이지 가로 스크롤·겹침이 없다. 글자 크기를 줄이지 않으며 각 링크의 터치 높이는 최소 44px다. 즐겨찾기 선택 표시·별표·URL 동작은 그대로다 |

**T-228이 완료 조건이다.** 같은 시각·같은 제목이 여럿일 때 **순서를 가르는 마지막 기준이 없으면 페이지마다 배치가 달라진다** — 같은 자료가 두 번 보이거나 빠지고, 훑는 사람은 알아채지 못한다. 건수만 세는 사례로는 잡히지 않아 **두 페이지를 합쳐 중복을 본다.**

**T-227은 화면이 보내는 값 하나로 서버가 터지지 않는지 본다.** 계약의 `sort`는 `latest`·`title`인데 Spring Data의 속성 정렬로 흘러가면 없는 속성 이름에 `500`이 난다.

**T-230은 규칙이 지켜지는지를 응답 전체에서 본다.** 필드를 하나씩 확인하면 나중에 늘어난 응답에서 새는 것을 놓친다.

**T-431이 "한 곳에서 만든다"를 지킨다** ([3-2 §3-2-2](3-2-DESIGN-CONTRACT.md#표시-이름은-이름--학번-끝-두-자리다-2026-08-30-확정-300) MUST). 도메인마다 재면 세 곳을 고치고 한 곳을 잊어도 전부 통과한다 — **활동사진이 지금 그 상태다.** 네 응답을 한 사례에서 견줘야 어긋남이 드러난다.

**T-428 ~ T-430, T-434는 학번에 형식이 없다는 사실을 견딘다.** 계약이 요구하는 것은 *"공백이 아닐 것"* 뿐이라([§3-2-3](3-2-DESIGN-CONTRACT.md#3-2-3-api--인증)) 짧거나 숫자가 아닌 값이 실제로 들어올 수 있다. **입력을 조이는 것은 [#328](https://github.com/HackerKHU/hacker_HP/issues/328)이 따로 보지만, 그것이 끝나도 이미 저장된 값은 남는다.**

**T-434는 Java의 함정을 겨냥한다.** `substring(length - 2)`는 **UTF-16 코드 유닛**으로 자르므로 BMP 밖 문자(이모지 등)가 걸리면 **서로게이트 쌍을 반으로 쪼개** 깨진 글자를 내보낸다. ASCII 값으로만 재면 그 구현이 그대로 통과한다.

**T-433은 결정이 받아들인 한계를 사례로 못 박는다.** 끝 두 자리가 같으면 표시 이름도 같다 — 그것이 결함이 아니라 [결정 18](3-3-DESIGN-DECISIONS.md#3-3-19-결정-18--표시-이름에-학번-끝-두-자리를-붙인다)이 *"드물다는 이유로 받아들인다"* 고 적은 값이다. 없으면 다음 사람이 이것을 버그로 읽고 자리를 늘려 **노출을 키운다.**

**T-435가 없으면 고지 조건이 빠진 채로 배포된다.** [결정 18](3-3-DESIGN-DECISIONS.md#3-3-19-결정-18--표시-이름에-학번-끝-두-자리를-붙인다)은 방침 고지를 **조건으로** 붙였는데, 나머지 사례는 전부 API의 표시 이름만 본다 — 표시 이름만 구현해도 다 통과한다. **[#301](https://github.com/HackerKHU/hacker_HP/issues/301)이 이 사례와 방침 문구를 함께 낸다** (T-195와 같은 자리를 본다).

### 자료 조회수 (#244, #245)

[2-1 §2-1-1](2-1-USER-STORIES.md#2-1-1-자료-목록검색필터)·[3-2 §3-2-4](3-2-DESIGN-CONTRACT.md)가 요구하는 결과다. 구현은 `NoteViewCountIntegrationTest`다.

| # | 조건 | 기대 |
|---|---|---|
| T-405 | `GET /notes/{id}` 한 번 | `view_count`가 **정확히 1** 오른다 |
| T-406 | 같은 사람이 같은 자료를 **세 번** 열기 | **3이다** (MUST). 중복을 걸러내지 않는다 |
| T-407 | 업로더 본인이 자기 자료를 열기 | **센다** — 남이 열었을 때와 같다 |
| T-408 | `GET /notes` 목록 조회 · `GET /bookmarks` · 파일 URL 발급 | **하나도 오르지 않는다** (MUST) |
| T-409 | 증가에 **성공한** 상세 응답의 `viewCount` | **올린 뒤의 값이다** (MUST). 직후의 목록 조회와 같은 숫자다 |
| T-410 | 증가가 **실패하는** 상태에서 상세 조회 | **`200`이고 자료가 그대로 나온다** (MUST). `viewCount`는 **증가 전 값**이다 |
| T-411 | 상세를 연 뒤 그 자료의 `updated_at` | **바뀌지 않는다** (MUST) |
| T-412 | 같은 자료를 **동시에** N번 열기 | `view_count`가 **정확히 N**이다 (MUST) |
| T-413 | `sort=views` | 조회수 내림차순. **조회수가 같은 자료들의 순서가 페이지를 넘겨도 같다** (MUST) |
| T-414 | 새 자료를 **목록에서** 본 뒤 **상세를 처음** 열기 | 목록은 **0**, 첫 상세 응답은 **1** |
| T-415 | `INACTIVE` 부원이 상세 조회 | `403 INACTIVE`이고 **오르지 않는다** — 자료 갈래가 통째로 막힌다 (T-338) |

**T-406이 D2 정책 자체다.** 중복 제거를 "나중에 넣을 개선"으로 여긴 구현이 `(회원, 자료)` 캐시를 슬쩍 끼워도 T-405는 통과한다. **세 번 열어 3인지 보는 사례가 있어야** 그 캐시가 정책 위반으로 드러난다.

**T-408은 세는 자리가 하나뿐인지 본다.** 목록에서 올리면 한 번 훑을 때마다 스무 건이 함께 오르고, 그때부터 숫자는 "몇 번 열렸나"를 뜻하지 못한다. **셋을 한 사례로 묶은 것은 "조회처럼 보이는 경로"가 셋이기 때문이다** — 하나만 재면 나머지 둘에 증가가 들어가도 통과한다.

**T-409와 T-410을 나눈 것은 두 MUST가 부딪치는 자리이기 때문이다.** 증가에 실패하면 "올린 뒤의 값"을 줄 방법이 없다 — 없는 증가를 응답에서만 더하면 **직후 목록과 어긋난다.** 그래서 실패한 경우는 증가 전 값이고, 그것을 별개 사례로 못 박는다. 한 사례로 묶으면 **어느 쪽 기대값을 쓸지 구현자가 고른다.**

**T-410과 T-411은 구현이 반드시 지나는 두 함정이다.** 조회 서비스는 읽기 전용 트랜잭션이라 그 안에서 `UPDATE`를 하면 PostgreSQL이 거절하고, 엔티티를 읽어 고치면 `updated_at`이 함께 바뀐다. **둘 중 하나만 재면 나머지가 살아남는다** — 별도 트랜잭션으로 옮기면 T-410은 통과하지만 엔티티를 쓰는 한 T-411은 깨진다.

**T-412는 `read → +1 → write`를 잡는다.** 그 구현은 혼자 눌러 보는 동안 완벽하게 동작하고, 사람이 몰리는 자료에서만 숫자를 잃는다 — **가장 많이 열린 자료가 가장 많이 잃는다.**

**T-413은 T-228과 같은 위험이 더 심한 자리다.** 새로 올라온 자료는 **전부 `0`이라 동률이 목록을 통째로 채운다.** 마지막 기준이 `id`가 아니면 페이지를 넘길 때마다 배치가 달라진다.

**T-414는 두 규칙이 만나는 자리다.** 목록은 세지 않고(T-408) 상세는 올린 뒤의 값을 주므로(T-409), **같은 자료의 두 숫자가 다른 것이 정상이다.** 하나로 맞추려 들면 둘 중 하나가 깨진다 — 목록에서 세거나, 상세가 증가 전 값을 주거나.

### 즐겨찾기

[2-1 §2-1-5](2-1-USER-STORIES.md)·[3-2 §3-2-4](3-2-DESIGN-CONTRACT.md)가 요구하는 결과다 (#56).

| # | 조건 | 기대 |
|---|---|---|
| T-234 | 같은 자료를 두 번 담음 | `204` 두 번, 행은 **하나**. **빠지지 않는다** — 토글이 아니다 |
| T-235 | 담기지 않은 자료를 해제 / 없는 자료를 해제 | 둘 다 `204` |
| T-236 | 없는 자료를 담음 | `404 NOT_FOUND` — 그대로 넣으면 FK 위반이 `500`이 된다 |
| T-237 | 남이 담은 자료를 내가 해제 | 남의 것은 그대로 남는다 |
| T-238 | `GET /bookmarks` | **내 것만** 나온다 (MUST). 응답은 자료 목록과 같은 형태다 |
| T-239 | 나중에 올라온 자료를 먼저 담음 | **내가 담은 순서**로 나온다 — 자료의 등록 시각이 아니다 |
| T-240 | 담아 둔 자료를 삭제 | 즐겨찾기도 함께 사라진다 (MUST, `ON DELETE CASCADE`) |
| T-241 | 자료 목록·상세의 `bookmarked` | 담기 전 `false`, 담은 뒤 `true` |
| T-242 | 남이 담은 자료의 `bookmarked` | 나에게는 `false` — 표시가 새면 목록이 **남의 상태**를 보여준다 |
| T-243 | 비로그인 / `PENDING` 회원 | `401` / `403 PENDING_APPROVAL` |
| T-244 | 같은 자료를 **동시에** 여러 번 담음 | 모두 `204`, 행은 **하나** (MUST) |
| T-245 | 같은 즐겨찾기를 **동시에** 여러 번 해제 | 모두 `204` (MUST) |
| T-246 | `GET /bookmarks`에 `?sort=` | 순서가 바뀌지 않고 `500`도 나지 않는다 |
| T-247 | 존재 확인 **뒤** 그 자료가 지워진 상태로 담기 | `404 NOT_FOUND` (MUST). FK 위반이 `500`으로 새지 않는다 |

**T-234가 토글이 아님을 지킨다.** 같은 요청이 상태를 뒤집으면 **재시도가 방금 담은 것을 조용히 뺀다** — 사용자는 한 번 눌렀는데 별표가 꺼지고 오류는 나지 않는다.

**T-239는 이 목록의 기준이 무엇인지를 고정한다.** 자료 정렬을 그대로 쓰면 "내가 방금 담은 것"이 아래에 묻힌다.

**T-244·T-245는 멱등이 순차 사례만으로는 지켜지지 않는다는 것을 본다.** 확인하고 저장하면 동시에 도착한 요청들이 모두 "없다"를 읽고 지나가고, 읽고 지우면 뒤의 요청이 지울 것을 잃는다 — **멱등이어야 할 경로가 `500`을 돌려준다.** 담기·빼기 모두 **DB에서 한 문장으로** 끝내야 한다 (있으면 넘어가는 삽입, 읽지 않는 삭제).

**T-247은 `ON CONFLICT`가 덮지 못하는 자리다.** 중복은 넘기지만 FK 위반은 그대로 올라온다 — 사용자에게 일어난 일은 "그 자료가 없다"이므로 계약대로 `404`여야 한다. 자료 행을 잠그지 않는 이유는 **잠글 값이 없기 때문이다** — 담기는 자료를 바꾸지 않아 잠금은 삭제를 기다리게 할 뿐이고 결과는 어차피 `404`다.

**T-246은 §3-2-4의 `sort` 함정과 같다.** 고정 정렬인 경로에 `Pageable`을 그대로 넘기면 순서가 덮이고 없는 속성 이름에 `500`이 난다.

### 관리자 승격 시도 제한

[3-3 결정 11](3-3-DESIGN-DECISIONS.md)·[3-2 §3-2-3](3-2-DESIGN-CONTRACT.md)이 요구하는 결과다 (#144).

| # | 조건 | 기대 |
|---|---|---|
| T-213 | 토큰이 틀린 시도 | **세어진다.** 거절은 예외로 나가고 승격 트랜잭션은 되돌아가므로, 그 안에서 잡으면 자리가 함께 사라진다 |
| T-214 | 상한만큼 실패한 뒤 **맞는 토큰**으로 호출 | 통과하지 못한다 (MUST). 승격되지 않는다 |
| T-215 | 잠긴 상태의 응답 | 다른 거절과 **같은 `403`·같은 문구** (MUST). 잠금은 로그에만 남는다 |
| T-216 | 한 계정이 잠긴 상태에서 **다른 계정**이 정상 호출 | 그대로 승격된다 — 한 사람이 잠겼다고 남의 절차가 막히면 안 된다 |
| T-217 | 정상적인 1회 승격 | **영향받지 않는다** (MUST). 최초 배포 절차가 이 제한에 막히면 안 된다 |
| T-218 | 몇 번 틀린 뒤 성공 | 그 계정의 시도 기록이 **지워진다** — 남기면 바로 다음 사고의 복구가 막힌다 |
| T-219 | 여러 계정으로 전체 상한을 채운 뒤 **한 번도 시도하지 않은 계정**이 맞는 토큰으로 호출 | 막힌다 (MUST). 계정 갈아타기를 전체 상한이 막는다 |
| T-220 | 같은 계정으로 **동시에** 상한의 몇 배를 보냄 | 잡히는 자리가 **상한까지다** (MUST) |

**T-215가 이 기능의 함정이다.** 잠금이 응답에 드러나면 **잠기는 시점을 재서 토큰이 맞았는지를 역으로 알아낼 수 있다** — 틀린 토큰만 세어진다면 잠기지 않는 시도가 곧 맞는 토큰이다. 그래서 잠금은 사유를 가르지 않는 규칙([3-2 §3-2-3](3-2-DESIGN-CONTRACT.md))에 그대로 포함된다.

**T-220이 이 제한이 실제로 서는지 본다.** 세어 보고 나중에 기록하면 동시에 도착한 요청들이 **모두 같은 옛 카운트를 읽고 전부 통과한다** — 순차 사례(T-214)만으로는 그 구현도 통과한다.

**T-219는 IP를 셀 수 없는 구조의 대비책을 본다.** 브라우저가 프록시를 거쳐 도착해 주소로 나눌 수 없으므로, 계정 갈아타기를 막는 것은 전체 상한뿐이다.

**T-217·T-218이 반대쪽 끝이다.** 제한이 사고 대응을 막으면 **안전장치가 사고의 원인이 된다.**

### 데이터 무결성

| # | 조건 | 기대 |
|---|---|---|
| T-21 | 자료 삭제 | `note_files`·`bookmarks` 레코드가 함께 사라진다 |
| T-22 | 같은 `(user, note)`로 즐겨찾기 두 번 | 두 번째는 실패하거나 멱등 처리 |
| T-23 | `category=EXAM`인데 `exam_type=NULL`로 INSERT | DB CHECK 제약 위반 |
| T-24 | 서로 다른 계정이 같은 학번으로 신청서 제출 | UNIQUE 제약 위반 |

### 가입 거부·회원 제거·권한 변경

[2-2 §2-2-2 §2-2-4 §2-2-5 §2-2-7](2-2-OPERATOR-REQUIREMENTS.md)이 요구하는 결과다 (#58).

| # | 조건 | 기대 |
|---|---|---|
| T-250 | 가입 거부 | 같은 id·`google_sub`·이메일·이름·생성일과 세션은 남고, `student_no`·`department`·`applied_at`만 `NULL`인 `USER/PENDING` 미승인 계정이 된다 (MUST) |
| T-250a | 거부된 계정의 기존 쿠키로 `/auth/me` 후 재신청 | 미승인 응답 뒤 같은 세션과 id로 신청서가 다시 저장된다. 세션 삭제·갱신은 없다 (MUST) |
| T-250b | 같은 신청을 두 번 거부 | 두 응답 모두 `rejected`에 id를 포함하지만 실제 `REJECT` 이력은 첫 변경 한 행뿐이다 |
| T-250c | 거부로 비워진 학번을 다른 계정이 신청 | UNIQUE 충돌 없이 저장된다 |
| T-250d | 거부와 같은 계정의 재신청을 동시에 실행 | 대상 행 잠금으로 직렬화된다. 최종 결과는 **거부 뒤 온전한 새 신청서** 또는 **재신청 뒤 `REJECT` 한 행과 함께 세 신청 필드가 모두 `NULL`** 중 하나이고, 이전 신청·부분 갱신은 남지 않는다 |
| T-251 | 거부 대상에 `ACTIVE`·`INACTIVE`·`SUSPENDED` 회원이 섞임 | 각 건은 `NOT_PENDING`으로 실패하고 원래 상태·신청 정보가 유지된다 — **회원 관리 경로를 우회할 수 없다** |
| T-252 | 거부 목록에 없는 id가 섞임 | 그 건은 `NOT_FOUND`, 성공한 신청 초기화는 살아남는다 |
| T-253 | 권한 부여 뒤 회수 | `GRANT_ADMIN`·`REVOKE_ADMIN`이 **따로** 남는다 |
| T-254 | 권한 변경 | **Status는 그대로다** (MUST) |
| T-255 | 이미 그 권한인 재요청 | 이력이 쌓이지 않는다 |
| T-256 | `PENDING`을 `ADMIN`으로 | `400 VALIDATION_ERROR` |
| T-257 | **마지막 활성 관리자가 자기 권한을 회수** | `403 FORBIDDEN` (MUST) |
| T-258 | 관리자가 둘일 때 자기 권한을 회수 | **허용된다** — 막아야 하는 것은 "마지막 1명이 사라지는 것"이다 |
| T-259 | 정지된 관리자만 남은 상태에서 회수 | `403` — 정지된 관리자는 활성으로 세지 않는다 |
| T-260 | 마지막 활성 관리자를 제거 | `403 FORBIDDEN` |
| T-261 | 회원 제거 | 계정이 사라지고 이력에 남는다 (**계정이 없어도 이력은 남는다**) |
| T-262 | 제거한 회원의 이력 | `SUSPEND` → `REMOVE` 순서 — **정지가 먼저 확정된다** (MUST) |
| T-263 | 로그인 중인 회원을 제거 | 그 사람의 세션이 **하나도 남지 않는다** (MUST) |
| T-264 | 제거한 회원이 올린 자료·공지 | **남고 작성자만 빈다** (MUST). 즐겨찾기는 함께 사라진다 |
| T-265 | **본인 제거** | `204`이되 **현재 세션과 토큰 쿠키가 함께 버려진다** (MUST). 그 쿠키로는 더 이상 열리지 않는다 |
| T-266 | `GET /admin/users/{id}/content-summary` | **네 값**(자료·공지·사진·게시글)을 **항상** 담는다. `0`도 담는다 |
| T-267 | **권한 회수 전에 만든 세션**으로 관리자 API 호출 | `403 FORBIDDEN`. 세션은 **지워지지 않고 갱신된다** (MUST) |
| T-268 | 권한 부여 전에 만든 세션으로 관리자 API 호출 | `200` — 다시 로그인하지 않아도 열린다 |
| T-269 | 제거된 회원이 들고 있던 세션으로 호출 | `401` — 갱신이 아니라 **폐기**다 |
| T-270 | 제거된 회원이 쓴 공지 조회 | `authorId`는 `null`, **`authorName`은 `"탈퇴한 회원"`** (MUST) |
| T-271 | **정지 확정과 삭제 사이에** 다른 관리자가 대상을 `ACTIVE`로 되돌림 | `409 CONCURRENT_CHANGE`. **지우지 않는다** (MUST) |
| T-272 | 활성 관리자 둘이 **동시에 각자 자기 권한을 회수** | 최소 한쪽이 실패하고 활성 관리자가 남는다 (MUST) |
| T-273 | 정지했는데 **세션 반영이 실패** | `500`. 상태는 `SUSPENDED`로 남고 **이력도 남는다** (MUST) |
| T-274 | 권한을 회수했는데 세션 반영이 실패 | 위와 같다 |
| T-275 | 반영이 실패한 뒤 **같은 요청을 다시 보냄** | 성공한다. 이력은 늘지 않는다 |
| T-276 | 정지 해제·권한 부여의 세션 반영이 실패 | **성공으로 답한다** — 완화되는 변경은 대상이 아니다 |

**T-262가 §2-2-4의 핵심을 지킨다.** 세션 폐기는 계정이 사라진 뒤라 실패해도 되돌릴 수 없다 — 정지가 먼저면 **어느 지점에서 실패하든 이미 막혀 있다.**

**T-265는 "지웠다"만 보면 놓친다.** 저장소의 세션 행을 지워도 **이 요청에 붙어 있는 세션은 응답을 내보낼 때 다시 저장되어** 방금 지운 `ADMIN` 세션이 되살아난다.

**T-257과 T-258이 한 벌이다.** 막는 기준은 "자기 자신인가"가 아니라 **"이 조작 뒤에도 활성 관리자가 남는가"** 이고, 하나만 보면 반대쪽이 틀린 채로 통과한다.

**T-264는 `notices.author_id`가 `V1`에 `ON DELETE` 절 없이 만들어져 있었기 때문에 필요하다.** 그대로면 **공지를 한 번이라도 쓴 관리자는 삭제 자체가 FK 위반으로 실패한다.**

**T-267~T-269는 DB가 아니라 세션을 본다.** 권한 회수의 핵심은 `role` 컬럼이 바뀌는 것이 아니라 **그 사람이 지금 들고 있는 세션이 다음 요청에서 막히는 것**이다 — 인가는 매 요청 세션 값으로 판단하고 필터는 `users`를 다시 읽지 않는다 ([3-3 결정 12](3-3-DESIGN-DECISIONS.md)). DB와 이력만 확인하면 **세션 반영이 통째로 빠져도 통과한다.** 그래서 **회수 전에 만든 그 쿠키를 그대로 다시 보낸다.**

**T-276이 T-273~T-275를 지킨다.** 없으면 "무조건 확인한다"로 고쳐도 나머지가 전부 통과한다 — 그러면 정지 해제나 권한 부여가 세션 저장소 장애에 `500`으로 실패하는데, 그쪽은 실패로 알릴 이유가 없는 방향이다 ([2-2 §2-2-5](2-2-OPERATOR-REQUIREMENTS.md#차단이-강해지는-변경은-세션에-닿아야-성공이다)).

**T-275는 "값이 이미 목표와 같아도 반영을 건너뛰지 않는다"를 지킨다.** 건너뛰면 한 번 실패한 정지는 **영영 세션에 닿지 못한다** — 재요청이 유일한 복구 수단인데 그 길이 막힌다.

**T-271은 시간 경합으로 재현하지 않는다.** 두 스레드를 띄우면 언제 어긋나는지가 스케줄러에 달려 있어 조용히 통과하는 날이 생긴다. **끼어드는 지점을 지목한다** — 정지 반영이 끝나는 순간에 대상을 되살려 두고, 삭제가 그것을 보고 멈추는지 본다.

**T-272는 "각자 자기 것을 회수"여야 한다.** 서로를 대상으로 하면 두 요청이 같은 두 행(요청자·대상)을 잠그므로 **활성 관리자 전부를 모아 잠그는 코드가 없어도 자연히 줄이 선다** — 그 조합으로는 이 사례를 재현하지 못한다. 각자 자기 것을 회수하면 잠그는 행이 자기 하나뿐이라 모아 잠그는 코드만이 유일한 방어가 된다.

**T-270은 T-264가 끝나는 자리에서 시작한다.** FK가 작성자 자리를 비우는 것까지가 T-264이고, **화면이 읽는 것은 응답이다** — 서버가 채우지 않으면 화면마다 문구가 갈리거나 작성자 줄이 통째로 비어 글이 깨진 것처럼 보인다 ([3-2 §3-2-2](3-2-DESIGN-CONTRACT.md)).

### 관리자 조작 이력

[2-2 §2-2-7](2-2-OPERATOR-REQUIREMENTS.md#2-2-7-안전장치)이 요구하는 결과다. **남기지 않은 것은 나중에 만들 수 없다.**

| # | 조건 | 기대 |
|---|---|---|
| T-205 | 두 명을 한 번에 승인 | **대상마다 한 행**. 행위자·대상·동작·시각이 들어 있다 |
| T-206 | 승인 요청에 실패하는 건이 섞임 | 성공한 건만 남는다. 아무것도 바뀌지 않은 건은 남기지 않는다 |
| T-207 | 정지한 뒤 해제 | `SUSPEND`와 `ACTIVATE`가 **따로** 남고, **시각이 그 순서를 따른다** — 뭉치면 방향을, 시각이 뒤집히면 순서를 알 수 없다 |
| T-208 | 이미 정지된 회원을 다시 정지 | **한 행만 남는다.** 재요청은 갱신 실패의 복구 수단이라 허용되지만, 그때마다 쌓이면 두 번 정지된 것으로 읽힌다 |
| T-209 | 최초 관리자 승격 | `PROMOTE_ADMIN`이 남고 **토큰은 남지 않는다** (MUST). 재요청은 남기지 않는다 |
| T-210 | 승격이 거절됨 | 이력이 비어 있다 — 반복 시도는 [#144](https://github.com/HackerKHU/hacker_HP/issues/144)의 몫이다 |
| T-211 | **이력 저장이 실패한 채로 정지** | **정지는 성공한다** (MUST). 예외가 올라가지 않고 DB는 `SUSPENDED`다 |
| T-212 | 변경 트랜잭션 안에서 기록을 시도 | 거부한다. 그 안에서 남기면 **기록 실패가 조작을 되돌린다** |

**T-211이 이 기능의 핵심 조건이다.** 기록이 조작의 조건이 되면 **이력 저장소가 죽었을 때 정지를 못 한다** — 안전장치가 사고의 원인이 된다. 예외가 올라가면 관리자는 "정지 실패"로 읽고 다시 누르는데 실제로는 정지돼 있다.

**T-212는 그 반대 방향을 막는다.** 같은 트랜잭션에서 남기면 원자적이지만 T-211이 깨진다. 조용히 어긋나는 종류라 코드가 그 자리에서 끊는다.

### 회원 제거

회원 제거는 `Post Launch`(#58)라 **구현하는 PR부터 적용한다.** 여기 적어 두는 이유는, 이 여섯이 [2-2 §2-2-4](2-2-OPERATOR-REQUIREMENTS.md#2-2-4-회원-제거)가 정한 결과 전부이기 때문이다 — 하나라도 빠지면 조용히 어긋난다.

| # | 조건 | 기대 |
|---|---|---|
| T-187 | 자료·공지·사진을 남긴 회원을 삭제 | **삭제가 성공한다.** 세 콘텐츠는 그대로 남고 작성자 id만 `NULL`이 된다 (MUST) |
| T-188 | 그 자료의 `note_files` | 함께 남는다. 자료가 남는데 파일이 사라지면 열 수 없는 자료가 된다 |
| T-189 | 그 회원의 즐겨찾기 | **함께 사라진다** — 여기만 `CASCADE`다 |
| T-190 | 삭제된 회원이 올린 자료·공지·사진을 조회 | `uploaderName`/`authorName`이 **`"탈퇴한 회원"`** 이다. `null`이 아니다 ([3-2 §3-2-2](3-2-DESIGN-CONTRACT.md#3-2-2-테이블-정의)) |
| T-191 | 로그인 중인 회원을 삭제 | 그 회원의 다음 요청이 **`401 UNAUTHENTICATED`** (MUST). 세션을 갱신하지 않고 폐기하므로 T-32의 `403`이 아니다 |
| T-192 | 삭제된 회원이 남긴 자료를 다른 회원이 수정·삭제 시도 | `403 FORBIDDEN`. 소유자 판단은 id로 하므로 작성자가 비었다고 아무나 손대지 못한다 |
| T-193 | 로그인이 계정을 읽은 **뒤, 세션이 저장되기 전에** 그 계정을 삭제 | 삭제가 끝난 뒤 그 사람의 세션이 **하나도 남지 않는다** (MUST). 로그인은 실패한다 |
| T-194 | 자료·공지·사진·게시글을 남긴 회원의 제거 확인 창 (화면) | 남게 될 **건수를 종류별로** 보여준다 (MUST). 관계를 끊고 나면 그 회원의 콘텐츠를 다시 찾을 수 없다 |
| T-196 | 계정 삭제는 커밋됐는데 **세션 폐기가 실패** | 남은 세션이 **`403 SUSPENDED`로 막힌다** (MUST). 정지를 먼저 확정하므로 폐기가 실패해도 열려 있지 않다 |
| T-197 | 활성 관리자가 둘일 때 **관리자가 자기 자신을 제거** | 그 요청의 **다음 요청이 `401`** (MUST). 응답을 내보낼 때 현재 세션이 되살아나지 않는다 |
| T-195 | 비로그인으로 `/privacy` 열람 (화면) | "보관과 파기"가 **콘텐츠는 남고 표시만 바뀐다**는 것과 **본문에 담긴 정보는 그대로 남는다**는 것을 함께 알린다 |

**T-196과 T-197은 "폐기했다"만 보면 놓친다.** T-196은 폐기가 **실패한** 경우이고, T-197은 폐기한 세션이 **응답을 내보낼 때 되살아나는** 경우다. 둘 다 정상 경로(T-191)를 통과한 뒤에 열린다.

**T-193은 미룰 수 없다.** 폐기하는 쪽에서 아직 보이지 않는 세션이 잠시 뒤 저장되면 **계정은 없는데 `ACTIVE`·`ADMIN` 세션만 남는다.** 승인·정지는 재로그인이 회복 수단이지만([3-1 §3-1-5](3-1-DESIGN-ARCHITECTURE.md)) **제거에는 돌아갈 계정이 없어 만료까지 아무것도 되돌리지 못한다.** 그래서 회원 제거(#58)는 [#127](https://github.com/HackerKHU/hacker_HP/issues/127) 뒤에 온다.

**T-187이 나머지의 전제다.** 세 FK 중 하나라도 `ON DELETE` 절이 빠지면 기본값 `NO ACTION`이라 **삭제 자체가 FK 위반으로 실패한다** — 그러면 T-188~T-192는 실행조차 되지 않는다.

T-191은 T-32와 짝이다. 정지는 세션을 **갱신**해 `403 SUSPENDED`가 되고, 제거는 세션을 **폐기**해 `401`이 된다. 지울 계정에는 세션에 써넣을 `role`·`status`가 없다.

### 파일

| # | 조건 | 기대 |
|---|---|---|
| T-25 | 20MB 초과 파일로 presigned URL 요청 | `413 FILE_TOO_LARGE` |
| T-26 | 허용되지 않은 확장자로 요청 | `415 UNSUPPORTED_FILE_TYPE` |
| T-27 | presigned URL 유효시간 경과 후 접근 | S3가 거부 |
| T-28 | S3 오브젝트 키를 직접 브라우저로 열기 | 403 (버킷 비공개) |

**T-25는 발급 조건만 검사하는 것으로 부족하다.** presigned PUT은 실제 업로드 용량을 강제하지 못하므로, 등록(③) 단계에서 실제 오브젝트 크기를 확인하는 경로까지 테스트한다 (MUST) — [2-1 §2-1-2](2-1-USER-STORIES.md).

**T-27·T-28은 실제 S3가 있어야 한다.** 배포 리허설([#48](https://github.com/HackerKHU/hacker_HP/issues/48))의 수동 점검 항목이다 — 아래 T-277~는 그 앞까지, **우리가 정한 규칙**을 본다.

#### 자료 업로드·등록 (#53)

| # | 조건 | 기대 |
|---|---|---|
| T-277 | 발급받은 키의 모양 | `notes/uploads/{내 id}/{uuid}.{ext}` — **임시 자리**이고 하루 뒤 사라진다 |
| T-278 | 확장자와 크기가 **둘 다** 어긋남 | `415` — **확장자를 먼저 답한다** |
| T-279 | 대문자 확장자(`.PDF`) | 통과한다 |
| T-280 | 한 요청의 파일 중 하나만 어긋남 | **아무것도 발급하지 않는다** |
| T-281 | 개수 상한 초과 | `400 VALIDATION_ERROR` |
| T-282 | 등록 성공 | 파일이 `notes/{uuid}.{ext}`로 옮겨지고 **임시본은 사라진다** |
| T-283 | 응답 본문 | **S3 키가 어디에도 없다** (MUST) |
| T-284 | **남이 발급받은 키**로 등록 | `403 FORBIDDEN`. 그 파일은 건드리지도 않는다 |
| T-285 | 올린 파일이 실제로 상한 초과 | `413`. **그 오브젝트를 지운다** (MUST) |
| T-286 | 올리지 않은 키로 등록 | `400` — 서버 장애가 아니라 클라이언트 실수다 |
| T-287 | 여러 파일 중 하나가 실패 | **최종 자리에 아무것도 남지 않는다** |
| T-288 | 저장이 실패 | 옮겨 둔 파일을 **도로 지운다**. 임시본은 남는다 |
| T-289 | 같은 키를 두 번 담음 | 한 번만 등록된다 |
| T-290 | 본문에 남의 `uploaderId` | 무시된다 — 업로더는 **세션에서만** 온다 (MUST) |
| T-291 | **잰 직후** 같은 키에 다른 파일을 올림 | `400`. **옮기지 않는다** (MUST) |
| T-292 | 같은 임시 키로 두 번 등록 | 최종 키가 갈린다 — **첫 자료의 파일이 바뀌지 않는다** (MUST) |
| T-293 | 발급은 `safe.pdf`, 등록은 `malware.exe` | `415` — **등록의 이름도 검사한다** (MUST) |
| T-294 | 인가를 지난 뒤 업로더가 정지됨 | `403 SUSPENDED`. 커밋되지 않는다 (MUST) |
| T-295 | `HeadObject`가 **본문 없는 404**를 던짐 | "없음"으로 본다 — `500`이 아니라 `400`이 나가야 한다 |
| T-296 | 커밋 **뒤에** 실패 | 파일을 지우지 않는다 — 저장된 자료가 껍데기가 되면 안 된다 |

**T-283이 §3-2-4의 "키를 어디에도 담지 않는다"를 지킨다.** 버킷이 비공개라 키를 알아도 열 수는 없지만, 키 구조를 드러낼 이유가 없다.

**T-284가 없으면 발급 자체가 무의미하다.** 등록은 "올라온 키 목록"을 그대로 받으므로, 키 문자열만 알면 **남이 올린 파일을 자기 자료로 삼을 수 있다.**

**T-288이 가장 조용한 사고를 막는다.** 파일은 저장하기 **전에** 최종 자리로 옮겨진다 — 그 뒤 저장이 실패하면 그 파일은 **아무도 지울 수 없는 것**이 된다. 최종 자리에는 만료 규칙이 없고(자료는 오래 남아야 한다), DB에 행이 없어 찾을 실마리도 없다.

**T-291은 갈아치우는 시점이 `HeadObject` 다음이어야 한다.** 미리 갈아 두면 등록이 **갈린 값을 재게 되어** 이 사례가 재현되지 않는다 — 통과하는 테스트가 아무것도 지키지 못한다.

**T-295는 가짜 저장소로 잡히지 않는다.** `HeadObject`는 응답 본문이 없어 SDK가 오류 코드를 읽지 못하고, 키가 없을 때 `NoSuchKeyException`이 아니라 **상태 코드만 404인 밋밋한 예외**가 올라올 수 있다. 그 갈래는 S3 클라이언트를 흉내낸 단위 테스트가 본다.

#### 자료 내려받기 (#55)

| # | 조건 | 기대 |
|---|---|---|
| T-298 | `ACTIVE`가 발급 요청 | `url`·`originalName`·`expiresAt`을 받는다 |
| T-299 | 발급된 서명 | **`attachment`와 원본 파일명**이 담겨 있다 (MUST) |
| T-300 | 응답 본문 | **S3 키를 담는 필드가 없다** (MUST) |
| T-301 | **다른 자료의 `fileId`** | `404` — 경로가 거짓말하게 두지 않는다 |
| T-302 | 비로그인·`PENDING`·`SUSPENDED` | `401`·`403` (MUST) |
| T-303 | 남이 올린 자료 | **받을 수 있다** — 자료 공유가 이 사이트의 존재 이유다 |
| T-304 | 파일명에 공백 | `+`가 아니라 `%20`으로 인코딩된다 |
| T-305 | 내려받기 URL의 수명 | 업로드보다 **짧다** |
| T-306 | 파일명에 `*` | `%2A`로 인코딩된다 — `attr-char`에 없는 글자다 |
| T-307 | 서명이 오래 걸림 | 알려 준 `expiresAt`이 **실제 만료보다 늦지 않다** |

#### 자료 수정·삭제 (#54)

| # | 조건 | 기대 |
|---|---|---|
| T-308 | 본인이 메타데이터 수정 | 바뀐다. **업로더는 그대로다** (MUST). 보내지 않은 `professor`는 비워진다 |
| T-309 | `files`에서 뺀 파일 | 첨부가 사라지고 **S3 오브젝트도 정리된다** |
| T-310 | **남의 자료 수정** | `403 FORBIDDEN` (MUST). 내용이 그대로다 |
| T-311 | `ADMIN`이 남의 자료 수정 | 된다. **업로더는 여전히 원래 사람이다** |
| T-312 | 다른 자료의 `fileId` | `400` |
| T-313 | `fileId`와 `key`를 둘 다 / 둘 다 없이 | `400` |
| T-314 | 삭제 | 첨부·즐겨찾기 레코드와 S3 오브젝트가 함께 사라진다 (MUST) |
| T-315 | **남의 자료 삭제** | `403`. 파일도 그대로다 |
| T-316 | 삭제했는데 **S3 정리가 실패** | 그래도 `204` (SHOULD) |
| T-317 | `files`에 `null`이 섞임 | `400` — `500`이 아니다 |
| T-318 | 새 첨부에 `originalName`이 없음 | `400` — `415`가 아니다 |
| T-319 | 같은 자료를 **동시에** 수정 | 한 줄로 선다. **어느 쪽도 `500`이 아니고** 첨부는 한 벌만 남는다 |

**T-310·T-315가 §3-1-7의 요지다.** 화면이 버튼을 숨기는 것은 권한이 아니다.

**T-311이 없으면 "업로더가 바뀌지 않는다"를 지킬 수 없다.** 본인이 고칠 때는 업로더가 그대로여도 티가 나지 않는다 — **관리자가 남의 것을 고칠 때만** 드러난다.

**T-316이 T-314의 반대편이다.** 정리를 응답의 조건으로 만들면 재요청해도 자료가 없어 영원히 실패한다 — 그래서 §2-1-3이 S3 삭제를 SHOULD로 둔다.

**T-317은 `@Valid`의 빈틈이다.** Bean Validation은 **원소가 `null`인 것 자체를 위반으로 보지 않는다** — `@NotEmpty`·`@Size`·`@Valid`를 모두 통과하고 뒤에서 `NullPointerException`이 난다.

**T-318은 코드가 아니라 안내가 문제다.** 이름이 없어도 빈 확장자가 허용 목록에 없어 `415`로 걸리기는 한다. 그런데 그것은 **파일 형식 문제가 아니라 요청 형식 문제라**, `415`로 답하면 화면은 "이 파일은 못 올린다"고 안내하고 사용자는 **멀쩡한 파일을 바꾸려 든다.**

**T-319는 "둘 다 남지 않는다"만 보면 부족하다.** 잠그지 않아도 뒤엣것은 낡은 목록을 들고 저장하다 터져 데이터는 멀쩡해 보인다 — **멀쩡한 요청이 `500`으로 거절되는 것**이 실제 증상이다. 요청자 계정 행을 잠그는 것으로는 막히지 않는다. **소유자와 관리자는 서로 다른 행을 잠그기 때문이다.**

**권한 없는 수정은 파일을 옮기기도 전에 끊는다.** 옮기고 나서 거절하면 되돌릴 것이 생기고, 되돌리기가 실패하면 만료 규칙 없는 자리에 주인 없는 파일이 남는다.

**T-299가 이 기능의 절반이다.** S3 키는 `uuid`라 담지 않으면 알아볼 수 없는 이름으로 저장되는데, **프론트가 `<a download="…">`로 고칠 수 없다** — 그 힌트는 다른 오리진 링크에서 무시된다. "받아지긴 한다"로 넘어가면 사용자는 `0f8c….pdf` 열 개를 손으로 이름 바꿔야 한다.

**T-304·T-306은 `URLEncoder`의 함정이다.** 그것은 폼 인코딩이라 RFC 5987과 두 군데가 어긋난다 — 공백을 `+`로 바꾸고(RFC는 `+`를 더하기 기호로 읽어 이름에 `+`가 박힌다), `*`를 그대로 둔다(`attr-char`에 없어 **엄격한 클라이언트는 파라미터를 통째로 무시**한다). 나머지 예약 문자(`. - _`)는 `attr-char`에 있어 건드리지 않는다.

**T-307은 틀리는 방향이 중요하다.** S3의 만료는 **서명이 만들어진 순간**부터 센다 — 기준 시각을 서명 뒤에 잡으면 우리가 알려 준 값이 실제보다 늦어져 **"아직 유효하다고 적혀 있는데 S3는 거절하는"** 구간이 생긴다. 반대로 이르게 알리는 것은 다시 발급받으면 그만이라 안전하다.

**T-301은 지금은 손해가 없다.** 모든 `ACTIVE`가 모든 자료를 받을 수 있기 때문이다. 그래도 막는 것은 **수정·삭제(#54)가 소유자를 따지기 시작할 때 이 경로만 기준이 다른 채로 남지 않게** 하기 위해서다.

**T-27·T-28(실제 만료·버킷 비공개)은 여기서 확인되지 않는다.** 진짜 S3가 필요해 배포 리허설(#48)의 수동 점검이 맡는다.

**T-296이 T-288의 반대편이다.** 정리 범위가 커밋 뒤까지 넘어가면 **이미 저장된 자료의 파일만 사라져 고칠 수 없는 껍데기**가 남는다 — 둘을 함께 두어야 범위가 고정된다.

**S3는 가짜로 갈아끼운다.** presigned 서명은 SDK가 하는 일이고, 여기서 지키는 것은 **발급 조건·크기 검증·정리 순서**다. LocalStack을 띄우면 컨테이너가 하나 더 늘고 CI가 느려지는데 그 대가로 얻는 것이 없다.

### 자유 게시판 (#235)

[2-1 §2-1-8](2-1-USER-STORIES.md#2-1-8-자유-게시판)이 요구하는 결과다. **이 저장소에서 일반 부원이 자유 서술을 남기는 첫 기능이라**, 지금까지 없던 표면이 함께 생긴다.

| # | 조건 | 기대 |
|---|---|---|
| T-321 | `ACTIVE`가 글을 쓰고 목록·상세를 조회 | 된다 |
| T-322 | 비로그인·`PENDING`·`SUSPENDED` | `401`·`403` |
| T-323 | 본문에 `<script>`나 HTML을 담아 등록 | **글자 그대로 저장되고 글자 그대로 나온다** (MUST) |
| T-324 | 요청 본문에 `authorId`를 담아 등록 | 무시된다 — 작성자는 **세션에서만** 온다 (MUST) |
| T-325 | 제목·본문이 **비었거나 공백뿐**이거나, 상한(200자 / 10,000자)을 넘음 | `400 VALIDATION_ERROR` |
| T-326 | `GET /posts` 응답 | **본문을 담지 않는다** (MUST) |
| T-327 | `sort`에 **이상한 값**과 **유효한 값**(`title` 등)을 각각 보냄 | `500`이 나지 않고, **순서가 바뀌지도 않는다** (MUST) |
| T-328 | 같은 시각에 등록된 글이 여럿인 페이지 넘김 | **같은 글이 두 번 보이거나 빠지지 않는다** |
| T-329 | 글을 쓴 회원을 제거 | **회원이 지워지고**, 그 글은 남고 작성자가 "탈퇴한 회원"이 된다 (MUST) |
| T-330 | **화면이** `<img src=x onerror=…>`가 **제목과 본문에** 담긴 글을 그림 | 목록·상세 **어디서도** 그 요소가 **DOM에 생기지 않고** 글자로 보인다 (MUST) |
| T-331 | DB에 10,000자를 넘는 본문을 **직접** INSERT | CHECK 제약 위반 |
| T-332 | 제목·본문을 **이모지로** 상한(200자 / 10,000자)만큼 채워 등록 | 된다 — 보충 문자도 **한 글자** (MUST) |
| T-333 | 같은 이모지로 상한을 **한 자** 넘김 | `400 VALIDATION_ERROR` |
| T-463 | `ADMIN`이 글을 삭제 | `204`. 목록·상세 어디서도 다시 보이지 않는다 (MUST, #238) |
| T-464 | 작성자 본인이 자기 글을 삭제 | `403 FORBIDDEN`. 예외가 없다 — 관리자 전용이다 |
| T-465 | 비로그인·CSRF 토큰 없이 삭제 | 각각 `401`·`403` |
| T-466 | 없는 글을 삭제 | `404 NOT_FOUND` |
| T-467 | **(웹)** 게시글 상세를 `ACTIVE ADMIN`과 그 밖의 세션으로 렌더 | 작성자와 무관하게 관리자에게만 destructive `삭제`가 보인다. 작성자인 USER·INACTIVE·PENDING·SUSPENDED·비로그인은 진입점이 없다 |
| T-468 | 관리자가 `삭제`를 열기 | 제목과 완전 삭제·되돌릴 수 없음을 알리는 modal `AlertDialog`가 뜨며 기존 전역 scrollbar 보정으로 레이아웃이 밀리지 않는다 |
| T-469 | 확인 창에서 취소 | DELETE를 부르지 않고 같은 상세에 남으며 초점이 삭제 트리거로 돌아간다 |
| T-470 | 삭제 확인 성공 | 정확히 한 번 DELETE하고 `게시글을 삭제했습니다.` live alert를 다음 `/posts` 이동에 보존한다. 상세 history는 `replace`한다 |
| T-471 | React disabled 렌더 전에 확인을 빠르게 두 번 실행 | 동기 잠금이 DELETE를 한 번만 보낸다 |
| T-472 | 삭제가 `403`·`404`로 실패 | 서버 사유를 오류 alert로 보여주고 성공 알림·목록 이동이 없다 |
| T-473 | 삭제가 네트워크(status 0)·5xx로 실패 | 삭제 여부를 확정할 수 없고 반영되었을 수 있으니 목록에서 확인하라고 알리되 성공처럼 이동하지 않는다 |
| T-474 | 삭제가 세션 전이 오류로 실패 | `reportApiError()`에 맡기고 중복 오류 alert나 성공 이동을 만들지 않는다 |
| T-475 | **(웹 API·fixture)** 삭제 | CSRF를 실은 `DELETE /posts/{id}`이고, admin fixture는 행을 제거해 상세 `NOT_FOUND`·목록 total 감소가 이어지며 USER fixture는 자기 글도 `FORBIDDEN`이다 |
| T-476 | 삭제 인가 뒤 DB에서 요청자의 관리자 권한 회수 또는 상태 정지 | 기존 ADMIN·ACTIVE 세션을 그대로 보내도 `403 FORBIDDEN`이고 글은 남는다. 서비스가 잠근 최신 사용자 행의 role·status를 커밋 직전에 다시 확인한다 (MUST) |

**T-325는 상한만 보면 안 된다.** PostgreSQL의 `NOT NULL`은 `""`나 공백 문자열을 막지 않는다 — 구현에서 `@NotBlank`가 빠져도 상한만 재는 검사는 통과하고 **내용 없는 글이 저장된다.**

**T-327의 절반은 "유효한 값"이다.** 이상한 값만 보면, Spring의 `Pageable`이 `sort=title`을 그대로 받아 **실제 순서를 바꾸는 구현도 통과한다** — 그러면 `created_at DESC, id DESC` 고정이라는 MUST가 깨지고 페이지가 흔들린다. 보내도 무시되는지를 함께 잰다.

**T-331은 T-325로 대체되지 않는다.** 상한을 요청 검증과 DB 양쪽에 두기로 했는데(§3-2-2), API만 재면 `@Size`만 넣고 **`CHECK`를 마이그레이션에서 빠뜨려도 전부 통과한다.** DB 제약의 목적은 API가 아닌 경로를 막는 것이므로 DB에 직접 넣어 봐야 한다.

**T-332는 두 상한이 같은 단위인지를 잰다.** PostgreSQL의 `LENGTH()`는 코드포인트를 세지만 Bean Validation의 `@Size`는 `String.length()` — UTF-16 단위 — 를 센다. 그래서 `@Size(max = 10000)`을 그대로 쓰면 **DB가 받아 줄 이모지 글을 API가 먼저 `400`으로 거절한다.** T-325·T-331은 둘 다 `가` 같은 기본 다국어 평면 문자로 재기 때문에 이 어긋남을 통과시킨다.

**T-333이 없으면 상한을 아예 없애도 T-332가 통과한다.** 경계는 양쪽에서 재야 한다.

**T-323과 T-330이 한 벌이다. 둘 중 하나만으로는 못 잡는다** (MUST).

T-323은 **서버가 문자열을 건드리지 않는지**만 본다 — 화면이 그 문자열을 `dangerouslySetInnerHTML`로 꽂아도 **T-323은 그대로 통과한다.** 반대로 T-330만 두면 서버가 조용히 정화하거나 변형하는 것을 놓친다.

**막는 자리는 화면이다.** 서버가 평문으로 저장·응답하고 화면이 텍스트 노드로 그리면 React가 자동으로 이스케이프한다 — 마크다운 렌더러나 `dangerouslySetInnerHTML`을 붙이는 순간 그 보장이 사라진다. 그래서 T-330은 **"실행되지 않는다"가 아니라 "요소가 DOM에 생기지 않는다"** 로 잰다. `onerror`가 우연히 안 터지는 것과 요소 자체가 없는 것은 다르다.

**제목도 함께 본다.** 제목은 **목록과 상세 양쪽에서** 다른 부원에게 보이는데, 본문만 검사하면 **본문은 텍스트 노드로 그리면서 제목만 HTML로 그리는 구현**이 통과한다. 저장형 XSS는 어느 한 자리만 뚫려도 성립한다.

관리자만 쓰는 공지와 달리 **승인된 모든 부원이 쓰는 입력**이라 위험도가 다르다.

**T-329는 스키마를 본다.** `posts.author_id`에 `ON DELETE SET NULL`이 없으면 **회원 삭제 자체가 FK 위반으로 실패한다** — 글이 남는지가 아니라 **회원을 지울 수 있는지**가 먼저 깨진다. `notices.author_id`가 정확히 그랬다 (#58).

**T-326은 응답 크기 문제다.** 본문 상한이 10,000자라 20건이면 그것만으로 200KB다. 자료 목록이 파일을 개수만 담는 것과 같은 판단이다.

**T-327·T-328이 한 벌이다.** 정렬 옵션을 아예 받지 않으므로 `500`이 날 자리가 없고(#52의 `sort=bogus` 사고), `created_at DESC, id DESC` 고정이라 페이지가 흔들리지 않는다 — **`id`가 마지막 기준이 아니면 같은 시각의 글들이 페이지마다 다르게 줄 선다.**

**T-464가 이 절의 핵심이다** (#238). 삭제는 관리자 전용이고 **작성자 본인도 예외가 아니다** — `hasRole('ADMIN')` 대신 소유자 검사를 흘려 넣으면 이 테스트가 잡는다. T-463은 완전 삭제를 잰다 — 목록에서만 빠지고 상세는 그대로 열리는 절반짜리 구현을 통과시키지 않는다. **T-476은 세션 인가와 DB 권한 사이의 시간차를 잰다.** 세션의 ADMIN·ACTIVE만 믿으면 권한 회수·정지가 이미 커밋된 뒤에도 되돌릴 수 없는 삭제가 통과한다.

### 비활동 부원과 학기 전환 (#228)

[3-1 §3-1-2](3-1-DESIGN-ARCHITECTURE.md#비활동-부원inactive은-자료만-막힌다)와 [2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md#학기-전환--일괄-비활성화와-복구)이 요구하는 결과다. **상태가 경로마다 다르게 작용하는 첫 사례라**, "막히는가"보다 **"막히지 않아야 할 것이 열려 있는가"** 가 잡기 어렵다.

| # | 조건 | 기대 |
|---|---|---|
| T-334 | `INACTIVE`가 로그인 | **된다** — `SUSPENDED`와 다르다 (MUST) |
| T-335 | `INACTIVE`가 공지 조회·활동사진 조회·게시판 조회·게시판 작성 | **전부 된다** (MUST) |
| T-336 | `INACTIVE`가 자료 목록·상세·다운로드 URL 발급 | `403` + 코드 **`INACTIVE`** (MUST) |
| T-337 | `INACTIVE`가 자료 업로드·수정·삭제·즐겨찾기 추가·해제 | `403 INACTIVE` |
| T-338 | **§3-2-4 표의 열한 경로 전부**를 `INACTIVE` 세션으로 한 번씩 호출 | **하나도 빠짐없이** `403 INACTIVE` (MUST) |
| T-339 | **본문 없는 전원** 비활성화 실행 | `ACTIVE`인 일반 부원만 `INACTIVE`가 되고, **`ADMIN`·`SUSPENDED`·`PENDING`은 그대로다** (MUST) |
| T-340 | 비활성화 응답 | `deactivated`에 **실제로 바뀐 id만** 담긴다. 이미 `INACTIVE`였던 사람은 없다 |
| T-341 | 비활성화를 **연달아 두 번** | 두 번째는 `{"deactivated": []}`이고 아무것도 바뀌지 않는다 |
| T-342 | 이용 중인 `ACTIVE`의 **기존 세션**이 있는 상태에서 비활성화 | 그 세션의 **다음 자료 요청이** `403 INACTIVE` (MUST) |
| T-343 | 세션 반영이 실패하도록 만든 뒤 비활성화 | `500 INTERNAL_ERROR`, **상태 변경은 되돌아가지 않는다** (MUST) |
| T-344 | 위 실패 뒤 **같은 요청을 다시** 보냄 | 이미 `INACTIVE`인 사람의 세션도 다시 반영된다 (MUST) |
| T-345 | 복구에 `INACTIVE`·`SUSPENDED`·없는 id를 섞어 보냄 | `200` + `reactivated`에 첫째만, 나머지는 `NOT_INACTIVE`·`NOT_FOUND` |
| T-346 | `PATCH /admin/users/{id}/status`에 `"INACTIVE"` | `400 VALIDATION_ERROR` (MUST) |
| T-347 | `INACTIVE` 대상 `PATCH .../status` = `SUSPENDED` | `200`, `SUSPENDED`가 된다 |
| T-348 | 위를 다시 `ACTIVE`로 해제 | `ACTIVE`다 — **`INACTIVE`로 돌아가지 않는다** |
| T-349 | `INACTIVE`/`SUSPENDED USER` 대상 `PATCH .../role` = `ADMIN` | `200`, 한 트랜잭션에서 `ACTIVE ADMIN`, `deactivated_at=NULL`로 끝난다. 세션에도 중간 invalid 조합이 없고 `GRANT_ADMIN` 이력은 한 행이다 (MUST) |
| T-350 | 비활성화·복구 뒤 `admin_actions` | 대상마다 한 행, `DEACTIVATE`·`REACTIVATE`로 **갈려 있다** (MUST) |
| T-351 | 비활성화를 두 번째로 실행 (바뀐 사람 없음) | 이력에 **행이 늘지 않는다** — 아무것도 바꾸지 않은 재요청은 남기지 않는다 |
| T-352 | `GET /admin/users?status=INACTIVE` | 비활동 회원만 나온다 |
| T-353 | **(웹)** 계약에 없는 `status` 값을 받음 — 구버전 번들 | **던지지 않는다.** 비로그인으로 정리하고 로그인 화면을 보여준다 — `ACTIVE`로 취급하지 않는다 (MUST) |
| T-354 | **(웹)** 자료 요청이 `403 INACTIVE`로 실패 | 세션이 `INACTIVE`로 정리된다 — 무시하지 않는다 (MUST) |
| T-355 | **(웹)** 위 직후의 화면 | **로그인 화면으로 가지 않는다.** 공지·게시판은 그대로 열려 있다 (MUST) |
| T-356 | **(하위호환 전원 API를 UI로 제공하는 별도 소비자)** 전원 비활성화를 누름 | 확인 창의 건수가 `status=ACTIVE&role=USER`의 `page.totalElements`다 — **현재 페이지의 행 수가 아니다** (MUST). #297 회원 관리 화면에는 이 버튼이 없다 |
| T-357 | **(위 별도 소비자)** 그 확인 창에서 취소 | 본문 없는 전원 비활성화 API를 **부르지 않는다** |
| T-358 | 활성 관리자 0명, 자격을 갖춘 `INACTIVE` 계정이 부트스트랩 호출 | `204`, 그 계정이 **`ACTIVE` `ADMIN`이다** — `INACTIVE ADMIN`도, `403`도 아니다 (MUST) |
| T-359 | 두 관리자가 비활성화를 **동시에** 호출 | 한 id는 **한쪽 응답에만** 담기고, 이력도 대상마다 한 행이다 (MUST) |
| T-360 | 세션 반영이 실패하도록 만든 뒤 비활성화 | 응답은 일반 `500`이지만 **바뀐 회원의 `deactivated_at`이 남아 있다** (MUST) |
| T-361 | `INACTIVE` 대상 `PATCH .../status` = `ACTIVE` | `400 VALIDATION_ERROR` — 개별 복구 경로를 만들지 않는다 (MUST) |
| T-362 | **(웹)** 회원 목록에 `INACTIVE` 회원이 있음 | 상태 칸이 **"비활동"** 이다 — `SUSPENDED`와 **같은 낱말이 아니다** (MUST) |
| T-363 | **(웹)** 상태 필터에서 "비활동"을 고름 | `status=INACTIVE`로 조회하고 주소에 남는다 |
| T-364 | **(하위호환 복구 API adapter)** 비활동 회원 둘을 복구 | `POST /admin/users/reactivate`에 **고른 id만** 실린다. #297 화면은 복구 전용 버튼 대신 `선택한 회원 활성화`를 쓴다 |
| T-365 | **(하위호환 복구 API 소비자)** 복구 응답에 `failed`가 섞여 옴 | **부분 실패를 안내한다** — 전부 성공한 것처럼 보이지 않는다 |
| T-366 | 비활성화 뒤 바뀐 회원들의 `deactivated_at` | **전원이 같은 값**이다 — 행마다 따로 찍지 않는다 (MUST) |
| T-367 | `INACTIVE`를 복구하거나 정지시킴 | `deactivated_at`이 **`NULL`이 된다** (MUST) |
| T-368 | **(하위호환 전원 API 운영 복구)** 응답을 받지 못한 채 직전 배치를 복구 | `deactivated_at`이 가장 최근인 회원만 골라 복구할 수 있다 |
| T-369 | `admin_actions`에 **열 값을 각각** 기록 | 전부 저장된다 — `REJECT`·`REMOVE`·`GRANT_ADMIN`·`REVOKE_ADMIN`도 그대로 쌓인다 (MUST) |
| T-416 | **(웹)** `INACTIVE`로 로그인 | 라우트 가드가 **보호 화면을 통과시킨다.** 홈은 `ACTIVE`와 같은 `/notices`다 (MUST). **자료 메뉴도 그대로 보인다** |
| T-417 | **(웹)** 자료 화면이 `403 INACTIVE`로 실패 | *"잠시 후 다시 시도"* 가 아니라 **비활동이라 막혔다는 사유**가 뜬다 |
| T-418 | **(하위호환 전원 API를 UI로 제공하는 별도 소비자)** 학기 전환 확인 창 | 건수와 함께 **제외되는 셋(관리자·정지·승인 대기)을 말한다** ([#228](https://github.com/HackerKHU/hacker_HP/issues/228) D4). #297 회원 관리 화면 계약이 아니다 |
| T-419 | **(위 별도 소비자)** 대상 건수를 못 셌을 때의 확인 창 | **그래도 누를 수 있다** — 건수는 참고치이지 실행의 조건이 아니다. 제거 확인 창과 다른 점이다 |
| T-420 | **(위 별도 소비자)** 대상 건수를 **아직 세는 중**인 확인 창 | **누를 수 없다** ([2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md#학기-전환--일괄-비활성화와-복구) MUST — 누르기 전에 건수를 보여준다). 건수가 도착하면 풀린다 |
| T-421 | 비활성화 요청에 본문 없음 | 기존처럼 활성 일반 부원 전원만 바뀌고 응답은 `deactivated` 필드만 가진다 (하위 호환 MUST) |
| T-422 | 비활성화 요청이 JSON 최상위 `null`·`{}`·`{"userIds": null}`·`{"userIds": []}` | 모두 T-421과 같은 전원 경로이며 `failed` 필드가 없다 |
| T-423 | 활성 일반 부원 일부 id만 선택 | 고른 회원만 `INACTIVE`, 고르지 않은 활성 회원은 그대로다 |
| T-424 | 선택 id에 `ACTIVE USER`·`SUSPENDED USER`·`ADMIN`·`PENDING`·`INACTIVE`·없는 id를 섞음 | 앞의 두 회원만 `INACTIVE`, 뒤의 세 계정은 `NOT_ACTIVE_USER`, 없는 id는 `NOT_FOUND`다. 전체는 `200` |
| T-425 | 성공 id와 실패 id를 역순·중복으로 선택 | 첫 등장 순서대로 처리되어 `deactivated`·`failed`에 각 id가 한 번만 나타난다. 행 잠금만 id 오름차순이고, `DEACTIVATE` 이력은 기존 id순으로 대상마다 한 행·같은 `occurredAt`이다 |
| T-426 | 선택 배열 100개 / 101개 | 100개는 처리하고 101개는 `400 VALIDATION_ERROR`. 중복이어도 원본 101개면 거절한다 |
| T-427 | 선택 id에 `null`·`0`·음수 | `400 VALIDATION_ERROR` |
| T-428 | 선택 비활성화 성공 | `deactivated_at`과 `DEACTIVATE` 이력이 실제 변경 대상에만 남고 한 요청의 시각이 같다 |
| T-429 | 선택 회원과 비선택 회원의 기존 세션이 있음 | 선택 회원만 다음 자료 요청이 `403 INACTIVE`, 비선택 회원은 계속 `200` |
| T-430 | 선택 경로의 세션 반영 실패 뒤 같은 선택 요청 재시도 | 변경·`deactivated_at`은 커밋되고, 이미 `INACTIVE`인 선택 회원은 다시 반영하되 비선택 회원은 반영하지 않는다 |
| T-431 | 같은 회원을 고른 선택 비활성화가 동시에 도착 | 그 id는 한 응답과 한 이력에만 변경으로 나타난다 |
| T-432 | 생성된 OpenAPI의 비활성화 계약 | 요청 본문은 optional, `userIds.maxItems=100`, 응답에 선택 경로 `failed`와 두 reason이 명시된다 |

**T-335가 이 묶음의 절반이다.** `INACTIVE`를 `SUSPENDED` 옆에 끼워 넣는 구현 — 필터 맨 위에서 상태만 보고 막는 것 — 은 **T-336·T-337을 전부 통과시키면서** 공지도 게시판도 함께 막는다. 막히는 쪽만 재면 그 구현이 정답으로 보인다.

**T-338은 T-336·T-337로 대체되지 않는다** (MUST). 둘은 **테스트가 아는 경로**만 두드린다. 필터는 `/api/v1/notes/**`·`/api/v1/bookmarks/**` **접두사로** 막으므로([3-3 결정 17의 2026-08-28 노트](3-3-DESIGN-DECISIONS.md#3-3-18-결정-17--상태가-경로마다-다르게-작용하는-것은-필터의-경로-목록으로-가른다)) 자료 갈래에 API가 늘어도 자동으로 막히는데, **그 접두사가 계약이 세는 열한 경로를 실제로 덮는가**는 별개 질문이다. 접두사를 기대값으로 쓰면 아무것도 재지 않는 것과 같으므로 **§3-2-4 표를 손으로 옮긴 목록**을 테스트가 들고 순회한다. 자료 경로가 그 접두사 밖으로 나가는 순간 이 사례가 깨진다.

**T-343·T-344가 한 벌이다.** T-343만 두면 "실패하면 500"까지만 지켜지고, **재요청이 실제로 복구가 되는지**는 확인되지 않는다. 대상을 `status = 'ACTIVE'`로만 잡은 구현은 T-339~T-343을 모두 통과하면서 **T-344에서만 깨진다** — 이미 `INACTIVE`가 된 사람이 재요청의 대상에서 빠져 세션이 낡은 채 남기 때문이다 ([2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md#학기-전환--일괄-비활성화와-복구) MUST).

**T-348은 이상해 보이는 것이 맞다.** 정지 전에 `INACTIVE`였던 사람이 해제 뒤 `ACTIVE`가 되는 것은 **의도된 손실**이다 — 이전 상태를 기억하는 열을 두지 않기로 했고, 잘못 올라온 사람은 다음 학기 전환에서 다시 내려간다. 사례로 못 박아 두지 않으면 나중에 "버그"로 고쳐진다.

**T-339는 본문 없는 하위 호환 전원 경로를 본다.** 이 경로는 학기 전환이
제재를 풀지 않도록 `SUSPENDED`를 제외한다. 반면 선택 경로는 관리자가 명시적으로
고른 `SUSPENDED USER`를 `INACTIVE`로 바꾸며, T-443이 두 경로의 차이를 지킨다.

**T-360이 T-343의 나머지 절반이다.** T-343은 *"실패하면 `500`이고 되돌아가지 않는다"* 까지만 재므로, **되돌릴 근거가 남아 있는지**는 확인되지 않는다. 되돌릴 근거를 응답에만 둔 구현은 T-339 ~ T-344를 전부 통과하면서, 그 순간 **원래 비활동이던 사람과 방금 내려간 사람을 가를 방법을 없앤다** ([2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md#학기-전환--일괄-비활성화와-복구) MUST). 근거를 `deactivated_at`으로 옮긴 이유가 그것이다 — **이력은 이 자리를 대신하지 못한다.** 이력은 세션 반영보다 뒤에 남기고 실패를 삼키므로(T-211·T-212) 정확히 이 실패 경로에서 비어 있다.

**T-362 ~ T-365가 없으면 이 기능은 반쪽이다.** 서버 사례가 전부 통과해도 상태 칸이
`INACTIVE`를 "정지"로 보여주거나 필터에 그 항목이 없으면 대상을 찾을 수 없다. #297의 현재
`MemberListPage`는 `선택한 회원 활성화`로 복구하고, T-364~T-365의 전용 복구 경로는 하위호환
API adapter·소비자 계약으로만 남는다.

**T-369는 마이그레이션 회귀다.** `admin_actions`의 `CHECK`는 새로 더할 때마다 **교체**되므로, 새 값 둘만 적은 제약으로 갈아끼우면 기존 네 조작의 이력 INSERT가 전부 거절된다. 그런데 **이력은 실패를 삼키므로 화면에는 아무 일도 없어 보인다** — 감사 기록만 조용히 비는 종류라 사례로 잡지 않으면 알아채지 못한다.

**T-359는 T-341로 대체되지 않는다.** T-341은 호출을 순차로 두 번 할 뿐이라, **대상을 먼저 조회한 뒤 갱신하는 구현도 통과한다.** 그 구현은 동시 호출에서만 깨진다 — 두 요청이 같은 `ACTIVE` 집합을 읽어 양쪽 응답에 같은 id를 담고 이력을 두 벌 쌓으므로, 한쪽 응답으로 되돌리면 **남이 방금 내린 사람까지 올라온다.**

**T-358은 승격 경로가 상태까지 바꾸기 때문에 필요하다.** `SUSPENDED`만 거절하는 구현에 `INACTIVE`가 그대로 지나가면 `ADMIN`/`INACTIVE`가 생기는데, **그 조합은 활성 관리자를 0명으로 둔다** — 이 문이 열리는 조건이 바로 그것이라 같은 요청을 몇 번 보내도 복구되지 않고 시도 상한만 깎인다. 거절하는 구현도 안 된다: 그러면 복구 경로가 통째로 막힌다.

**T-416이 웹에서의 T-335다** (2026-08-29, [#231](https://github.com/HackerKHU/hacker_HP/issues/231)). 서버와 마찬가지로 화면에서도 **막히지 않아야 할 것이 열려 있는가**가 잡기 어렵다 — 새 상태를 `SUSPENDED` 옆에 끼워 넣은 세션 유니온은 T-354를 통과하면서 **모든 보호 화면에서 튕긴다.** 그 사람은 공지도 마이페이지도 못 보게 되고, 자료만 막는다는 이 상태의 뜻이 화면에서 사라진다.

**T-418은 T-356의 나머지 절반이다** (2026-08-29, [#232](https://github.com/HackerKHU/hacker_HP/issues/232)). 둘은 본문 없는 전원 경로를 별도 UI가 제공할 때의 계약이다. T-356은 *"몇 명인가"* 까지만 재므로 **누가 대상이 아닌지**는 확인되지 않는다. 대상에서 빼는 셋은 이미 MUST인데([2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md#학기-전환--일괄-비활성화와-복구)), 그 별도 화면이 이를 말하지 않으면 관리자는 **"전체"라는 말을 글자 그대로 믿는다.** #297 회원 관리 화면에는 전원 경로가 없고 현재 페이지에서 고른 사람과 이름을 보여준다.

**T-419·T-420은 한 벌이고, 화면은 세 경우를 갈라야 한다** — 세는 중 / 못 셈 / 셌음. 둘 중 하나만 재면 반대쪽이 반드시 어긋난다.

- **못 셌을 때까지 잠그면**(제거 확인 창의 규칙을 그대로 베낀 구현, [§2-2-4](2-2-OPERATOR-REQUIREMENTS.md#제거-전에-확인한다) MUST) **건수 조회가 실패할 때 학기 전환이 통째로 멈춘다.** 두 창의 건수는 성격이 다르다 — 제거는 *"무엇이 남는가"* 라 조작의 근거이고, 여기는 *"참고치이지 실행의 조건이 아니다"* ([§2-2-3](2-2-OPERATOR-REQUIREMENTS.md#학기-전환--일괄-비활성화와-복구)).
- **세는 중까지 열어 두면** 건수가 도착하기 전에 수십 명이 바뀐다 — *"누르기 전에 대상 건수를 보여준다"* (§2-2-3 MUST)를 정면으로 어긴다. **아직 아무것도 보여주지 않은 상태**라 "못 셌다"와 같지 않다: 그쪽은 셀 수 없다는 것까지 보여준 뒤 관리자가 알고 누르는 것이다.

**T-417이 T-355의 나머지 절반이다.** T-355는 *"로그인 화면으로 가지 않는다"* 까지만 재므로, **왜 막혔는지가 화면에 있는지**는 확인되지 않는다. 사유가 없으면 비활동 부원은 자료를 누를 때마다 *"잠시 후 다시 시도해 주세요"* 를 받는데, 그 말은 **이번 학기 내내 거짓말이다** — 다시 시도해도 답은 같다.

**상태를 상시로 알리는 표시는 두지 않는다** (2026-08-29 결정, #231). 한때 헤더에 *"이번 학기 비활동"* 배지를 두고 *"은근하게 드러난다"* 를 사례로 두었다가 걷어냈다 — **아무 일도 하지 않는 사람에게 상태를 계속 알리는 값이 작기 때문이다.** 비활동 부원이 하는 일의 대부분(공지·게시판·갤러리·마이페이지)은 `ACTIVE`와 똑같이 되고, 그 화면들에서 이 배지는 아무것도 바꾸지 않는 채 자리만 차지한다. **알아야 하는 순간은 막히는 순간 하나뿐이고**, 그 자리는 T-417이 이미 맡는다 — 자료를 눌렀을 때 왜 막혔는지가 뜨면 사람은 거기서 자기 상태를 안다. 상시 표시를 다시 두려면 *"막히기 전에 미리 알아야 할 이유"* 를 먼저 찾아야 한다.

**표에서 (웹)으로 표시한 것은 현재 프론트엔드 사례다.** 하위호환 API adapter·소비자로
표시한 T-356~T-357, T-364~T-365, T-368, T-418~T-420은 서버 계약을 현재 #297 화면에
버튼으로 되살리라는 뜻이 아니다. 현재 화면은 T-431~의 선택 기반 다섯 버튼 계약을 따른다.
위 서버 사례가 전부 통과해도 웹이 `403 INACTIVE`를 무시하면 비활동 부원은 계속 `active`로
보여 자료 화면에서 실패만 반복한다.

### 마이페이지와 회원 탈퇴 (#223)

[2-1 §2-1-9](2-1-USER-STORIES.md#2-1-9-마이페이지)와 [2-2 §2-2-4](2-2-OPERATOR-REQUIREMENTS.md#본인-탈퇴--같은-처리-다른-문)가 요구하는 결과다. **처리는 회원 제거와 같으므로 그쪽 사례([T-187 ~ T-197](#회원-제거))를 다시 쓰지 않는다** — 여기서 재는 것은 **문이 다르다는 데서 오는 차이**뿐이다. T-370·T-371만 예외로 겹치는데, *"같은 처리"* 가 실제로 같은지를 이 경로에서 한 번은 확인해야 하기 때문이다.

| # | 조건 | 기대 |
|---|---|---|
| T-370 | `ACTIVE`가 `DELETE /auth/me` | `204`. 계정은 사라지고 **자료·공지·활동사진·게시글은 남아** 작성자가 "탈퇴한 회원"이 된다 (MUST) |
| T-371 | 위 회원의 즐겨찾기 | **함께 지워진다** |
| T-372 | 탈퇴 응답 | **세션과 신원 토큰 쿠키를 함께 버린다** — 같은 브라우저의 다음 요청이 `401` (MUST) |
| T-373 | 탈퇴한 회원의 **다른 기기 세션** | 다음 요청이 `401` — 세션이 전부 폐기된다 (MUST) |
| T-374 | **마지막 활성 관리자**가 탈퇴 | `403 FORBIDDEN`, **계정이 그대로 남는다** (MUST) |
| T-375 | 활성 관리자가 둘일 때 한 명이 탈퇴 | `204` |
| T-376 | **`PENDING`(신청 전, `applied_at IS NULL`)이 탈퇴** | `204` — **본인이 직접 지울 수 있는 유일한 경로다** (MUST). 관리자 제거는 그대로 된다 |
| T-377 | `PENDING`(신청 후)이 탈퇴 | `204` |
| T-378 | `INACTIVE`가 탈퇴 | `204` — 자료 차단과 무관하다. **`INACTIVE` 상태가 생기는 [#229](https://github.com/HackerKHU/hacker_HP/issues/229)부터 적용한다** |
| T-379 | `SUSPENDED` 세션으로 탈퇴 시도 | `403 SUSPENDED` — **탈퇴를 위한 예외를 뚫지 않는다** (MUST) |
| T-380 | 비로그인이 탈퇴 시도 | `401 UNAUTHENTICATED` |
| T-381 | 탈퇴 뒤 **같은 구글 계정으로 다시 로그인** | 새 계정이 `PENDING`으로 만들어진다 — `google_sub`·`email` UNIQUE에 걸리지 않는다 (MUST) |
| T-382 | **`PENDING`이** 탈퇴한 뒤 `admin_actions` | **`WITHDRAW` 한 행**, `actor_id = target_id`. 정지를 밟지 않으므로 `SUSPEND`가 없다 (MUST) |
| T-383 | 관리자가 자기 자신을 `DELETE /admin/users/{id}`로 제거 | 이력이 **`REMOVE`다** — `WITHDRAW`가 아니다 (MUST) |
| T-384 | `GET /auth/me/content-summary` | 네 값을 **항상** 담는다 — `0`도 빠지지 않는다 (MUST) |
| T-385 | 남이 올린 자료가 섞인 상태에서 위를 호출 | **본인 것만** 센다 |
| T-386 | 일반 부원이 `GET /admin/users/{id}/content-summary` | `403` — 관리자 경로를 열어 남의 건수를 세게 하지 않는다 (MUST) |
| T-387 | **(웹)** 마이페이지 | 이름·이메일·학번·학과·상태·가입 신청일·승인일이 보이고 **고칠 수 있는 입력란이 없다** (MUST) |
| T-388 | **(웹)** 탈퇴 확인 창 | 남을 콘텐츠 **네 갈래의 건수**를 보여준다 (MUST) |
| T-389 | **(웹)** 확인 창에서 취소 | `DELETE /auth/me`를 **부르지 않는다** |
| T-390 | **(웹)** 신청·대기 화면 | **탈퇴 진입점이 있다** (MUST) — `PENDING`이 탈퇴할 수 있는 유일한 자리다 |
| T-391 | **(웹)** 탈퇴 성공 직후 | 세션이 비로그인으로 정리되고 **랜딩(`/`)으로 간다** — 보호 화면에 남기면 가드가 로그인으로 튕겨, 방금 계정을 지운 사람이 로그인 화면을 마주한다 ([#226](https://github.com/HackerKHU/hacker_HP/issues/226)) |
| T-392 | **`PENDING`이** `GET /auth/me/content-summary` | `200` + 네 값이 전부 `0` (MUST). 상태 필터가 **이 경로도** 통과시켜야 한다 |
| T-393 | **`ACTIVE`가** 탈퇴한 뒤 `admin_actions` | **`SUSPEND` → `WITHDRAW`** 순서 (MUST) — 회원 제거의 T-262와 같은 모양이다 |
| T-394 | 정지가 확정된 뒤 **관리자가 대상을 `ACTIVE`로 되돌린** 상태에서 삭제 단계에 도달 | **`409 CONCURRENT_CHANGE`**, 계정이 남는다 (MUST) |
| T-395 | **(웹)** 건수 조회가 실패한 채로 탈퇴 확인 창 | **실패를 알리고 `DELETE /auth/me`를 부르지 않는다** (MUST) |
| T-396 | 일반 `ACTIVE` 부원(관리자 아님)이 탈퇴 | `204` — **선행 정지가 관리자 자격을 요구하지 않는다** (MUST) |
| T-397 | **활성 관리자가 정확히 둘**인 상태에서 **둘이 동시에** 탈퇴 | 최소 하나가 `403`이고 **활성 관리자가 한 명 남는다** (MUST) |
| T-398 | 선행 정지가 커밋된 뒤 **세션 반영 확인이 실패** | 계정이 `SUSPENDED`로 남고, **"탈퇴 중 정지만 남았다" 로그에 대상 id가 있다** (MUST). 그 계정의 다음 `DELETE /auth/me`는 `403 SUSPENDED`다 |
| T-399 | 위 계정을 관리자가 제거 / 해제 | 둘 다 된다 — 이어받을 경로가 이미 있다 ([runbook](../docs/ops/runbook.md)) |
| T-400 | **(웹)** `PENDING`이 신청·대기 화면에서 탈퇴를 끝까지 실행 | 건수 조회 → 네 건수 표시 → 확인 → `DELETE /auth/me` → 비로그인 전환까지 **이어진다** (MUST) |
| T-401 | 선행 정지가 커밋된 뒤 **삭제가 실패** | T-398과 **같은 로그**를 남긴다 (MUST) — 두 갈래가 같은 결과이므로 문맥도 같아야 한다 |
| T-402 | **(웹)** `DELETE /auth/me`가 `403`·`409`로 실패 | 실패를 알리고 **세션과 화면을 그대로 둔다** (MUST) — 로그인 화면으로 밀지 않는다 |

**T-376과 T-390이 한 벌이다.** `PENDING`을 빼는 구현은 T-370 ~ T-375를 전부 통과하면서 **아무도 지울 수 없는 계정**을 남긴다 — 신청서를 내지 않은 계정은 관리자의 승인 목록(`status=PENDING&applied=true`)에도 뜨지 않기 때문이다. 서버만 열고 화면에 진입점을 두지 않아도 결과는 같다: `PENDING`은 마이페이지를 볼 수 없으므로 **누를 곳이 없다.**

**T-379는 정확히 반대 방향을 막는다.** *"탈퇴는 누구나 할 수 있어야 한다"* 로 읽고 상태 필터에 예외를 뚫으면, T-381이 보장하는 재가입과 맞물려 **정지된 사람이 탈퇴 후 재가입으로 정지를 지운다.** 두 사례를 함께 두어야 그 조합이 드러난다.

**T-383이 없으면 `WITHDRAW`를 나눈 의미가 사라진다.** 같은 사람이 같은 계정을 지우는 두 경로라 구현이 하나로 합쳐지기 쉽고, 합쳐지면 **계정이 사라진 뒤에는 어느 문으로 나갔는지 알 방법이 없다** — 남는 것이 숫자 id뿐이기 때문이다 ([2-2 §2-2-7](2-2-OPERATOR-REQUIREMENTS.md#2-2-7-안전장치)).

**T-392는 T-376이 잡지 못하는 자리다.** 인증 영역의 새 경로는 상태 필터에서 기본이 `403 PENDING_APPROVAL`이라, 통과 목록에 `DELETE /auth/me`만 넣어도 **T-376은 통과한다.** 그런데 확인 창은 건수를 먼저 읽으므로 **실제 흐름은 탈퇴에 닿기 전에 막힌다** — 두 경로가 한 벌이라는 것을 사례로 못 박아 둔다.

**T-382와 T-393이 갈리는 이유가 정지다.** `ACTIVE` 탈퇴는 회원 제거와 같이 `SUSPENDED`를 먼저 확정하므로 이력이 **두 행**이고(T-262와 같은 모양), `PENDING`은 그 전이가 없어 정지를 건너뛰므로 **한 행**이다. 한쪽만 재면 구현이 다른 쪽에서 어긋난다 — `WITHDRAW` 한 행만 기대하는 사례를 두면 **`SUSPEND` 기록을 지우는 쪽으로 구현이 맞춰져** [§2-2-7](2-2-OPERATOR-REQUIREMENTS.md#2-2-7-안전장치)의 감사 규칙이 깨진다.

**T-394는 문이 달라져도 남는 검사다.** 이 `409`의 근거는 *"다른 관리자가 의도를 갖고 되살렸다"* 가 아니라 **차단이 먼저 확정되어야 한다**는 것이다 — `ACTIVE`인 채로 지운 뒤 세션 폐기가 실패하면 **계정 없는 `ACTIVE` 세션이 만료까지 산다.** 본인 탈퇴라고 느슨해질 수 없다.

**T-395가 T-388·T-389의 빈자리다.** 둘은 각각 조회가 성공한 경우와 사용자가 취소한 경우만 본다. **건수를 못 읽었는데 확인 버튼이 열려 있는 구현**은 둘 다 통과하면서, 사용자가 *"남을 것을 확인했다"* 는 잘못된 전제로 되돌릴 수 없는 조작을 하게 만든다.

**T-396이 없으면 이 기능은 관리자만 쓸 수 있다.** 회원 제거의 선행 정지는 *"요청자가 활성 관리자인가"* 를 먼저 보므로, 그 경로를 그대로 재사용하면 **일반 부원의 탈퇴가 첫 단계에서 `403`이 된다.** *"처리는 회원 제거와 같다"* 를 곧이곧대로 읽으면 정확히 그렇게 구현된다.

**T-397은 T-374·T-375가 못 보는 자리다.** 둘은 관리자가 한 명일 때와 두 명일 때를 **순차로** 잴 뿐이라, 활성 관리자가 정확히 둘일 때 **둘이 동시에 나가면 서로를 세어 보고 둘 다 통과**할 수 있다. 그러면 승인할 사람이 아무도 남지 않고, 복구는 부트스트랩 토큰뿐이다.

**T-398과 T-401은 실패 지점이 다르다.** 정지가 커밋된 뒤 깨질 수 있는 곳이 **세션 반영 확인**과 **삭제** 둘인데, 결과는 같다 — 계정이 `SUSPENDED`로 잠긴다. 한쪽만 재면 **구현이 삭제 예외에서만 탈퇴 문맥을 남기고 세션 반영 실패에서는 일반 오류만 찍어도** 통과하고, 그러면 그 계정은 **일반 정지와 구별되지 않아 아무도 이어받지 못한다** ([runbook](../docs/ops/runbook.md)의 조회가 로그 문구에 걸려 있다).

**T-402가 나머지 웹 실패 경로다.** 지금 사례들은 취소·건수 조회 실패·탈퇴 성공만 본다. `DELETE`를 `try/finally`로 감싸 **응답이 `403`이든 `409`든 세션을 비우고 `/login`으로 보내는 구현**이 전부 통과하는데, 마지막 활성 관리자나 삭제 직전 재활성화에서는 **계정이 그대로 남아 있는데 사용자는 탈퇴가 끝난 줄 안다.**

**T-398·T-399·T-401이 이 경로의 사고 시나리오다.** 정지가 커밋된 뒤 실패하면 계정이 `SUSPENDED`로 남는데 **본인은 로그인과 API 접근을 함께 잃어 재시도할 수 없다** — 관리자 제거의 *"재요청이 복구 수단"* 이 여기서는 성립하지 않는다. 그 상태를 없앨 수는 없으므로(정지 선행이 세션 폐기 실패를 막는 유일한 수단이다) **관리자가 이어받는 것**을 사례로 못 박는다. `SUSPENDED`에게 탈퇴를 열어 이어가게 하는 구현은 T-379를 깨야 하고, 그것이 곧 정지 회피다.

**T-391의 목적지는 랜딩이지 로그인이 아니다** (2026-08-29, [#226](https://github.com/HackerKHU/hacker_HP/issues/226) 구현 중 확인). 계정이 사라진 사람에게 로그인 화면을 보여주면 **방금 지운 계정으로 다시 들어가라는 말처럼 읽힌다.** 화면 구현에도 함정이 하나 있다: 세션을 먼저 비우면 아직 보호 화면에 있는 가드가 비로그인을 보고 `/login`으로 보내는데, 그 이동이 랜딩으로 가는 이동보다 늦게 도착해 **이긴다.** 세션 비우기와 이동을 한 커밋으로 묶어야 한다.

**T-400이 T-390의 나머지다.** T-390은 진입점이 **보이는지**만 보므로, 눌러도 아무 일이 없거나 확인 창만 열고 요청을 보내지 않는 구현도 통과한다. 마이페이지 쪽(T-388·T-389·T-391)만 이어 두면 **`PENDING`의 유일한 UI 경로가 연결되지 않은 채 남는다.**

**T-387은 "아직 안 만들었다"로 통과시킬 수 없다.** 보기 전용은 **기능을 덜 만든 상태가 아니라 결정이다** ([2-1 §2-1-9](2-1-USER-STORIES.md#2-1-9-마이페이지)). 나중에 입력란이 하나 생기면 이 사례가 깨져야 한다.
### 세션·CSRF

신원은 JWT, 인가 상태는 서버 세션이 담당한다 ([3-1 §3-1-5](3-1-DESIGN-ARCHITECTURE.md), [3-3 결정 12](3-3-DESIGN-DECISIONS.md#3-3-13-결정-12--인증은-jwt-인가-상태는-서버-세션으로-나눈다)). 아래는 **Spring Security 필터 체인을 통과하는 통합 테스트로 검증한다** (MUST). 필터를 태우지 않으면 이 결합과 CSRF 검사가 실제로 도는지 알 수 없다.

**자격 증명 결합**

| # | 조건 | 기대 |
|---|---|---|
| T-29 | 사용자 A의 JWT 쿠키와 사용자 B의 세션 쿠키를 함께 전송 | `401 UNAUTHENTICATED`. **A의 신원으로 B의 `role`이 쓰이지 않는다.** 응답이 JWT·세션 쿠키를 모두 폐기한다 |
| T-30 | 유효한 JWT만 있고 세션이 없다 (로그아웃·만료 이후) | `401 UNAUTHENTICATED`. 서명이 유효해도 통과하지 못한다. **정지는 여기 해당하지 않는다** — 세션을 남기므로 T-32의 `403 SUSPENDED`다 |
| T-31 | 세션만 있고 JWT 쿠키가 없다 | `401 UNAUTHENTICATED` |

**토큰을 요청마다 다시 발급하는가** ([#299](https://github.com/HackerKHU/hacker_HP/issues/299)) — 구현은 `AccessTokenRenewalIntegrationTest`다.

| # | 조건 | 기대 |
|---|---|---|
| T-421 | 인증이 성립한 아무 요청 | 응답에 **새 토큰 쿠키가 실린다.** 버리는 쿠키가 아니다 |
| T-422 | 그 새 토큰만 들고 다음 요청 | 통과하고 **같은 사람**이다 — 발급이 `sub`를 바꾸지 않는다 |
| T-423 | 수명을 지나도록 **계속 쓰면서** 매번 직전 응답의 토큰을 실어 보낸다 | 요청마다 **만료가 앞 것보다 뒤로 밀린다.** 그동안 처음 토큰의 만료 시각은 지나 있다 |
| T-424 | 재발급된 토큰의 수명 | **설정값 그대로다.** 조금씩 깎이면 오래 쓸수록 창이 좁아져 같은 결함으로 돌아온다 |
| T-425 | T-29·T-30의 거부 조합 | **갱신이 아니라 폐기다** (MUST). 발급을 인증 성공보다 앞에 두면 거부해야 할 자격을 갱신해 준다 |
| T-426 | 로그아웃 | 응답에 `ACCESS_TOKEN` 헤더가 **둘**(갱신·폐기) 실리고 **마지막이 폐기다** — 브라우저가 적용하는 값이다 |

**T-423이 결함 자체다.** 없으면 세션만 유휴 시계가 밀리고 토큰은 로그인 시각 + 수명에 죽어, **쓰는 도중에 로그아웃**된다.

**"만료된 토큰으로 가면 `401`"로 재지 않는다.** `NimbusJwtDecoder`가 기본으로 **60초의 시계 오차**를 허용해 방금 만료된 토큰도 1분은 통과한다 — 짧게 기다리면 **고치지 않아도 통과하고**, 1분을 기다리면 사례 하나가 1분을 먹는다. 그래서 만료 시각이 밀리는지를 직접 잰다.

**T-426은 순서에 기대는 동작을 고정한다.** 토큰을 버리는 세 경로(로그아웃·탈퇴·자기 제거)는 필터가 쓴 갱신 뒤에 폐기를 덧붙인다. 검증은 **마지막 헤더**로 해야 한다 — `getCookie`는 첫 번째만 주므로 그것으로 재면 정반대로 읽는다 (`ResponseCookies`).

**상태 변경이 기존 세션에 반영되는가** — 새로 로그인하지 않고 **같은 세션으로** 다음 요청을 보내 확인한다.

| # | 조건 | 기대 |
|---|---|---|
| T-32 | 로그인 중인 `ACTIVE` 회원을 관리자가 `SUSPENDED`로 변경 | 그 회원의 다음 요청이 **`403 SUSPENDED`로** 차단된다 — 세션을 지우지 않고 갱신하므로 T-30의 `401`이 아니다 ([2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md), [3-3 결정 12의 2026-08-11 노트](3-3-DESIGN-DECISIONS.md#3-3-13-결정-12--인증은-jwt-인가-상태는-서버-세션으로-나눈다)). 코드가 다르면 T-99의 경로(쿼리 없이 도착한 정지 세션)에서 안내를 띄우지 못한다 |
| T-33 | 로그인 중인 `PENDING` 회원을 관리자가 일괄 승인 | 그 회원의 다음 요청이 더 이상 `403 PENDING_APPROVAL`을 받지 않는다 |
| T-34 | 로그인 중인 `ADMIN`의 role을 `USER`로 회수 | 그 사용자의 다음 관리자 API 요청이 `403 FORBIDDEN` |

**로그인 세션이 만들어지는 중에 상태가 바뀌면** (#127) — 진짜 `SessionRepositoryFilter`를 통과시켜 확인한다. 이 창의 정체가 **"세션이 언제 저장소에 들어가는가"** 라서, 필터를 흉내 내면 확인하려는 것이 사라진다.

| # | 조건 | 기대 |
|---|---|---|
| T-198 | 로그인이 끝난 직후 | 발급된 `SESSION` 쿠키가 가리키는 세션이 **이미 저장소에 있다** (MUST). `PRINCIPAL_NAME` 색인도 채워져 있다 |
| T-199 | 로그인 전후의 세션 id | **다르다** (MUST) — 세션 고정 보호 |
| T-200 | 로그인 뒤 인가 요청이 쓰던 세션 | 저장소에 **남지 않는다.** 그 사람의 세션은 하나뿐이다 |
| T-201 | 로그인 **전에** 커밋된 승인 | 발급된 세션이 처음부터 `ACTIVE`다 |
| T-202 | 정지가 **로그인이 계정 행을 잠근 사이에** 도착 | 정지가 잠금을 잡은 시점에 **로그인 세션이 이미 저장돼 있다** (MUST). 끝난 뒤 세션과 DB가 같다 |
| T-203 | 로그인 **뒤에** 커밋된 승인 | 동기화 조회가 그 세션을 찾아 갱신한다 |
| T-204 | 계정을 읽은 뒤 세션이 만들어지기 전에 그 계정이 삭제됨 | 세션이 **하나도 만들어지지 않고** 로그인이 실패한다 (MUST) |

**T-202가 본체이고, 순서를 우연에 맡기면 안 된다** (MUST). 그냥 동시에 던지면 로그인이 인가 단계부터 밟느라 정지가 거의 항상 먼저 끝나고, 그러면 **읽은 뒤 저장 전에 잠금을 놓는 잘못된 구현도 통과한다.** 시험이 먼저 행을 잡아 로그인·정지를 차례로 대기시킨 뒤 놓아준다.

**끝난 뒤 상태만 보는 것으로도 부족하다.** 저장을 잠금 밖으로 뺀 구현도 대개 제때 저장하므로 최종 상태가 맞아 버린다. 그래서 **정지가 잠금을 잡은 그 순간 세션이 이미 저장돼 있는지**를 본다 — 그것이 "저장까지가 잠금 안"의 정의다.

**"저장한 뒤에 대조한다"로도 닫히지 않는다.** 세션은 응답이 커밋될 때 저장되는데, 응답이 나가면 **대조가 끝나기 전에 그 쿠키로 요청이 도착할 수 있다.** 그래서 응답을 내보내는 호출까지 잠금 안에 있어야 한다.

**T-198은 쿠키와 저장소를 함께 본다.** 한쪽만 맞기 쉽다 — 저장은 됐는데 쿠키가 다른 세션을 가리키면 로그인이 안 되고, 쿠키는 맞는데 저장이 늦으면 창이 그대로 열려 있다. 세션을 저장소에 직접 만들면 필터가 자기 판단으로 쿠키를 덮어쓰거나 만료시킨다.

**T-199·T-200은 실제로 저장된 인가 세션을 써야 한다.** 콜백 안에서 즉석으로 만든 세션은 저장소에 없어서, **지우지 않아도 "지웠다"가 통과한다.**

**T-204는 T-193의 로그인 쪽 절반이다.** 이쪽은 아직 응답을 내보내기 전이라 로그인 실패로 되돌릴 수 있다. 이미 만들어진 세션 쪽은 갱신 경로가 지운다 — 필터는 매 요청 `users`를 읽지 않으므로([3-3 결정 12](3-3-DESIGN-DECISIONS.md)) 남겨 두면 계정 없는 사람이 만료까지 인증된다.
| T-162 | 한 사람이 두 브라우저에 로그인한 상태에서 정지 | **두 세션 모두** 다음 요청이 막힌다 |
| T-163 | 상태 변경 트랜잭션이 롤백됨 | 세션도 옛 값 그대로다. 되돌아간 변경이 세션에만 남지 않는다 |
| T-164 | 로그인해 만들어진 세션 | 사용자 id로 찾을 수 있다 (`PRINCIPAL_NAME` 색인) |
| T-165 | `PATCH /admin/users/{id}/status`로 정지·해제 | `200` + **갱신된 회원 본문.** 화면이 재조회 없이 그 행을 고친다 |
| T-166 | 이미 그 상태인데 다시 요청 | `200` + 현재 상태. 아무것도 바뀌지 않는다 |
| T-167 | `PENDING` 계정의 상태를 이 경로로 변경 | `400 VALIDATION_ERROR`. 승인은 `POST /admin/users/approve`가 한다 |
| T-168 | 활성 관리자가 **한 명 더 있을 때** 자기 자신을 정지 | `403 FORBIDDEN`. 관리자 수와 무관하게 권한 회수를 먼저 요구한다 (#296) |
| T-169 | 기존 `SUSPENDED ADMIN`에게 다시 `SUSPENDED` 요청 | 같은 정책 `403`. 멱등 `200`의 명시적 예외이며 기존 행은 그대로다 |
| T-170 | 활성 관리자 둘이 **동시에 서로를** 정지 | 둘 다 정책 `403`, 두 계정과 감사 이력이 그대로다 |
| T-171 | 세션 갱신이 실패한 뒤 같은 상태 변경을 재요청 | 세션이 다시 맞춰진다. 재시도가 복구 수단이다 |
| T-172 | 요청이 인가를 지난 **뒤에** 요청자가 정지되거나 권한을 잃음 | 그 요청은 커밋되지 않는다 — **상태 변경도 일괄 승인도** 마찬가지다. 코드는 필터가 막았을 때와 같다 |
| T-173 | 세션에 더 새 값이 반영된 뒤 **옛 변경의 갱신이 도착** | 덮어쓰지 않는다. 정지가 해제로 되돌아가지 않는다 |
| T-174 | 옛 갱신과 새 갱신이 **동시에** 도착 | 새 값이 남는다. 비교와 저장이 한 연산이어야 한다 |

DB만 바꾸고 세션을 그대로 두는 구현을 잡아내는 것이 이 사례들의 목적이다.

**실제 세션 저장소로 검증한다** (MUST). `MockHttpSession`으로는 확인할 수 없다 — 저장소에 없는 세션은 애초에 찾을 수 없고, 색인이 채워지는지도 드러나지 않는다.

T-164가 나머지의 전제다. 색인이 비면 **찾지 못한 세션은 갱신되지 않고 어디에서도 오류가 나지 않는다** — T-32~T-34가 조용히 무의미해진다.

T-165~T-169는 정지·해제 API다. **T-32가 여기서 실제 API로 닫힌다** — 일반 회원 정지는 기존처럼 세션까지 갱신되고, 관리자 직접 정지는 그 앞에서 변화 없이 거절된다.

T-170은 #296 정책의 동시성 경계를, T-15·T-272·T-397은 마지막 활성 관리자 원자성을 본다. 직접 정지는 모두 일찍 거절되므로 활성 관리자 0명 경쟁은 이제 권한 회수·삭제·탈퇴 경로에서 재현해야 한다.

T-174가 T-173의 짝이다. 버전을 견주는 것만으로는 부족하다 — 둘이 **같은 옛 세션을 함께 읽으면 둘 다 비교를 통과한다.** 읽기부터 저장까지가 한 연산이어야 한다.

T-173은 **커밋 순서와 세션 저장 순서가 다르다**는 데서 온다. 행 잠금은 DB 변경만 직렬화하고, 잠금이 풀린 뒤의 세션 저장에는 순서 보장이 없다 — 해제와 정지가 잇따르면 늦게 도착한 해제가 정지를 지운다. 세션이 들고 있는 계정 버전보다 낮은 갱신은 버린다.

T-172는 **인가와 저장 사이**를 본다. 인가는 세션 값으로 이루어지므로 요청이 필터를 지난 뒤에도 요청자의 권한이 바뀔 수 있고, 잠금을 기다리는 동안이면 그 사이가 더 길다. 되돌릴 수 없는 조작 전에 잠근 요청자 행으로 다시 확인하지 않으면 **이미 정지된 관리자의 요청이 그대로 커밋된다.**

T-171은 **삼킨 실패의 복구 경로**를 지킨다. 세션 갱신 실패를 예외로 올리지 않는 대신(그러면 이미 커밋된 변경까지 실패한 것처럼 보인다) 관리자가 다시 누르는 것이 유일한 수단인데, "이미 그 상태"라고 일찍 돌아가면 그 재시도가 아무 일도 하지 않는다.

T-169는 과거 데이터에 대한 자동 보정을 막는다. 이 요청이 `role`을 회수하거나 세션·감사를 건드리면 운영 판단 없이 기존 데이터를 바꾼 것이다. `SUSPENDED ADMIN`의 처리는 [runbook](../docs/ops/runbook.md#관리자-직접-정지-정책-배포-전후-점검)이 맡는다.

T-163은 방향이 중요하다. 갱신이 실패하면 세션은 옛 값(권한이 더 좁은 쪽)으로 남고 만료까지 기다리면 되지만, 반대로 두면 **롤백된 정지 때문에 멀쩡한 회원이 이유도 모른 채 막힌다.**

**CSRF**

| # | 조건 | 기대 |
|---|---|---|
| T-35 | 비로그인 상태로 `GET /auth/csrf` | `XSRF-TOKEN` 쿠키 발급. 인증 없이 접근 가능 |
| T-36 | CSRF 토큰 없이 `POST /auth/application` | `403 FORBIDDEN`. 인증 영역 경로도 검사 대상이다 |
| T-37 | 쿠키와 `X-XSRF-TOKEN` 헤더의 값이 서로 다른 상태로 `POST /notices` | `403 FORBIDDEN` |
| T-38 | **`PENDING` 세션**이 T-35의 토큰을 헤더에 실어 `POST /auth/application` | 성공 |
| T-39 | **`ADMIN` 세션**이 T-35의 토큰을 헤더에 실어 `POST /notices` | 성공 |
| T-40 | 구글 로그인 시작 요청에서 `state`를 지우거나 바꿔 콜백 | 로그인 거부 — OAuth `state` 검증을 끄면 로그인 CSRF가 열린다 |
| T-143 | 계약과 다른 이름(`X-CSRF-TOKEN`·`CSRF-TOKEN`)으로 토큰을 실어 보냄 | `403 FORBIDDEN`. **이름은 계약이 정한 둘뿐이다** |

T-36는 `permitAll` 경로를 CSRF 검사에서까지 빼는 구현을, T-37은 관리자 쓰기 요청의 검사를 끈 구현을 잡는다.

**T-38과 T-39는 서로 다른 세션이다** (MUST). `POST /auth/application`은 `PENDING`만, 관리자 쓰기는 `ACTIVE`·`ADMIN`만 성공할 수 있어 한 세션으로 둘 다 통과할 수 없다. 정상 경로가 CSRF 때문에 막히지 않는지를 각 권한에서 따로 확인한다.

T-143이 필요한 이유는 **Spring의 기본 헤더 이름이 `X-CSRF-TOKEN`이기 때문이다.** 저장소 설정을 손대다 기본값으로 돌아가면 화면이 보내는 `X-XSRF-TOKEN`이 무시되어 **모든 쓰기가 403이 된다** — 서버는 정상이고 화면만 죽는, 원인을 찾기 어려운 형태다.

**로그인 시 CSRF 토큰 회전은 `CsrfAuthenticationStrategy`가 맡는다.** `CsrfConfigurer`가 그것을 세션 인증 전략에 등록하므로 구성상 보장되지만, 실제로 도는지는 **구글 로그인을 한 바퀴 돌려야** 볼 수 있어 자동 검증에서 빠져 있다. 회전이 없으면 공격자가 미리 심어둔 토큰 값이 로그인 후에도 유효하다(토큰 고정). §5-3 수동 검증에 넣어 두었다.

### 구글 OAuth·신청

가입과 로그인은 구글 OAuth 하나로 처리한다 ([3-1 §3-1-4](3-1-DESIGN-ARCHITECTURE.md), [3-3 결정 13](3-3-DESIGN-DECISIONS.md#3-3-14-결정-13--가입로그인을-구글-oauth로-한다)). 구글 응답은 스텁으로 대체하되 **서버의 검증 경로는 그대로 태운다** (MUST).

| # | 조건 | 기대 |
|---|---|---|
| T-41 | 구글이 준 ID 토큰의 `email_verified`가 거짓 | `/login?error=unverified`로 리다이렉트, 계정 미생성 |
| T-42 | `hd` 주장만 `khu.ac.kr`이고 `email`은 다른 도메인 | `/login?error=domain`으로 리다이렉트 — **`hd`를 신뢰해 통과시키면 안 된다** |
| T-43 | 콜백 실패 응답 전반 | **JSON 본문이 아니라 리다이렉트다.** 브라우저가 SPA 밖에 남지 않는다 |
| T-44 | 실패 리다이렉트의 쿼리 | 이메일·토큰·예외 메시지가 담기지 않는다 |
| T-45 | 기존 계정의 이메일이 구글에서 바뀐 채로 같은 `sub`로 로그인 | 새 계정을 만들지 않고 기존 계정에 붙는다. **`users.email`이 새 값으로 갱신되고** `GET /auth/me`·회원 목록이 새 주소를 보여준다 |
| T-46 | 첫 로그인 직후 `GET /auth/me` | `status=PENDING`, `studentNo`는 비어 있고 신청 여부가 거짓 |
| T-47 | `PENDING`이 신청서 제출 후 `GET /auth/me` | 신청 여부가 참, 학번이 채워짐 |
| T-48 | 신청하지 않은 `PENDING` 계정이 있는 상태에서 관리자가 회원 목록 조회 | **그 계정은 목록에 나오되 승인 대상이 아니다** — `POST /admin/users/approve`에 그 id를 담아 보내면 실패로 집계된다 (T-49) |
| T-49 | **목록을 우회해** `POST /admin/users/approve`에 미신청 계정 id를 직접 전달 | 그 건은 실패로 집계되고 계정은 `PENDING`으로 남는다. `student_no=NULL`인 `ACTIVE`가 만들어지지 않는다 |
| T-50 | `ACTIVE` 사용자가 `POST /auth/application` 호출 | **`403 FORBIDDEN`으로** 거부된다 — 승인 후에는 이 경로로 학번을 바꿀 수 없다 |
| T-51 | 승인 대기 중 신청서를 다시 제출 | 성공, 내용이 갱신된다 |
| T-52 | `studentNo`·`department`를 `""`·공백만으로 제출 | `400 VALIDATION_ERROR`. **`applied_at`이 기록되지 않아 승인 대상이 되지 않는다** |
| T-320 | 본문에 `name`을 담아 `POST /auth/application` 호출 | `204`. **저장된 이름이 바뀌지 않는다** — 구글 계정의 값 그대로다. 화면만 잠그면 이 경로로 우회된다 (#224) |
| T-53 | `user@notkhu.ac.kr`로 로그인 | 거부 — **`endsWith` 검사를 쓰면 통과한다.** `@` 뒤를 잘라 정확히 비교해야 한다 |
| T-54 | `user@cs.khu.ac.kr`(하위 도메인)로 로그인 | 거부 — 정확 일치가 아니다 |
| T-55 | 이메일이 바뀌었는데 그 주소가 다른 계정에 이미 있음 | 로그인 거부(`/login?error=failed`). **두 계정이 합쳐지지 않는다** |
| T-56 | 신청서 제출과 관리자 승인을 **동시에** 실행 | 승인 후 `student_no`·`department`가 바뀌지 않는다 — 관리자가 심사한 내용과 저장된 내용이 같다. `name`은 신청서가 받는 값이 아니라 이 경합의 대상이 아니다 (#224) |
| T-181 | `department`를 정해진 목록에 없는 값으로 제출 | `400 VALIDATION_ERROR`. **`applied_at`이 기록되지 않는다** — 자유 입력을 허용하면 회원 목록에서 학과로 걸러보는 것이 무의미해진다 (#100) |
| T-182 | 신청서 제출 뒤 `GET /auth/me`, 승인 뒤 `GET /admin/users` 조회 | 응답에 `department`가 포함된다. 신청 전이거나 이 필드가 생기기 전 승인된 회원은 `null`이다 (#100) |

T-42이 이 절의 핵심이다. `hd` 파라미터는 구글이 붙여주는 힌트일 뿐이라 그것만 보고 통과시키면 도메인 제한이 무력해진다.

### 공개 랜딩

랜딩은 인증 영역 밖의 정적 페이지다 ([3-1 §3-1-3](3-1-DESIGN-ARCHITECTURE.md#3-1-3-권한-매트릭스)). 비로그인·`PENDING`·`ACTIVE`·`SUSPENDED` **어느 상태에서도** 열려야 하고, 세션 가드에 걸려 리다이렉트되면 안 된다.

| # | 조건 | 기대 |
|---|---|---|
| T-57 | 비로그인으로 랜딩 접근 | 랜딩이 그대로 렌더된다. 로그인 화면으로 보내지 않는다 |
| T-58 | `PENDING` 사용자로 랜딩 접근 | 랜딩이 그대로 렌더된다. 대기 안내 화면으로 되돌리지 않는다 |
| T-59 | `ACTIVE` 사용자로 랜딩 접근 | 랜딩이 그대로 렌더된다 |
| T-60 | 랜딩을 열었을 때 나가는 `fetch`/XHR 요청 | **나가는 요청이 전부 앱 셸의 세션 확인(`GET /auth/me`)이어야 한다.** 랜딩 콘텐츠(소개·활동·통계·FAQ·후원)를 가져오는 요청은 0건이다. 경로를 가리지 않는다 — 문서·스크립트·스타일·이미지·폰트 같은 정적 자산 요청만 세지 않는다 |
| T-61 | `SUSPENDED` 세션으로 랜딩 접근 | 랜딩이 그대로 렌더된다. 로그인 화면으로 보내지 않는다 |
| T-62 | 서빙되는 `index.html`의 `<head>` | `title`, `meta[name=description]`, `og:title`·`og:description`·`og:image`가 모두 존재한다 |
| T-334 | 비로그인으로 `/terms` 열람 (화면) | 약관이 그대로 렌더된다. 로그인 화면으로 보내지 않는다 |
| T-335 | `/terms`의 게시물 삭제 항목 | 자유 게시판 글은 **올린 사람은 직접 지울 수 없고 운영진은 관리 화면에서 완전 삭제할 수 있다**고 따로 밝힌다 ([3-3 결정 20](3-3-DESIGN-DECISIONS.md#3-3-21-결정-20--관리자가-자유-게시판-글을-삭제할-수-있다)) |
| T-336 | 랜딩 푸터·로그인 카드·앱 셸 푸터 | 세 곳 모두에서 `/privacy`와 `/terms`로 갈 수 있다 |
| T-421 | 랜딩 헤더를 `guest`, 신청 전·신청 후·사용자 정보가 없는 `pending`, `suspended`, `INACTIVE USER`, `ACTIVE USER`, `ACTIVE ADMIN`으로 각각 렌더 | 앞의 다섯 상태는 공개 앵커 다섯 개, 두 USER 상태는 내부 메뉴 네 개, ADMIN은 회원 관리를 더한 다섯 개를 정확한 순서로 보인다 |
| T-422 | T-421의 각 상태에서 데스크톱 메뉴와 모바일 메뉴를 비교 | 항목·순서·주소가 같고 공개 앵커와 내부 메뉴가 동시에 존재하지 않는다 |
| T-423 | 세션 확인이 `loading`인 랜딩 헤더 | 왼쪽 내비게이션과 모바일 메뉴 버튼이 모두 없고, 오른쪽도 기존 정책대로 로그인·계정 조작을 먼저 그리지 않는다 |
| T-424 | `ACTIVE ADMIN`의 데스크톱·모바일 메뉴 | 둘 다 갤러리와 회원 관리 사이에 스크린리더가 읽지 않는 구분자가 있다. 다른 상태에는 구분자가 없다 |
| T-425 | 320·360·390·767·768·820 CSS px(고정 스크롤바 환경의 `clientWidth` 감소 포함)에서 T-421의 모든 상태로 랜딩 헤더를 닫힘·열림 렌더 | `html`·`body`·`header`·메뉴의 `scrollWidth <= clientWidth`, 보이는 요소가 viewport 안에 있고 서로 겹치지 않는다. 좌우 24px 패딩과 32px 로고 높이를 유지하며 모바일 메뉴가 모든 진입점을 제공한다 |
| T-426 | T-425의 비로그인·신청 전·신청 후·사용자 정보 없음·정지 상태 | 지원하기·로그인·승인 대기·계정 메뉴가 기존 상태 매트릭스대로 남고, 비로그인의 로그인은 모바일 메뉴에서도 도달할 수 있다. 세션 확인 중에는 먼저 나타나지 않는다 |
| T-427 | 두 헤더의 모바일 메뉴를 버튼·링크·현재 링크·Escape로 각각 조작하고, 열린 상태에서 다른 컴포넌트의 `navigate()` 및 history로 위치 변경 | 로고 홈 링크·햄버거·계정 버튼과 펼친 메뉴 링크는 44px 이상이며, 버튼은 이름·`aria-expanded`·`aria-controls`를 가진다. 재토글과 링크 선택으로 닫히고, Escape로 닫으면 버튼에 초점이 돌아가지만 라우터 위치 변경으로 닫힐 때는 현재 초점을 빼앗지 않는다 |
| T-428 | 320·360·390·767·768·820 CSS px에서 `INACTIVE USER`·`ACTIVE USER`·`ACTIVE ADMIN`의 앱 헤더를 닫힘·열림 렌더 | 데스크톱 nav가 접히고 모바일 메뉴가 권한별 항목·순서·주소·현재 위치를 그대로 제공한다. ADMIN 구분자는 스크린리더가 읽지 않는다 |
| T-429 | 앱 헤더의 `loading`·비로그인·`PENDING` 상태 | 접근할 메뉴가 없으므로 빈 모바일 메뉴 버튼을 그리지 않고 기존 계정 조작 정책을 바꾸지 않는다 |
| T-430 | 두 헤더를 320·360·390·767·768·820·1024·1152px에서 비교 | 1024px 미만은 각 배경에 맞는 심볼·44px 계정/메뉴 버튼, 1024px 이상은 기존 가로 lockup·한 줄 nav를 쓴다. 모든 폭에서 두 헤더의 로고 높이와 컨테이너 패딩이 같고, `clientWidth = scrollWidth`이며 보이는 헤더 요소끼리 겹치지 않는다 |

T-334는 T-61과 같은 이유다. **정지된 사람이 약관을 못 읽으면** 자기가 무엇을 어겼다는 것인지 확인할 길이 없다 — 조치의 근거를 적어 둔 문서를 조치당한 사람만 못 보는 셈이 된다.

T-335가 재는 것은 **약관이 권한 매트릭스보다 넓게 적혔는가**이다 ([3-1 §3-1-3](3-1-DESIGN-ARCHITECTURE.md)). "운영진이 게시물을 지울 수 있다"로 뭉뚱그리면 자유 게시판에 대해서는 거짓이고, 그 거짓이 지워 달라는 요청을 받아 줄 수 없는 자리에서 드러난다.

**T-335는 권한 매트릭스와 공개 약관을 맞춘다.** 작성자 삭제가 없다는 사실과 운영진의 완전 삭제가 모두 보여야 한다. 한쪽만 적으면 이용자는 직접 지울 수 있다고 오해하거나, 운영진에게 요청할 수 있는 길이 없다고 오해한다.

T-336이 없으면 문서가 있어도 닿을 수 없다. 특히 **앱 셸 푸터가 로그인 이후의 유일한 길**이다 — 랜딩 푸터와 로그인 카드는 로그인한 사람이 다시 갈 일이 없다.

T-58가 특히 중요하다. `PENDING`을 대기 안내 화면으로 되돌리는 가드가 랜딩까지 잡으면, 승인 대기 중인 지원자가 동아리 소개를 볼 수 없게 된다. T-61도 같은 이유다. 프론트엔드는 정지된 세션을 세션 없음으로 수렴시켜 로그인 화면으로 보내는데, 그 정책이 랜딩까지 적용되면 정지된 사람이 공개 페이지조차 못 본다.

T-60이 재는 것은 **랜딩이 백엔드에 의존하는가**이지 요청 수가 아니다. 세션 확인은 랜딩이 시키는 일이 아니라 **앱 셸(`SessionProvider`)이 경로와 무관하게 하는 일**이다 — `/notices`로 들어가도 똑같이 나간다. 헤더가 세션 상태에 따라 네 갈래로 갈리므로(비로그인·신청 전·승인 대기·`ACTIVE`) 어느 화면에서든 필요하다. 그래서 랜딩 컴포넌트가 부르는 API는 여전히 **0건**이고, [3-3 결정 8](3-3-DESIGN-DECISIONS.md#3-3-9-결정-8--공개-랜딩-페이지를-1차-출시에-포함하고-정적으로-구현한다)의 "콘텐츠를 전부 코드에 포함하고 API를 호출하지 않는다"는 그대로 참이다. 세션 확인이 실패해도 소개·활동·통계·FAQ·후원은 그대로 렌더된다 — 백엔드가 죽어도 랜딩은 뜬다.

**그 세션 확인은 이제 실패로 끝나지 않는다** (2026-08-22, #190). 비로그인이면 서버가 `204`로 답한다 — 예전에는 `401`이라 **비로그인 방문자마다 콘솔에 오류가 하나씩 남았고, 진짜 오류가 그 사이에 묻혔다.** T-60이 재는 것(요청이 그것 하나뿐인가)은 그대로다.

**횟수로 규정하지 않는다** (MUST). React `StrictMode`는 개발에서 effect를 두 번 실행하므로 `GET /auth/me`가 2회 나간다. 이건 개발 모드 산물이지 랜딩이 백엔드에 의존한다는 신호가 아니다. 횟수를 스펙에 박으면 이런 산물에 스펙이 끌려다닌다. 검사 대상은 **어떤 요청이 나가는가**지 몇 번 나가는가가 아니다.

**대상을 넓히지 않는다** (MUST). 허용되는 것은 세션 확인 하나뿐이다. `GET /auth/me`가 아닌 요청이 하나라도 나가면 — 그것이 랜딩 콘텐츠든 통계든 활동사진이든 — 랜딩이 백엔드에 의존하기 시작한 것이므로 T-60 위반이다.

T-62은 서빙되는 HTML을 직접 본다. 링크 미리보기 봇은 JS를 실행하지 않으므로, 런타임에 태그를 꽂는 방식으로는 통과할 수 없다 ([3-3 결정 8](3-3-DESIGN-DECISIONS.md#3-3-9-결정-8--공개-랜딩-페이지를-1차-출시에-포함하고-정적으로-구현한다)). 이 검증이 없으면 카카오톡·인스타그램에 뿌린 홍보 링크의 미리보기가 깨진 채로 MVP 출시 조건을 만족한다.

### 공지 작성·수정 (화면)

공지 쓰기는 ADMIN 전용이다 ([3-1 §3-1-3](3-1-DESIGN-ARCHITECTURE.md#3-1-3-권한-매트릭스), [3-2 §3-2-5](3-2-DESIGN-CONTRACT.md)). API 레벨 차단은 T-04가 이미 본다 — 여기는 **화면이 그 계약과 어긋나지 않는지**만 다룬다.

| # | 조건 | 기대 |
|---|---|---|
| T-63 | `USER`로 `/admin/notices/new`에 주소로 직접 접근 | 폼이 그려지지 않고 공지 목록으로 되돌린다 |
| T-64 | `USER`로 `/admin/notices/{id}/edit`에 주소로 직접 접근 | 폼이 그려지지 않고 공지 목록으로 되돌린다 |
| T-65 | `USER`로 공지 목록·상세 열람 | 글쓰기·수정·삭제 진입점이 보이지 않는다 |
| T-66 | 제목 또는 내용이 비었거나 공백뿐인 상태로 저장 | 저장 요청이 나가지 않고 폼에 오류가 보인다 |
| T-67 | 공지 작성 화면의 제목 입력 | 상한(200자)이 화면에 드러나고 그 이상 입력되지 않는다 |
| T-68 | 저장 요청이 서버 오류로 실패 | 폼에 서버 메시지가 보이고 **이동하지 않으며** 입력값이 남는다 |
| T-69 | 삭제 버튼을 누른 직후 (확인 전) | 삭제 요청이 나가지 않는다. 확인 창에 **지울 공지의 제목**이 보인다 |
| T-70 | 등록·수정·삭제 성공 | 각각 새 공지 상세 · 그 공지 상세 · 공지 목록으로 이동한다 |
| T-71 | 고정하지 않은 공지를 목록의 관리 모드에서 고정 | 목록을 다시 조회해 **그 공지가 고정 표시와 함께 위로 올라간다** |

T-63~T-65가 한 벌이다. **T-65는 권한 통제가 아니다** — 진입점을 감추는 것은 편의이고, 실제 차단은 T-63·T-64의 라우트 가드와 서버다 ([3-1 §3-1-7](3-1-DESIGN-ARCHITECTURE.md)). 그래서 T-65만 통과하고 T-63이 깨지면 화면은 멀쩡해 보여도 뚫린 것이다. 이 케이스들은 컴포넌트를 직접 렌더해서 확인할 수 없다 — 가드는 라우트에 붙어 있으므로 **실제 경로로 앱을 띄워야** 밟힌다.

T-66과 T-67은 스키마에서 온다 (`title varchar(200) NOT NULL`, `content text NOT NULL`, [3-2 §3-2-2](3-2-DESIGN-CONTRACT.md)). 화면이 먼저 막는 것은 서버 검증을 대신하는 것이 아니라 같은 규칙을 앞당겨 적용하는 것이다 — 그래서 T-68이 따로 있다. 서버가 거부했는데 화면이 성공한 것처럼 넘어가면, 사용자는 올린 줄 알고 자리를 뜨고 공지는 없다.

T-69의 확인 창은 **무엇을 지우는지 제목으로 보여준다.** "이 항목을 삭제할까요?"로는 잘못된 글을 지우는 중인지 알 수 없고, 삭제는 되돌릴 수 없다.

T-71은 **호출 여부가 아니라 결과를 본다.** 고정의 효과는 "그 글이 위로 올라간다"이고 정렬은 서버가 한다 ([3-2 §3-2-2](3-2-DESIGN-CONTRACT.md) — `is_pinned DESC, created_at DESC`). 그래서 토글 후 목록을 다시 조회하지 않으면 화면 순서가 그대로다. 재조회가 곧 이 기능의 동작이므로 화면에 보이는 순서로 확인한다.

### 회원 관리 (화면)

선택 상태 조작은 여러 명을 한 요청으로 처리한다. 그래서 "무엇을 대상으로 하고 어떤 전이가
실패할 수 있는가"를 화면이 정확히 말하는지가 이 절의 중심이다. API 레벨 차단은 T-05가 이미 본다.

| # | 조건 | 기대 |
|---|---|---|
| T-72 | `USER`로 `/admin/members`에 주소로 직접 접근 | 화면이 그려지지 않고 공지 목록으로 되돌린다 |
| T-73 | 신청서를 내지 않은 `PENDING`(`applied_at IS NULL`) 행 | 상태가 **"미승인"**으로 보이고 관리 메뉴에는 destructive `제거`만 있다. inline 승인·거부 버튼과 메뉴의 승인·거부는 없고, 선택 체크박스는 있다 |
| T-74 | 상태·권한이 서로 다른 회원 행 | `PENDING`·`ACTIVE`·`INACTIVE`·`SUSPENDED`, `USER`·`ADMIN` 모두 체크박스로 선택할 수 있다 |
| T-75 | 전체 선택 체크박스 | 상태·권한과 무관하게 **현재 페이지에 보이는 모든 행만** 선택된다. 검색 결과의 다른 페이지는 선택되지 않는다 |
| T-76 | 선택된 상태의 화면 문구 | 선택 범위가 **이 페이지**임과 전체 인원이 함께 보인다 |
| T-183 | 상태 필터에서 "승인 대기" 선택 | `status=PENDING`과 `applied=true`를 **함께** 보낸다. 화면이 받아서 거르지 않는다 |
| T-184 | `?status=PENDING&applied=true`로 직접 접근 | 그 필터가 선택된 상태로 그려진다 — 뒤로가기·링크 공유에 살아남는다 |
| T-185 | 필터를 쪼개기 전의 주소(`?status=PENDING`)로 접근 | "승인 대기"로 맞추고 **그 사실을 알린다.** 화면이 "전체"라고 말하면서 일부만 보여주면 안 된다 |
| T-77 | 다섯 선택 버튼 중 하나를 누른 직후 (확인 전) | 요청이 나가지 않는다. 확인 창에 **대상 수·이름·무엇이 일어나는지·상태별 실패 가능성**이 보인다 |
| T-78 | 확인 후 일부가 실패한 응답 | 처리·실패 건수, 실패한 사람과 정확한 사유를 **서버 배열 순서대로** 안내한다. `processed`가 실제 변경과 멱등 처리를 함께 센다는 점도 말한다 |
| T-161 | 선택 요청이 `200`으로 끝남 | 성공·부분 실패와 무관하게 선택을 비우고 목록을 다시 조회한다 |
| T-79 | 선택 요청이 `400`·`403`으로 실패 | 서버 사유를 그대로 알리고 목록을 다시 조회한다. 성공 문구가 나오지 않는다 |
| T-80 | 정지 대상에 `ADMIN`이 있음 | 화면이 미리 선택을 막지 않고 `ADMIN_SUSPEND_REQUIRES_ROLE_REVOCATION` 사유를 안내한다 |
| T-81 | 목록의 "가입 신청일" 칸 | `applied_at`을 보여준다. `created_at`이 아니다 |
| T-186 | 목록의 "학과" 칸 | `department`를 보여준다. 이 필드가 생기기 전에 승인된 회원과 신청 전 계정은 `—`로 보인다 — 값이 없는 것이지 오류가 아니다 (#100) |
| T-82 | 검색·상태·권한·정렬을 바꿈 | 그 값으로 다시 조회하고 **첫 페이지로 돌아간다** |
| T-83 | 선택이 있는 상태에서 조회 조건을 바꿈 | 선택이 해제되고 **그 사실을 알린다.** 선택이 없었으면 아무 안내도 하지 않는다 |
| T-84 | 검색 후 브라우저 뒤로가기 | 검색 입력창이 URL의 검색어를 따라간다 — 목록과 입력창이 어긋나지 않는다 |
| T-85 | 범위를 넘은 `page`로 접근 | 마지막 유효 페이지로 되돌린다 |
| T-86 | 신청 완료 행 메뉴의 승인·거부 | 체크박스를 거치지 않고 그 한 명 id만 처리한다. 확인 창 전에는 요청하지 않고 **다른 사람의 선택은 건드리지 않으며**, 취소하면 같은 `…` 트리거로 포커스가 돌아간다 |
| T-87 | 확인 창이 열린 상태에서 조회 조건이 바뀜 | 확인 창이 닫히고 이유를 알린다. 사라진 조건의 대상이 승인되지 않는다 |
| T-431 | 선택 요약 오른쪽의 조작 버튼 | `선택한 회원 승인`·`선택한 회원 거부`·`선택한 회원 활성화`·`선택한 회원 비활성화`·`선택한 회원 정지`가 이 순서로 **정확히 다섯 개만** 항상 보이고 같은 variant·size·너비 규칙을 쓴다. 본문 없는 전원 비활성화·복구 전용 버튼은 없다 |
| T-432 | 다섯 버튼의 API 호출 | 승인·거부는 기존 전용 API에, 활성화·정지는 `PATCH /admin/users/status`에, 비활성화는 `POST /admin/users/deactivate`에 고른 id를 상태 선행 필터 없이 각각 한 번만 보낸다. 선택하지 않은 id는 보내지 않는다 |
| T-433 | 선택 요청이 `500` 또는 네트워크 오류로 실패 | DB 변경 여부를 단정하지 않고 목록을 다시 조회하며, 다시 고른 뒤 재시도하라고 안내한다 |
| T-434 | 회원 관리 화면을 320px·1440px Chromium에서 확인 | 다섯 버튼은 320px에서 세로로 읽고 누를 수 있고, desktop에서는 선택 요약 영역 안에서 정렬·wrap되어 페이지를 넘치지 않는다. 넓은 표는 화면 전체가 아니라 목록 surface 안에서만 가로 스크롤된다 |
| T-435 | 확인 실행을 React 재렌더보다 빠르게 연속 클릭 | 선택 승인·거부·상태 조작과 행 단건 상태 조작이 공유하는 동기 잠금으로 API는 한 번만 호출된다. 요청 중 확인 실행·다섯 버튼·행·헤더 선택 checkbox는 모두 잠긴다 |
| T-436 | 320px Chromium·실제 admin fixture에서 현재 페이지 전체 선택 | 활성화 성공은 `role=status`가 **정확히 하나**이고 `alert`는 없다. 정지 혼합 실패는 `role=alert`가 **정확히 하나**이고 `status`는 없으며, 승인 대기·관리자 실패 사유를 모두 포함한다 |
| T-437 | `ACTIVE`/`INACTIVE`/`SUSPENDED USER` 행 메뉴 | 각각 `비활성화·정지·관리자 지정·제거`, `활성화·정지·관리자 지정·제거`, `활성화·비활성화·관리자 지정·제거`만 순서대로 보인다. 관리자 지정 뒤에 구분선이 있고 제거는 destructive다 |
| T-438 | `ADMIN` 행 메뉴 | `권한 회수`만 보인다. `활성화`·`비활성화`·`정지`·`관리자 지정`·`제거`와 구분선은 없다 |
| T-439 | 행의 `활성화`·`정지`·`비활성화` 확인 | 활성화·정지는 `PATCH /admin/users/status`, 비활성화는 `POST /admin/users/deactivate`에 해당 회원 id 하나만 배열로 보낸다. `정지 해제`라는 표시·확인 문구는 없다 |
| T-440 | `INACTIVE`/`SUSPENDED USER`의 `관리자 지정` | 확인 창이 `활동 관리자로 전환`됨을 명시하고, fixture 응답은 `status=ACTIVE`, `role=ADMIN`, `deactivatedAt=null`이다. `PENDING`은 지정 메뉴가 없다 |
| T-441 | 행 상태 조작 확인을 동일 틱에서 두 번 실행 | 공유 동기 잠금이 첫 실행에서 즉시 잡히고 상태 API는 한 번만 나간다 |
| T-442 | 행 `활성화`·`비활성화`·`정지`가 `failed[0]`으로 응답 | success live region은 없고 error alert에 서버 reason을 보이며 목록을 다시 조회한다 |
| T-443 | 선택 비활성화에 `ACTIVE`·`SUSPENDED USER`, `ADMIN`·`PENDING`·`INACTIVE`를 섞음 | 앞의 두 명만 `INACTIVE`가 되고 같은 배치 `deactivated_at`을 갖는다. 나머지는 하위 호환 `NOT_ACTIVE_USER`로 집계되며, 이는 비활성화 비대상 상태·권한을 뜻한다 |
| T-444 | `INACTIVE`/`SUSPENDED USER` 승격과 상태 변경이 경쟁 | 대상 잠금과 단일 트랜잭션으로 최종 `ACTIVE ADMIN`만 커밋·세션 반영된다. `deactivated_at=NULL`, `GRANT_ADMIN` 이력 한 행이며 중간 invalid 조합은 없다 |
| T-445 | `PENDING` 대상 `PATCH .../role` = `ADMIN` | `400 VALIDATION_ERROR`. 승인·승인일시 기록을 건너뛰지 않는다 |
| T-446 | 행 `⋯` 관리 메뉴를 반복해 열고 `Escape`로 닫음 | 비모달 메뉴라 `body` 의 `overflow`·`padding`·`right`·`width`가 바뀌지 않고, 페이지가 좌우로 밀리지 않으며, 포커스가 해당 행 트리거로 돌아온다 |
| T-447 | 상태가 섞인 행을 고른 뒤 선택 승인·거부 | 고른 id 전체를 각 API에 한 번만 보내고 행 메뉴의 단건 승인·거부도 그대로 남는다. 부분 실패는 success로 빠지지 않고 서버 사유를 error live alert로 보여준다 |
| T-448 | **(웹/fixture)** 신청 완료 행을 거부 | 행은 사라지지 않고 `미승인`으로 남으며 학번·학과·가입 신청일은 `—`다. 전체 `PENDING` 목록에는 남고 `applied=true` 결과에서는 빠진다 |
| T-449 | 거부 확인·성공·불확정 안내 | 계정 삭제라고 말하지 않고 **미승인으로 되돌리며 같은 계정으로 재신청 가능**함을 말한다. 네트워크/5xx는 일부 미승인 변경 가능성과 재조회 확인을 안내한다 |
| T-450 | 전역 `AlertDialog`를 열고 닫음 | modal focus trap·`body[data-scroll-locked]` scroll lock은 유지된다. `html`이 이미 scrollbar 자리를 예약하므로 body 오른쪽 margin은 추가되지 않고, 닫으면 lock 속성이 제거되어 가운데 정렬 화면이 좌우로 밀리지 않는다 |
| T-451 | 신청 완료·신청 전 `PENDING` 행 메뉴 | 신청 완료는 normal `승인`·`거부` → 구분선 → destructive `제거`, 신청 전은 destructive `제거`만 정확히 보인다. inline 승인·거부 버튼과 상태 변경·관리자 지정·권한 회수는 없으며, status 분기가 role보다 우선한다. 승인·거부·제거 모두 기존 확인·결과·포커스 복귀 흐름을 쓴다 |
| T-452 | 공지·자료(전체·즐겨찾기)·갤러리·자유게시판·회원 목록의 공통 페이지 번호 | 767px 이하는 최대 5개, `md`(768px) 이상은 최대 10개 숫자를 보인다. 상한 이하는 전부, 그보다 많으면 첫·마지막과 현재 주변의 연속 창을 보이고 실제 gap에만 비대화형 `…`가 있다. 반응형으로 숨은 링크는 접근성 트리·tab 순서에서 빠지고 viewport별 현재 번호는 1-based `{n}페이지로 이동`과 `aria-current="page"`를 정확히 하나 가진다 |
| T-453 | 다섯 목록에서 번호·`이전`·`다음`으로 페이지 이동 | 기존 query·필터를 보존하고 내부 API에는 0-based page를 보낸다. 특히 자료는 `category`·`bookmarked`·검색·과목·교수·연도·학기·시험 구분·정렬을 보존한다. 1페이지는 `page`를 지우고 양 끝 비활성 링크를 눌러도 URL·조회가 바뀌지 않는다. 회원 번호 이동은 T-83·T-87처럼 선택과 열린 확인을 해제·안내한다 |
| T-454 | 공통 pager를 320px·767px·768px·desktop에서 확인 | 767px 이하에는 숫자 최대 5개와 한국어 accessible name을 가진 화살표만, 768px 이상에는 숫자 최대 10개와 시각 `이전`·`다음`이 보인다. `flex-wrap`은 좁은 화면에서 페이지 가로 overflow를 막으며 JavaScript viewport 분기나 중복 current/tab stop이 없다 |

T-183이 **화면에서 거르지 않는다**를 지킨다. 서버가 20건을 주고 화면이 그중 일부를 버리면 페이지마다 보이는 건수가 들쭉날쭉하고 **총 건수와 총 페이지 수가 실제와 어긋난다** — 관리자가 "12명 남았다"고 읽는 숫자가 틀리게 된다.

필터의 낱말은 **행의 상태 칸과 같아야 한다.** 전에는 필터의 "미승인"이 `PENDING` 전부였고 행의 "미승인"은 그중 신청 안 한 계정만이라 **같은 낱말이 두 범위로 쓰였다** — 서버가 둘을 갈라 주므로(§3-2-6) 그 임시 조치는 끝났다 (2026-08-15, #142).

T-73~T-76은 **표시와 선택 범위를 분리한다.** 신청하지 않은 계정은 행별 승인 대상이 아니지만,
선택 상태 변경 API는 모든 행을 받아 항목별 실패를 돌려준다. 그래서 화면은 그 계정도 선택할 수
있게 두고 확인 창과 결과에서 실패 가능성·사유를 정확히 말한다. 전체 선택의 경계는 상태가
아니라 현재 페이지다.

**그 계정이 목록에 안 보이는 것이 아니다** (T-48도 같은 기준이다). 상태 칸이 "미승인"과
"승인 대기"를 갈라 행별 승인 가능 여부를 설명하고, 체크박스는 선택 상태 조작의 대상으로
그 행을 포함한다. 두 개는 서로 다른 범위다.

T-77과 T-86은 상태·계정을 바꾸는 조작 전부에 확인 창이 붙는다는 같은 규칙이다. 특히 정지와
비활성화는 접근을 제한하고, 활성화는 신청 승인까지 포함할 수 있으므로 대상과 실패 가능성을
확인 전에 숨기면 안 된다.

T-75와 T-76도 짝이다. 전체 선택이 "검색 결과 전부"를 뜻하면 관리자가 보지 못한 다른 페이지의
회원까지 바뀐다. 그래서 범위를 현재 페이지로 한정하고 화면도 그 범위를 말한다.

T-80은 `ADMIN`을 선택할 수 있되 서버가 요구한 순서를 안내하는지 본다. 정지 전에 먼저 권한을
회수해야 하므로, 이 실패를 일반 권한 오류로 뭉개면 관리자는 다음 조치를 알 수 없다.

T-78과 T-161이 짝이다. `processed`는 실제 변경뿐 아니라 멱등 처리도 포함하고, 실패 사유는
전이마다 다르다([3-2 §3-2-6](3-2-DESIGN-CONTRACT.md)). 화면이 이를 하나로 뭉치면 처리 건수를
거짓으로 설명하거나 운영자에게 틀린 후속 조치를 안내한다.

T-161은 `200` 응답으로 한 시도를 끝냈다는 경계다. 성공과 실패를 결과로 안내한 뒤에는 재조회한
현재 상태에서 다시 고르게 한다. `500`은 반대로 결과를 확정할 수 없으므로 T-433의 보수적인
안내와 재조회가 필요하다.

T-83·T-86·T-87은 **선택과 확인 창이 조용히 어긋나는 경우**를 모아 본다. 조회 조건이 바뀌면
화면에서 사라진 대상을 비우고 알리되, 행별 처리는 별도 선택을 건드리지 않아야 한다.
T-452·T-453의 번호 이동도 같은 조회 조건 변경이다. 번호 링크가 `pageParams`를 우회하면
query 보존뿐 아니라 이 선택·확인 정리 흐름까지 갈라지므로, 직접 이동과 범위 초과 클램프
(T-85)가 모두 `writePage`의 0-based 규약을 쓰는지 함께 본다.

T-431은 API 하위 호환성과 현재 UI를 분리한다. 본문 없는 전원 비활성화 경로는 서버 계약에
남지만, #297 회원 관리 화면에서 누를 수 있는 일괄 조작은 **선택 기반 다섯 버튼뿐**이다.
버튼 이름의 일부 포함 여부가 아니라 정확한 개수·순서와 기존 버튼의 부재를 함께 검사한다.

T-435는 `disabled` 렌더만으로 막히지 않는 틈을 본다. 첫 클릭이 상태를 예약했지만 아직 DOM이
잠기기 전에 둘째 클릭이 들어와도 동기 잠금이 먼저 서 있어야 한다. T-436은 mock 결과 문구만
맞추지 않고 실제 fixture의 상태 전이와 실패 사유를 연결해 live region이 하나만 남는지 확인한다.

**#324 웹은 #320·#321·#322 API에 의존한다.** Fixture는 시각 검수를 위해 최종
계약을 재현하지만, 배포는 #320(선택 `SUSPENDED` 비활성화)·#321(원자적
`ACTIVE ADMIN` 승격)·#322(선택 bulk 상태 변경)가 #324보다 먼저 merge·deploy된
상태에서만 진행한다. 이 순서를 어기면 화면에는 메뉴가 보이지만 실제 API가
거부하는 의존성 오류다.

### CSRF 토큰 (클라이언트)

서버가 토큰을 검증하는지는 [T-35~T-39](#5-2-필수-테스트-사례)가 본다. 여기는 **클라이언트가 헤더를 싣는가**만 다룬다 — 서버가 아무리 올바르게 검증해도 클라이언트가 안 실으면 모든 쓰기가 403이다.

| # | 조건 | 기대 |
|---|---|---|
| T-88 | `POST`·`PATCH`·`DELETE` 요청 | `XSRF-TOKEN` 쿠키 값이 `X-XSRF-TOKEN` 헤더로 실린다 |
| T-89 | `GET`·`HEAD`·`OPTIONS` 요청 | 헤더가 실리지 않고 발급도 부르지 않는다. **대소문자를 가리지 않는다** (`get`도 안전) |
| T-90 | 계약이 열거하지 않은 쓰기 메서드(`PUT` 등) | 헤더가 실린다 — 대상을 나열하지 않고 안전한 메서드만 나열한다 |
| T-91 | 쿠키가 없는 상태의 첫 쓰기 요청 | `GET /auth/csrf`를 **먼저** 부르고 받은 값을 싣는다 |
| T-92 | 쿠키가 이미 있는 상태의 쓰기 요청 | 발급을 부르지 않는다 |
| T-93 | 쿠키가 없는 상태에서 쓰기 요청이 **동시에** 여러 개 | 발급은 **한 번만** 나간다 |
| T-94 | 쿠키 값이 퍼센트 인코딩되어 있음 | 디코딩해서 싣는다 |
| T-95 | 이름이 비슷한 다른 쿠키(`XSRF-TOKEN-BACKUP` 등)가 함께 있음 | `XSRF-TOKEN`만 정확히 읽는다 |
| T-96 | 발급이 실패하거나 성공했는데 쿠키가 없음 | **쓰기 요청을 보내지 않고** 그 실패를 그대로 알린다 |

T-89가 T-60과 이어진다. 발급을 앱 시작에 미리 부르거나 안전한 메서드에까지 태우면 **랜딩에서 `GET /auth/csrf`가 나가 T-60이 깨진다.** 계약도 "첫 상태 변경 요청 전에"라고 쓴다([3-2 §3-2-3](3-2-DESIGN-CONTRACT.md)) — 앱 시작이 아니다.

T-96이 중요한 이유는 **오진 때문이다.** 토큰 없이 보내면 서버는 `403 FORBIDDEN`을 주고 화면은 그 메시지를 "권한이 없습니다"로 보여준다 — 원인은 토큰인데 사용자는 자기 권한 문제로 읽는다. 실패가 확정된 요청을 보내는 것이기도 하다.

이 절은 `request()`를 mock하고는 검증할 수 없다. **전역 `fetch`를 감시해 실제로 나간 요청의 헤더를 봐야 한다** — mock 뒤에서는 헤더가 실렸는지 알 수 없다.

### 로그인 화면

서버가 어떤 코드로 되돌리는지는 [T-03·T-08·T-41·T-42·T-55](#5-2-필수-테스트-사례)가 본다. 여기는 **그 주소에 도착한 화면이 무엇을 보여주는가**만 다룬다.

| # | 조건 | 기대 |
|---|---|---|
| T-97 | `/login?error=domain`·`unverified`·`suspended`·`failed` | 네 코드가 **각각 다른** 안내를 띄운다 ([3-2 §3-2-3](3-2-DESIGN-CONTRACT.md) 표) |
| T-98 | `/login` (쿼리 없음, 비로그인) | 아무 안내도 띄우지 않는다 |
| T-99 | **정지된 세션**으로 `/login` (쿼리 없음) | `?error=suspended`와 **같은** 정지 안내를 띄운다 ([3-1 §3-1-5](3-1-DESIGN-ARCHITECTURE.md)) |
| T-100 | `/login?error=` 에 표에 없는 값 | 아무 안내도 띄우지 않는다. 받은 값을 화면에 그리지 않는다 |
| T-101 | 로그인 버튼 | 브라우저 전체가 구글 로그인 경로로 이동한다. `fetch`로 부르지 않는다 |
| T-102 | 이미 로그인한 사용자(`ACTIVE`·`PENDING`)가 `/login` 접근 | 각자 홈으로 되돌린다 |
| T-103 | `/signup` 접근 | 로그인 화면으로 보낸다. 별도 가입 화면은 없다 ([2-1 §2-1-10](2-1-USER-STORIES.md)) |
| T-117 | 로그인 버튼의 이름 | **`Google로 계속하기`** — 승인된 CTA의 한글판이다 ([3-1 §3-1-5](3-1-DESIGN-ARCHITECTURE.md)) |
| T-118 | 버튼 안의 구글 로고 | **공식 배포본의 모양 그대로다.** 직접 그리거나 색을 바꾸지 않는다 |

T-99가 이 절의 핵심이다. 정지 안내가 필요한 경로는 둘인데(§3-1-5) **쿼리가 없는 쪽이 더 흔하다** — 이용 중 정지된 사람은 관리자가 정지시킨 직후 자기 화면에서 튕겨 나온다. 이 경우를 빠뜨리면 **설명 없는 로그인 화면으로 한 번 튕긴다.** 다시 로그인하면 콜백이 `?error=suspended`를 주므로 안내가 영영 안 뜨지는 않지만, 그때까지는 무엇이 잘못됐는지 알 수 없다.

T-100은 **쿼리 값을 그대로 그리지 않는 것**까지 본다. 계약이 쿼리에 이메일·토큰·예외 메시지를 담지 않기로 했지만(§3-2-3 MUST), 화면이 받은 값을 출력하면 그 규정이 깨졌을 때 그대로 새어나간다. 알려진 코드를 문구로 매핑하고 매핑에 없으면 아무것도 띄우지 않는다.

T-117은 문구가 **승인 목록 안에 있는지**를 본다. 가이드라인이 세 가지 CTA만 허용하고 그 현지화를 허용한다 — 임의 표현을 쓰면 승인 범위 밖이다.

T-118은 로고가 **공식 모양 그대로인지**를 본다. 로고는 우리가 그리는 것이 아니므로 모양이 바뀌었다는 것은 누가 손댔다는 뜻이고, 눈으로는 알기 어려워 자동으로 잡는다.

**다만 자동으로 잡는 범위에 끝이 있다.** 로고 마크업이 공식 배포본과 같은지, 그리고 로고와 버튼에 직접 건 표시 속성 일부가 기본값인지를 본다. **정확히 무엇을 보는지는 테스트가 원본이다** (`apps/web/src/features/auth/LoginPage.test.tsx`) — 여기 말로 옮겨 적으면 검사와 설명이 어긋난다. 실제로 세 번 어긋났고 그때마다 설명 쪽이 넓었다.

**CSS로 최종 표시를 바꾸는 경로를 전부 막지는 못한다.** 그 경로는 열거할 수 없어서, 하나를 막으면 다른 하나가 나오고 목록을 늘릴수록 정당한 변경까지 막을 위험만 커진다.

**그래서 T-118을 통과한 것이 가이드라인 준수의 증명은 아니다.** 로고 주변의 CSS를 새로 건드리는 변경은 사람이 화면을 보고 확인한다. 이 경계를 적어 두는 이유는, 모른 채로 경계 밖으로 나가는 것과 알고 나가는 것이 다르기 때문이다.

T-101은 `fetch`가 아니라 **브라우저 이동**임을 확인한다. OAuth는 리다이렉트 흐름이라 `fetch`로는 성립하지 않는다 — 구글 화면이 떠야 하는데 응답만 받아온다.

### 신청·대기 화면

서버가 무엇을 저장하고 무엇을 거부하는지는 [T-46·T-47·T-50·T-51·T-52·T-07](#5-2-필수-테스트-사례)이 본다. 여기는 **한 화면이 두 모습을 어떻게 가르는가**만 다룬다.

| # | 조건 | 기대 |
|---|---|---|
| T-104 | 신청 전 `PENDING`(`appliedAt` 없음)으로 `/pending` | 학번·학과 **신청 폼**이 뜬다. 이름·이메일은 구글 계정 값이 **채워진 채 읽기 전용**이다 — `disabled`가 아니라 `readOnly`다 (#224) |
| T-105 | 신청 후 `PENDING`(`appliedAt` 있음)으로 `/pending` | **대기 안내**가 뜨고, 신청 내용을 고칠 수 있다 |
| T-106 | **신청 여부를 모르는** `PENDING`(403으로만 알아낸 상태) | **폼도 안내도 띄우지 않는다.** `GET /auth/me`로 확인한 뒤에 정한다 |
| T-107 | 신청서 제출 성공 | 서버에서 다시 읽어 대기 안내로 바뀐다. 새로고침해도 유지된다 |
| T-108 | `409 DUPLICATE_STUDENT_NO`·`400 VALIDATION_ERROR` | 화면에 사유가 뜨고 **입력이 남는다.** 성공한 것처럼 보이지 않는다 |
| T-109 | 대기 안내의 **"다시 확인"** 버튼 | `GET /auth/me`를 다시 부른다. 승인되었으면 그때 화면이 풀린다 |
| T-110 | 대기 안내 문구 | **저절로 바뀌지 않는다**는 것과 다시 확인해야 한다는 것을 알린다 |
| T-111 | `PENDING`이 `/notices` 등 다른 인증 화면에 접근 | `/pending`으로 되돌린다 — 인증 화면 중 여기만 열린다 |
| T-403 | 신청 폼의 학과 목록 | **`GET /departments`가 내려준 값이다** (#166). 화면은 목록을 갖고 있지 않다 |
| T-404 | 그 요청이 실패 | **못 받았다는 것을 알리고 다시 부를 자리를 준다.** 조용히 빈 `<select>`를 두지 않는다 — 학과는 필수라 신청 자체가 막힌다 |

T-106이 이 절의 핵심이다. 신청 여부를 모르는 상태가 실제로 존재하고(§3-1-6), **기본값으로 폼을 띄우면 이미 신청한 사람이 다시 쓰게 된다.** 그 사람은 자기가 낸 내용을 볼 수 없어 학번을 새로 기억해내야 하고, 재제출은 성공하므로 잘못된 값이 그대로 저장된다.

T-109·T-110도 한 벌이다. 승인은 화면에 저절로 반영되지 않으므로(§3-1-6) 버튼이 필요한데, **버튼만 두고 설명이 없으면 누를 이유를 모른다.** 둘 중 하나만 지키면 승인된 사람이 대기 화면에 머문다.

T-404가 T-403의 짝이다. 목록을 서버에서 받게 되면 **못 받는 경우가 생긴다** — 학과 없이는 서버가 신청을 거부하므로(T-181), 빈 `<select>`를 조용히 두면 사용자는 제출이 안 되는 이유를 알 수 없다.

T-108의 "입력이 남는다"가 중요하다. 학번을 다시 입력하게 하면 사용자는 무엇이 거부됐는지 확인할 방법이 없다.

### 세션 상태 판단 (클라이언트)

서버가 어떤 코드를 주는지는 [T-01·T-02·T-32](#5-2-필수-테스트-사례) 등이 본다. 여기는 **클라이언트가 그 응답으로 세션을 어떻게 바꾸는가**만 다룬다 ([3-1 §3-1-5](3-1-DESIGN-ARCHITECTURE.md)).

| # | 조건 | 기대 |
|---|---|---|
| T-112 | 세션이 있는 상태에서 `GET /auth/me`가 **네트워크 오류**로 실패 | 세션을 그대로 둔다. 로그인 화면으로 보내지 않는다 |
| T-113 | 같은 상황에서 **5xx**·계약에 없는 코드로 실패 | 같다 — 세션을 바꾸지 않는다 |
| T-114 | `401 UNAUTHENTICATED` | 비로그인으로 정리한다 |
| T-115 | `403 SUSPENDED` | 정지 세션으로 정리한다. 화면은 정지 안내로 간다 (T-99) |
| T-116 | 화면의 조작이 `403 SUSPENDED`로 실패 | 화면이 그 코드를 세션 계층에 넘긴다 — 오류 문구만 그리고 넘어가지 않는다 |

T-112·T-113이 이 절의 이유다. **"서버에 못 닿았다"는 "로그아웃됐다"가 아니다.** 이 둘을 섞으면 네트워크가 잠깐 끊긴 것만으로 로그인 화면으로 튕기고, 사용자는 자기가 무엇을 잘못했는지 알 수 없다.

T-116은 화면마다 빠지기 쉽다. `403`은 `PENDING_APPROVAL`·`SUSPENDED`·`FORBIDDEN`이 함께 쓰므로 **화면이 코드를 세션 계층에 넘겨야** 정지·승인 대기가 감지된다. 넘기지 않으면 오류 문구만 뜨고 세션은 옛 상태로 남는다.

### 비동기 결과 알림과 안정된 화면 자리

짧은 조작 결과는 viewport 고정 live alert로 알리되, 해결에 필요한 상태는 화면 안의 예약된
자리에 남긴다. 두 경로를 함께 쓰면 같은 실패가 두 번 발표되고, 둘 다 쓰지 않으면 발표 경로가
사라진다. 따라서 아래 사례는 문구의 존재뿐 아니라 **발표 노드의 수와 위치**를 함께 본다.

| # | 조건 | 기대 |
|---|---|---|
| T-421 | success·info·error를 차례로 호출 | 화면에는 최신 live node **하나만** 있다. success/info는 `role=status` + `aria-live=polite`, error는 `role=alert`, 모두 `aria-atomic=true`다 |
| T-422 | 기본 자동 소멸 | success/info는 6초, error는 10초다. 80자 이상은 늘어나되 15초를 넘지 않는다 |
| T-423 | 새 알림·같은 문구 재호출·닫기·provider unmount·StrictMode 재실행 | 이전 timer가 새 알림을 닫지 않고, 같은 문구도 새 node로 발표하며, 닫기는 포커스를 옮기지 않고 timer를 정리한다 |
| T-424 | pathname/search 이동 | 일반 알림은 사라진다. 알림 원인이 된 1회 navigation을 건너야 해 `persistOnNavigation`을 명시한 쓰기 성공(저장·업로드·삭제)·URL 정규화 안내만 다음 route 한 번까지 남고 그 다음 이동에는 사라진다 |
| T-425 | `reportApiError()`가 세션 전이 오류를 처리 | `true`를 반환하고 호출부는 전역 alert를 만들지 않는다. 이동한 로그인·대기·제한 화면의 지속 안내만 남아 발표가 중복되지 않는다 |
| T-426 | 목록의 loading → fetch error → retry → ready/empty | 같은 `data-list-surface` 안에서 바뀌고 `data-pager-slot`은 계속 남는다. fetch error는 그 surface의 인라인 alert **하나**로만 발표한다 |
| T-427 | 신청·공지·자료·게시글 폼의 필드 검증 | 오류가 난 필드만 `aria-invalid=true`이고 그 필드의 `aria-describedby`가 정확한 오류를 가리킨다. 빈 때도 `data-form-feedback-slot` 또는 업로드 feedback slot이 남는다 |
| T-428 | 사진·자료 업로드 진행 및 부분 실패 | progress와 `failure.key`로 원본에 연결한 `파일명: 사유` 목록이 높이가 명시된 인라인 slot에 남고, 초과 목록은 내부 스크롤된다. 사진 부분 실패의 fixed alert는 성공/실패 건수 요약 한 건만 발표하며 인라인 목록은 중복 live region이 아니다 |
| T-429 | 삭제·탈퇴·회원 상태 변경 | 실행 전 `AlertDialog`의 설명·취소·확인과 disabled 조건이 보존되고, 확인 뒤 짧은 실행 결과만 fixed alert로 바뀐다 |
| T-430 | fixed alert를 320px·1440px에서 표시 | 5rem 헤더 아래 12px 간격의 상단 중앙에 있고 safe area를 침범하지 않는다. 긴 한글·공백 없는 URL이 가로로 넘치지 않으며, 바깥 layer는 아래 조작을 막지 않는다. 일반 콘텐츠 < fixed alert < 공개·내부 헤더와 그 모바일 메뉴 < `AlertDialog`·dropdown 순으로 쌓인다. 내부 헤더는 이 계층을 위해 레이어만 만들고 sticky로 바꾸지 않는다 |

T-425는 세션 판정과 시각 알림을 분리하는 경계다. `reportApiError()` 안에서 alert까지 만들면
모든 호출부가 간단해 보이지만, 가드가 옮긴 화면의 설명과 같은 오류를 두 번 읽는다. 반대로
호출부가 반환값을 무시하면 세션 전이 오류와 일반 오류를 같은 문구로 한 번 더 알리게 된다.

T-426·T-427·T-428은 fixed alert만으로 해결되지 않는 이동을 잡는다. 목록 결과 자체와 pager,
필드별 오류, 업로드 진행은 문서 흐름에 남아야 하므로 빈 상태에도 자리를 예약한다. 좌표와
가로 overflow, pointer-events, dialog 적층은 jsdom이 계산하지 못하므로 T-430은 실제 Chromium
수동 검증도 함께 남긴다.

사진의 T-428은 `failed` 배열 순서만 보고 사유를 나열하지 않는다. 등록 전에 받은 key 배열과
제출 당시 파일 배열을 짝지어 서버가 돌려준 `failure.key`를 원본 파일명으로 역매핑한다. 실패
목록은 고정 높이 영역 안에서 스크롤되고 live role은 두지 않는다. 건수 요약을 발표하는 fixed
error alert와 같은 내용을 두 번 읽지 않게 하기 위해서다.

T-81은 헷갈리기 쉬운 자리라 따로 둔다. `created_at`(첫 구글 로그인)과 `applied_at`(신청서 제출)은 며칠 차이가 나므로([2-2 §2-2-1](2-2-OPERATOR-REQUIREMENTS.md) MUST) 바꿔 쓰면 운영자가 다른 날짜를 보고 판단한다. 형태가 같은 두 날짜라 눈으로는 안 걸린다.

## 5-3 수동 검증

자동화 비용이 큰 것들이다. 관련 기능을 건드린 PR에서 확인하고 결과를 본문에 적는다 (MUST).

- [ ] 로컬 `docker compose up -d` 후 `./gradlew bootRun` → DB 연결 확인
- [ ] `curl localhost:8080/actuator/health` → `{"status":"UP"}`
- [ ] 브라우저에서 파일 업로드 → S3 콘솔에서 오브젝트 확인 → 다운로드 링크로 열림
- [ ] 구글 계정으로 처음 로그인 → 신청 폼이 뜨는지, 제출 후 대기 안내로 바뀌는지
- [ ] 로그인 **전후로 `XSRF-TOKEN` 쿠키 값이 바뀌는지** — 안 바뀌면 공격자가 미리 심어둔 토큰이 로그인 후에도 유효하다 (토큰 고정)
- [ ] 승인 대기 계정으로 다른 경로 접근 → 신청·대기 화면으로 리다이렉트되는지
- [ ] 관리자 화면 URL을 `USER` 계정으로 직접 입력 → 접근 차단되는지
- [ ] 랜딩의 섹션 앵커(소개·활동사진·통계·FAQ·후원)를 눌러 해당 섹션으로 이동하는지 — 새 라우트로 나가지 않는다
- [ ] 배포 후 `curl https://api.khuhacker.com/actuator/health` → 200

## 5-4 오류 응답 규칙

- 모든 오류는 커스텀 예외 + `@RestControllerAdvice`로 일괄 처리한다 (MUST). 컨트롤러에서 `try-catch`로 응답을 만들지 않는다.
- 응답 본문 형식:

  ```json
  { "code": "PENDING_APPROVAL", "message": "승인 대기 중인 계정입니다." }
  ```

- `message`는 사용자에게 보여줄 수 있는 문장으로 쓴다 (MUST). **스택 트레이스, SQL, 내부 경로를 응답에 담지 않는다.**
- 로그인 거부 사유는 이용자가 스스로 고칠 수 있는 수준으로만 알린다 (MUST). 도메인이 다르면 "`khu.ac.kr` 계정으로 로그인하세요"처럼 안내한다 — 구글이 이미 본인을 인증했으므로 계정 존재 여부가 새어나가는 문제가 아니다.
- 서버 오류(5xx)는 로그에 스택 트레이스를 남기고, 응답에는 일반 메시지만 반환한다. 본문의 `code`는 `INTERNAL_ERROR`다 — **§3-2-7 표에는 넣지 않는다.** 5xx는 화면이 코드로 분기할 일이 없고, 웹은 계약에 없는 코드를 `INVALID_RESPONSE`로 격리해 일반 문구를 보여준다 (T-113). 표에 넣으면 서버 내부 사정이 프론트 분기 대상이 된다.
- 코드 목록은 [3-2 §3-2-7](3-2-DESIGN-CONTRACT.md)이 원본이다. 새 코드를 추가하면 그 표를 같은 PR에서 갱신한다 (MUST).
- **`@RestControllerAdvice`는 `DispatcherServlet` 안에서만 동작한다.** 그 앞의 필터에서 거절되는 요청(`UNAUTHENTICATED`, CSRF `FORBIDDEN`, 세션 상태 검사의 `PENDING_APPROVAL`·`SUSPENDED`)은 advice에 도달하지 않는다. 그 경로도 같은 본문 형식을 내보내야 한다 (MUST) — 형식을 각자 만들면 계약이 두 갈래로 갈라진다.

### 공통 예외 처리

advice가 계약대로 응답하는지 본다. 필터 계층에서 나가는 응답은 T-02·T-32·T-36·T-37이 각각 본다.

| # | 조건 | 기대 |
|---|---|---|
| T-141 | `BusinessException(PENDING_APPROVAL)`을 던지는 핸들러 호출 | `403` + `{"code":"PENDING_APPROVAL", ...}`. 상태·코드·기본 메시지가 코드 정의에서 나온다 |
| T-142 | 같은 응답의 본문 필드 수 | `code`·`message` **둘뿐이다.** 필드가 늘면 웹의 파싱 계약이 흔들린다 |
| T-119 | 메시지를 덮어쓴 `BusinessException` | 메시지만 바뀌고 코드·상태는 그대로다 |
| T-120 | `@Valid` 실패 | `400 VALIDATION_ERROR`. 메시지는 **DTO에 적은 문장**이 그대로 나온다 (T-108) |
| T-121 | 본문이 JSON으로 읽히지 않음 | `400 VALIDATION_ERROR`. 파서 위치·클래스명이 응답에 없다 |
| T-122 | 경로변수 타입 불일치 | `400 VALIDATION_ERROR` |
| T-123 | 매핑된 핸들러가 없는 경로 | `404 NOT_FOUND`. Spring 기본 형식이 나가면 웹이 사유를 읽지 못한다 |
| T-124 | 계약에 없는 예외 | `500` + `INTERNAL_ERROR`. **예외 메시지의 접속 문자열이 응답에 없다** |
| T-125 | 에러 코드 정의와 §3-2-7 표 대조 | 코드 9개가 모두 있고 **그 밖의 코드는 없다.** 상태도 표와 같다 |
| T-126 | 필터 계층용 응답 작성기 | advice와 같은 형식·상태를 쓰고 UTF-8을 선언한다 |
| T-127 | `POST` 전용 경로에 `GET` | **`405`다.** 포괄 핸들러가 삼켜 `500`이 되면 안 된다. `Allow` 헤더가 남는다 |

T-142와 T-125가 이 절의 이유다. 웹은 `ERROR_CODES` 목록에 없는 코드를 `INVALID_RESPONSE`로 버리므로, **서버가 코드를 늘리고 웹을 안 고치면 화면에서 사유가 조용히 사라진다.** 두 테스트가 표·enum·웹 목록을 같이 고쳤는지 되묻는 자리다.

T-124의 "접속 문자열"은 실제 사고 형태다. 예외 메시지를 그대로 `message`에 담는 구현이 흔한데, JDBC URL·호스트명이 그 안에 들어 있다.

T-127은 **매핑하지 않는 것과 삼키는 것이 다르다**는 점을 지킨다. 405·406·415는 계약에 코드가 없어 계약 형식으로 바꾸지 않지만, 포괄 예외 처리가 먼저 잡으면 `500 INTERNAL_ERROR`가 되어 클라이언트 실수가 서버 오류 로그로 쌓인다. 상태와 헤더는 Spring이 정한 대로 내보낸다.

### 인증 기반 설정

시큐리티 필터 체인을 실제로 태워서 본다 (§5-1). 로그인 흐름 자체(콜백·계정 생성·세션 발급)는 #25·#26이 다룬다.

| # | 조건 | 기대 |
|---|---|---|
| T-128 | 미인증으로 보호된 경로 호출 | `401 UNAUTHENTICATED`. **본문이 §5-4 형식이다** — 필터에서 나가는 응답도 advice와 같아야 화면이 코드로 분기한다 |
| T-129 | `GET /api/v1/oauth2/authorization/google` | 구글 로그인 화면으로 리다이렉트된다 |
| T-130 | 같은 요청의 `redirect_uri` | **설정값 그대로다.** 요청 host로 조립하지 않는다 |
| T-131 | 같은 요청의 `state` | 붙어 있다. 끄면 로그인 CSRF가 열린다 (T-40) |
| T-132 | 프레임워크 기본 경로 `GET /oauth2/authorization/google` | **열려 있지 않다.** `/api/v1` 아래로 옮겼다 |
| T-133 | `app.auth.allowed-email-domain`이 없거나 공백 | **기동에 실패한다** |
| T-134 | 미인증 요청 뒤 세션 | **만들어지지 않는다.** 비로그인 방문자마다 세션 행이 쌓이면 안 된다 |
| T-135 | 구글 자격 없이 `local` 프로파일 기동 | **뜬다.** 로그인과 무관한 작업까지 시크릿을 요구하지 않는다 |
| T-136 | 허용 도메인 밖의 계정, `email_verified`가 거짓인 계정 | **인증되지 않는다.** 도메인은 접미사로 본다 |
| T-137 | 계약에 없는 실패 사유 | 전부 `failed`로 접힌다. Spring 내부 코드가 쿼리에 실리지 않는다 |
| T-138 | 저장된 인가 요청 없이 콜백 도착 (`state` 불일치와 같은 경로) | `/login?error=failed`로 리다이렉트. JSON을 반환하지 않는다 |
| T-139 | 허용 도메인 계정이 구글 인증에 **성공** | **세션이 남지 않는다.** 계정 조회·세션 발급이 붙기 전까지 로그인이 성립하지 않는다 |
| T-140 | `@PreAuthorize`가 거절한 요청 | `403 FORBIDDEN` + 계약 형식 본문. **`500`이 되면 안 된다** |

T-140이 없으면 권한 부족이 서버 오류로 기록된다. 메서드 보안은 핸들러를 부르는 도중에 예외를 던지므로 `DispatcherServlet`이 먼저 잡고, 시큐리티의 `ExceptionTranslationFilter`까지 올라가지 못한다 — 포괄 예외 처리가 그대로 삼킨다.

T-136이 **로그인 자체를 막는 유일한 검사다.** 기본 `oauth2Login()`은 토큰 교환에 성공하면 그대로 신원을 세션에 올리므로, 이 검사를 걸지 않으면 아무 구글 계정이나 통과한다. 도메인은 `contains`가 아니라 `@도메인` 접미사로 본다 — 포함으로 짜면 `someone@khu.ac.kr.evil.com`이나 `khu.ac.kr@gmail.com`이 통과한다.

T-138이 없으면 사용자가 **SPA 밖의 빈 화면에 갇힌다.** 콜백은 브라우저 전체가 이동한 흐름이라 프론트엔드의 공통 오류 처리(`request()`)를 거치지 않는다.

T-139는 **계정 조회(#25)와 세션 발급(#26)이 붙을 때까지만 유효하다.** 인증은 JWT와 서버 세션이 함께 성립해야 하는데(§3-1-5 MUST), 둘 다 없는 상태에서 구글 인증만으로 세션을 만들면 **`users` 행도 `status` 확인도 없는 신원이 `authenticated()`를 통과한다.** 허용 도메인을 지났다는 것은 "우리 학교 사람"이라는 뜻일 뿐 "가입이 승인된 회원"이 아니다. 그 두 이슈가 이 테스트를 정상 로그인 사례로 바꾼다.

T-134가 막는 것은 조용한 낭비다. 시큐리티 기본 설정은 401로 돌려보내기 전에 그 요청을 세션에 저장하는데, 화면은 랜딩을 포함해 **최초 렌더마다 `GET /auth/me`를 부른다.** 그대로 두면 비로그인 방문자 한 명당 세션 행 하나가 RDS에 쌓이고, 로그인 후 돌아갈 곳도 저장된 API 경로가 되어 SPA 대신 JSON 응답에 멈춰 선다. 오류가 나지 않아 배포 후에도 모른 채 지나간다.

T-135는 **설정 파일을 실제로 읽어서** 본다. 값을 테스트에서 흉내 내면 정작 그 파일이 바뀌었을 때 아무것도 잡지 못한다.

T-130과 T-132가 같은 사고를 막는다. **로컬에서는 되고 배포하면 안 되는** 종류라 눈으로는 걸리지 않는다. 프록시가 두 겹(Vercel → ALB)이라 Spring이 요청 host로 조립한 `redirect_uri`는 구글 등록값과 어긋나 `redirect_uri_mismatch`로 거부되고([docs/ops/infra.md](../docs/ops/infra.md)), 기본 경로에 남아 있으면 Vercel rewrites가 `/api/*`만 프록시하므로 콜백이 ALB에 닿지 않는다.

T-133은 **실패하는 쪽이 안전한** 경우다. 기본값을 코드에 심어두면 설정 누락이 조용히 지나가고, 도메인 제한 없이 아무 구글 계정이나 가입할 수 있는 상태로 서비스가 떠 있게 된다.

---
[← 이전: 결정 기록](3-3-DESIGN-DECISIONS.md) · [다음: 배포 →](7-DEPLOYMENT.md)
