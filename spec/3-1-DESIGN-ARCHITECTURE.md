[← 스펙 인덱스](README.md)

# 3-1. 인증 / 권한 아키텍처

**권한과 관련된 작업(새 API 추가, 화면 접근 제한 등)을 하기 전에 이 문서를 먼저 읽는다** (MUST). `@PreAuthorize`와 프론트엔드 라우트 가드는 §3-1-3 매트릭스와 반드시 일치해야 한다.

```text
§3-1-1   Role            권한 등급
§3-1-2   Status          계정 상태 — Role과 분리한다
§3-1-3   권한 매트릭스    이 표가 원본이다
§3-1-4   가입 신청       AUTH-01
§3-1-5   로그인·로그아웃  AUTH-02, AUTH-03
§3-1-6   대기중 안내      AUTH-04
§3-1-7   서버 재검증 원칙  UI 숨김은 권한이 아니다
```

## 3-1-1 Role

| Role | 설명 |
|---|---|
| `USER` | 일반 동아리원 |
| `ADMIN` | 관리자 |

## 3-1-2 Status

**Role과 Status는 분리하여 관리한다** (MUST). 가입 대기와 정지는 권한이 아니라 계정 상태다. 둘을 하나의 enum으로 합치면 "정지된 관리자" 같은 조합을 표현할 수 없다.

| Status | 설명 | 로그인 | 접근 가능 범위 |
|---|---|---|---|
| `PENDING` | 구글 로그인은 마쳤고 아직 승인되지 않음 | 가능 | 신청·대기 화면만 (공개 랜딩 제외) |
| `ACTIVE` | 정상 이용 | 가능 | 권한에 따른 전체 기능 |
| `INACTIVE` | 이번 학기에 활동하지 않는 부원 | 가능 | **자료를 뺀 전체 기능** |
| `SUSPENDED` | 관리자에 의한 이용 정지 | **차단** | 없음 (정지 안내 표시) |

**`PENDING`은 신청 전과 신청 후를 모두 포함한다.** 첫 구글 로그인만 마친 계정도 `PENDING`이다 (§3-1-4). 두 경우의 **접근 권한이 같으므로** — 둘 다 신청·대기 화면만 볼 수 있다 — Status를 나누지 않는다. 신청 여부는 권한이 아니라 데이터이며 `applied_at`으로 구분한다 ([3-2 §3-2-2](3-2-DESIGN-CONTRACT.md)).

| PENDING의 두 모습 | 구분 | 보는 화면 |
|---|---|---|
| 신청 전 | `applied_at IS NULL` | 신청 폼 |
| 신청 후 | `applied_at IS NOT NULL` | 승인 대기 안내 |

**신청 API는 `PENDING` 전체에 열려 있어야 한다** (MUST). 신청 전 계정을 차단하면 아무도 신청서를 낼 수 없다.

**회원 탈퇴도 마찬가지다** (MUST, 2026-08-28, [#223](https://github.com/HackerKHU/hacker_HP/issues/223)). `PENDING`에게 열린 인증 API가 이 둘이다 — **들어오는 문과 나가는 문**이다. 나가는 문을 닫으면 신청 전 계정(`applied_at IS NULL`)은 **본인이 지울 수 없고**, 승인 목록에도 뜨지 않아 관리자 눈에도 띄지 않는다 — 관리자 제거 경로는 그대로 열려 있다 (§3-1-3).

"접근 가능 범위"는 **인증이 필요한 화면**을 말한다. 공개 랜딩은 인증 영역 밖이라 어떤 상태에서도 열려 있다 (§3-1-3).

가입 **거부** 시에는 별도 상태를 두지 않고 계정 레코드를 삭제한다. 거부된 사용자는 재신청할 수 있다.

### 비활동 부원(`INACTIVE`)은 자료만 막힌다

*(2026-08-26, #228.)*

동아리는 **매 학기 활동하는 사람이 바뀐다.** 지난 학기 부원이 계정을 그대로 들고 시험·과목 정리본을 계속 받아 가는 상태를 막으려고 이 상태를 둔다. 학기가 바뀌면 관리자가 일반 부원을 한 번에 내리고, 이번 학기에 활동하는 사람만 다시 올린다 ([2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md#학기-전환--일괄-비활성화와-복구)).

**막는 것은 자료 기능 전체다** — 목록·검색·상세·다운로드·업로드·수정·삭제·즐겨찾기. 공지·활동사진·자유 게시판·마이페이지는 그대로 열려 있다 ([2-1 §2-1-9](2-1-USER-STORIES.md#2-1-9-마이페이지) — 마이페이지는 2026-08-28에 스펙이 정해졌고 구현은 [#178](https://github.com/HackerKHU/hacker_HP/issues/178)이다). **비활동 부원도 탈퇴할 수 있다** — 나가는 문을 자료와 함께 잠그면 나가고 싶은데 못 나가는 사람이 생긴다.

> **왜 자료 하나만 통째로인가.** 자료가 이 사이트의 값나가는 자산이고, 그것만 잠그면 나머지는 잠글 이유가 없다. 그리고 **자료 안에서 갈라 놓으면 어긋난 상태가 생긴다** — 목록을 못 보는데 업로드만 되거나, 볼 수 없는 자료에 즐겨찾기가 남는다. 잠근다면 그 갈래 전체여야 한다.

**로그인은 된다** (MUST). `SUSPENDED`와 다른 점이 이것이다. 자료 말고는 다 되므로 들어오는 것 자체를 막을 이유가 없고, 무엇보다 **막으면 본인이 자기 상태를 확인할 길이 없다** — 로그인 화면에 "정지"와 "비활동"이 같은 얼굴로 서게 된다.

**`INACTIVE`는 언제나 `USER`다** (MUST). 관리자는 비활성화 대상이 아니고([2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md#2-2-3-회원-상태-변경)), 비활동 부원에게 권한을 부여하는 것도 막는다 — 허용하면 **자료를 못 보는 관리자**가 생겨 회원 관리 화면에서 남의 자료를 지우면서 자기는 목록을 열지 못한다. 그래서 매트릭스의 `INACTIVE` 열은 하나뿐이다.

**최초 관리자 승격도 예외가 아니다** (MUST, 2026-08-26 리뷰). `INACTIVE` 계정이 [`POST /auth/bootstrap-admin`](3-2-DESIGN-CONTRACT.md#post-authbootstrap-admin--신청-완료-계정만)을 통과하면 **`ACTIVE`로 올린 뒤** 승격한다. 상태를 그대로 두고 `role`만 바꾸면 이 MUST가 깨질 뿐 아니라, 그 경로가 여는 문의 조건이 *"활성 관리자가 0명"* 이라 **`ADMIN`/`INACTIVE`는 0명을 그대로 두어 몇 번을 불러도 복구되지 않는다.**

**이것이 상태가 경로마다 다르게 작용하는 첫 사례다.** `PENDING`·`SUSPENDED`는 경로와 무관하게 막히지만 `INACTIVE`는 자료 경로에서만 막힌다. **어디서 막는지**는 [3-3 결정 17](3-3-DESIGN-DECISIONS.md#3-3-18-결정-17--상태가-경로마다-다르게-작용하는-것은-필터의-경로-목록으로-가른다)에 있다.

## 3-1-3 권한 매트릭스

| 기능 | 비로그인 | PENDING | USER (INACTIVE) | USER (ACTIVE) | ADMIN (ACTIVE) |
|---|:---:|:---:|:---:|:---:|:---:|
| 랜딩 조회 | O | O | O | O | O |
| 구글 로그인 (가입 겸용) | O | O | O | O | O |
| 로그아웃 | - | O | O | O | O |
| CSRF 토큰 발급 | O | O | O | O | O |
| 세션 확인 (`GET /auth/me`) | O | O | O | O | O |
| 학과 목록 조회 (`GET /departments`) | O | O | O | O | O |
| 신청서 제출·수정 | X | O | X | X | X |
| 신청·대기 화면 | - | O | - | - | - |
| 마이페이지 (내 정보 조회) | X | X | O | O | O |
| **회원 탈퇴** (건수 조회·실행) | X | **O** | O | O | O※ |
| 자료 목록·검색·다운로드 | X | X | **X** | O | O |
| 자료 업로드 | X | X | **X** | O | O |
| 자료 수정·삭제 | X | X | **X** | 본인 것만 | 전체 |
| 즐겨찾기 | X | X | **X** | O | O |
| 공지 조회 | X | X | O | O | O |
| 공지 등록·수정·삭제·고정 | X | X | X | X | O |
| 활동사진 조회 | X | X | O | O | O |
| 활동사진 업로드·삭제 | X | X | X | X | O |
| 게시판 조회 | X | X | O | O | O |
| 게시판 작성 | X | X | O | O | O |
| 회원 관리 | X | X | X | X | O |

랜딩은 인증 영역 밖의 공개 페이지라 Role·Status와 무관하게 열려 있고, 정적이라 **랜딩 콘텐츠를 가져오는 API를 호출하지 않는다** ([3-3 결정 8](3-3-DESIGN-DECISIONS.md#3-3-9-결정-8--공개-랜딩-페이지를-1차-출시에-포함하고-정적으로-구현한다)). 랜딩을 열 때 나가는 `GET /auth/me`는 앱 셸이 경로와 무관하게 하는 세션 확인이지 랜딩의 요청이 아니다 (같은 결정의 2026-08-10 경계 명확화, [5-TESTING T-60](5-TESTING.md#공개-랜딩)).

**그 세션 확인은 비로그인에게도 열려 있다** (2026-08-22, #190). 막으면 **비로그인 방문자마다 실패 응답이 하나씩 남는데**, 브라우저가 콘솔에 남기는 그 줄은 앱이 지울 수 없어 진짜 오류가 묻힌다. `GET /auth/csrf`를 연 것과 같은 이유다 — 세션도 토큰도 없는 최초 진입에 필요하다.

**여는 것은 "세션이 있는가"라는 답뿐이다.** 비로그인에게는 `204`가 나가고 계정 정보는 한 줄도 실리지 않는다 ([3-2 §3-2-3](3-2-DESIGN-CONTRACT.md)). 이 행이 매트릭스의 다른 `O`와 다른 점이 그것이다 — **경로가 열린 것이지 데이터가 열린 것이 아니다.** 나머지 행은 모두 인증이 필요한 기능이며, 라우트 가드와 `@PreAuthorize`가 일치시켜야 할 대상도 그쪽이다.

**※ 마지막 활성 관리자는 자기 자신을 지울 수 없다** ([2-2 §2-2-7](2-2-OPERATOR-REQUIREMENTS.md#2-2-7-안전장치)). 남이 지우든 본인이 나가든 활성 관리자가 0명이 되는 것은 같으므로 규칙도 같다.

**회원 탈퇴는 `PENDING`에게도 열린다** (MUST, 2026-08-28, [#223](https://github.com/HackerKHU/hacker_HP/issues/223)). 이 표에서 `PENDING`이 `O`인 몇 안 되는 인증 기능이다 — 닫으면 **구글 로그인만 해보고 신청서를 내지 않은 사람이 자기 계정을 지울 방법이 없다** — 관리자 제거는 되지만 승인 목록에 뜨지 않아 눈에 띄지 않는다 ([2-1 §2-1-9](2-1-USER-STORIES.md#2-1-9-마이페이지)). **마이페이지 화면은 그대로 `PENDING`에게 닫혀 있고**, 진입점은 신청·대기 화면이 맡는다 (§3-1-6).

**`SUSPENDED` 열이 없는 것이 곧 답이다.** 정지 계정은 로그인이 차단되고 상태 필터가 보호 API를 열지 않으므로 이 표의 어떤 행에도 닿지 못한다 — 탈퇴를 따로 막는 규칙이 필요 없다.

**`INACTIVE` 열은 자료 네 행만 `ACTIVE`와 다르다** (2026-08-26, #228). 나머지 열일곱은 `USER (ACTIVE)`와 같은 값이다 — 그 사실 자체가 이 상태의 정의다. **`ADMIN (INACTIVE)` 열은 두지 않는다.** 그런 조합이 생기지 않도록 막기 때문이다 (§3-1-2).

**이 표에 없는 조합이 필요하면 추측하지 말고 먼저 확인한다** (MUST). 표를 늘리는 것이 결정이고, 코드에서 임의로 정하는 것은 결정이 아니다.

**열이 하나 늘면 모든 행을 다시 판단한다** (MUST). 새 열을 `ACTIVE` 열에서 복사해 놓고 다른 자리만 고치면, 그때 눈에 띄지 않은 행이 **판단된 적 없는 채로 계약이 된다.**

## 3-1-4 AUTH-01 가입 신청

**가입과 로그인은 구글 OAuth 하나로 처리한다** ([3-3 결정 13](3-3-DESIGN-DECISIONS.md#3-3-14-결정-13--가입로그인을-구글-oauth로-한다)). 자체 비밀번호를 받지도, 저장하지도 않는다 (MUST).

전체 흐름은 네 단계다.

```text
① 구글 로그인   →  ② 신청서 제출  →  ③ 관리자 승인  →  ④ 이용 가능
   계정 생성          학번·학과          PENDING → ACTIVE
   USER / PENDING
   이름·이메일
```

**① 구글 로그인 — 계정이 없으면 만든다**

- 허용 도메인은 **`khu.ac.kr`** 이다 (MUST). 설정 키 **`app.auth.allowed-email-domain`** 으로 주입하며 코드에 하드코딩하지 않는다. 값이 없으면 기동에 실패시킨다 (MUST) — 코드에 기본값을 심어두면 설정 누락이 조용히 지나가 도메인 제한이 무력해진다. 주입 방법은 [docs/ops/infra.md](../docs/ops/infra.md)에 있다.
- **도메인 검증은 서버가 한다** (MUST). 구글의 `hd` 파라미터는 로그인 화면에서 계정을 좁혀주는 힌트일 뿐 위조를 막지 못하므로, 서버가 ID 토큰의 `email`과 `email_verified`를 직접 확인한다.
- **`@` 뒤 도메인을 잘라내 설정값과 정확히 일치하는지 본다** (MUST). `endsWith`로 검사하면 `user@notkhu.ac.kr`이 통과한다. 하위 도메인(`user@cs.khu.ac.kr`)도 정확 일치가 아니므로 거부한다 — 필요해지면 허용 목록을 늘리는 것이 결정이고, 접미사 검사로 흘리는 것은 결정이 아니다.
- 도메인이 다르거나 `email_verified`가 거짓이면 계정을 만들지 않고 로그인을 거부한다 (`403 FORBIDDEN`).
- 처음 로그인한 계정은 `USER` / `PENDING`으로 생성된다. 이름은 구글 프로필에서 받아 채운다.
- 같은 구글 계정으로 다시 로그인하면 새 계정을 만들지 않고 기존 계정에 붙는다. 그래서 이메일 중복이라는 상황 자체가 생기지 않는다.
- 이메일이 바뀌었으면 갱신한다. **그 주소를 다른 계정이 이미 쓰고 있으면 로그인을 거부한다** (MUST). 두 계정을 합치는 것은 사람이 판단할 일이고, 자동으로 합치면 학교가 재활용한 주소를 받은 사람이 남의 계정에 올라탄다.
- **이름은 계정을 만들 때만 구글 프로필에서 받고, 이후 로그인에서는 갱신하지 않는다** (MUST). 신청서도 이름을 받지 않으므로(②) 이 값이 **관리자가 심사하는 이름이자 회원 목록에 뜨는 이름**이다. 매 로그인마다 덮으면 그 이름이 본인도 관리자도 모르게 바뀐다 — 구글 표시 이름은 별명으로 바꿀 수 있는 값이다.
- **표시 이름의 학적 접미사는 저장 전에 걷어낸다** (#215). 학교 Workspace 계정은 `강경현[학생](소프트웨어융합대학 컴퓨터공학부)`처럼 실명 뒤에 소속을 붙여 준다. 첫 `[`·`(` 앞까지만 남긴다 — 이름 글자 수로 자르면 외자나 네 자를 넘는 이름이 잘린다.

**② 신청서 제출 — 승인 심사에 필요한 정보를 받는다**

- 구글은 학번을 주지 않는다. 그래서 로그인 직후 계정에는 학번이 비어 있다.
- `PENDING` 사용자는 신청 화면에서 **학번·학과**를 제출한다 (§3-1-6).
- **이름·이메일은 신청서가 받지 않는다** (MUST, #224). 구글 계정에서 온 값을 화면이 **읽기 전용으로 채워** 보여주고, 서버도 `name`을 받지 않는다 — 화면만 잠그면 API 직접 호출로 우회된다. 이메일은 원래 신청 항목이 아니었고 표시만 더한 것이다 (계정이 여럿인 사람이 어느 계정으로 신청하는지 확인해야 한다).
- **학과는 정해진 목록에서 고른다** (MUST) — 자유 입력이 아니다. 목록에 없는 값은 거부한다 (`400 VALIDATION_ERROR`). 목록의 출처와 컬럼 제약은 [3-2 §3-2-2](3-2-DESIGN-CONTRACT.md)를 따른다.
- 제출 시각을 기록한다. **승인 대상은 신청서를 낸 `PENDING` 계정으로 한정한다** (MUST) — 구글 로그인만 해보고 신청하지 않은 계정이 관리자의 승인 목록에 섞이면 운영이 어려워진다.
- 학번이 이미 다른 계정에 쓰이고 있으면 거부한다 (`409 DUPLICATE_STUDENT_NO`). 한 학번으로 여러 계정을 만드는 것을 막는다.
- 승인 전까지는 다시 제출해 고칠 수 있다.
- **신청서 저장은 승인과 직렬화한다** (MUST). `WHERE status = 'PENDING'` 조건부 갱신이나 행 잠금을 쓴다. 권한 검사만 통과시키고 저장을 나중에 하면, 그 사이 관리자가 승인해 `ACTIVE`가 된 계정의 학번·학과가 승인 뒤에 바뀐다 — 관리자가 심사한 내용과 저장된 내용이 달라진다.

**③ 관리자 승인 → ④ 이용 가능**

- 관리자가 승인하면 `PENDING` → `ACTIVE`가 되고 승인일시가 기록된다 ([2-2 §2-2-2](2-2-OPERATOR-REQUIREMENTS.md)).
- 승인은 기존 세션에 즉시 반영된다 (§3-1-5). 재로그인 없이 바로 이용할 수 있다.

> **인증 수단 결정** — 학번은 동아리 안에서 서로 알고 있는 값이므로 인증에 쓰지 않는다. 신원 확인(승인 심사)용으로만 저장한다. 신원 증명은 구글이 담당한다.

## 3-1-5 AUTH-02 로그인 / AUTH-03 로그아웃

- 로그인 수단은 **구글 OAuth 하나**다 (MUST). 비밀번호 입력란이 없다.
- 로그인 버튼 **안의 구글 로고는 공식 에셋을 수정 없이 쓴다** (MUST). 버튼 자체와 문구는 우리가 만들되 가이드라인이 정한 규격을 따른다 — 아래 참조.
- 이미 계정이 있으면 그 계정으로, 없으면 만들면서 로그인한다 (§3-1-4). 화면상 "로그인"과 "가입"이 같은 버튼이다.
- 도메인·`email_verified` 검증은 매 로그인마다 수행한다 (MUST). 최초 가입 때 한 번만 확인하고 넘어가지 않는다.

Status별 처리:

| Status | 결과 |
|---|---|
| `ACTIVE` | 정상 로그인 |
| `INACTIVE` | **정상 로그인** — `ACTIVE`와 같다. 자료 화면에서만 막힌다 (§3-1-2) |
| `PENDING` | 로그인은 허용하되 신청·대기 화면으로 강제 이동 |
| `SUSPENDED` | 로그인 차단. 세션을 발급하지 않고 로그인 화면으로 되돌린다 ([3-2 §3-2-3](3-2-DESIGN-CONTRACT.md) 콜백 리다이렉트) |

### 정지 안내는 로그인 화면이 띄운다 — 도달 경로가 둘이다

정지된 사람이 로그인 화면에 도착하는 길은 두 가지이고, **둘 다 같은 안내를 봐야 한다** (MUST).

| 경로 | 어떻게 도착하나 | 주소 |
|---|---|---|
| ① 로그인 시도가 막힘 | 서버가 세션을 발급하지 않고 되돌린다 ([3-2 §3-2-3](3-2-DESIGN-CONTRACT.md) 콜백 표) | `/login?error=suspended` |
| ② 이용 중 정지됨 | 관리자가 정지시킨 뒤([2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md) MUST) 다음 API 호출이 `403 SUSPENDED`로 막히고, 세션이 정지로 정리되어 라우트 가드가 로그인 화면으로 보낸다 | `/login` (쿼리 없음) |

**경로 ①이 안전망이다.** ②가 어떤 이유로 실패해도 — 서버가 세션을 지워 `401`이 오거나, 클라이언트가 코드를 놓치거나 — 사용자가 로그인을 다시 시도하면 콜백이 `?error=suspended`를 준다. 안내가 영영 안 뜨는 것은 아니다. 그래서 ②를 위해 과하게 설계할 필요는 없다.

**②가 막는 것은 그 한 번이다.** ②가 없으면 관리자가 정지시킨 직후 사용자는 **아무 설명 없는 로그인 화면으로 한 번 튕긴다.** 이유를 알려면 스스로 로그인을 다시 눌러 봐야 하고, 그 전까지는 무엇이 잘못됐는지 알 수 없다. 그래서 화면은 `?error=suspended`뿐 아니라 **세션이 정지 상태인 것만으로도** 안내를 띄운다.

**두 경로의 안내 문구는 같다** (MUST). 사용자에게는 같은 사실 — "이 계정은 정지되어 이용할 수 없다" — 이고, 도달 경로는 사용자가 알 필요도 구별할 수도 없는 내부 사정이다. 다르게 쓰면 왜 다른지 설명할 수 없고 한쪽만 고쳐지는 자리가 된다.

### 로그인 버튼

구글 브랜딩 가이드라인이 로고와 버튼 규격을 정한다. **로고는 공식 것을 그대로 쓰고, 버튼과 문구는 우리가 만든다** (MUST).

**로고를 직접 그리지 않는다** (MUST). 색을 바꾸거나 단색으로 만들거나 자체 아이콘으로 대체하지 않는다. 구글 배포본에서 로고만 뽑아 쓴다.

**문구는 한글로 쓴다.** 가이드라인이 현지화를 허용하고 권장한다. 규격이 요구하는 `Google Sans Medium`에는 **한글 글자가 없으므로**, 그 폰트 규정은 라틴 문자 렌더링에 해당한다 — 한글은 사이트 본문 폰트로 쓴다.

**승인된 문구는 셋이다** — `Sign in with Google`, `Sign up with Google`, `Continue with Google`. 이 사이트의 버튼은 **로그인과 가입을 겸하므로**([3-3 결정 13](3-3-DESIGN-DECISIONS.md#3-3-14-결정-13--가입로그인을-구글-oauth로-한다)) `Continue with Google`의 한글판인 **`Google로 계속하기`**를 쓴다. 승인 목록에 없는 표현("시작하기" 등)이나 `Google` 단독은 쓰지 않는다 (MUST).

**가이드라인이 정한 두 테마 중 하나를 통째로 쓴다** (MUST). 배경·테두리·글씨는 한 벌로만 유효하므로, 카드 배경에 맞는 쪽을 골라 세 값을 함께 쓴다. 한 값만 다른 테마에서 가져오거나 임의의 색을 칠하는 것은 규격 위반이다.

| 항목 | Light | Dark |
|---|---|---|
| 배경 | `#FFFFFF` | `#131314` |
| 테두리 | `#747775`, 1px inside | `#8E918F`, 1px inside |
| 글씨 | `#1F1F1F`, 14/20 | `#E3E3E3`, 14/20 |

**로그인 화면은 Dark를 쓴다.** 이 화면은 배경도 카드도 검정이라, Light를 그대로 두면 흰 버튼이 검정 카드 위에 홀로 뜬다. 화면 배색을 바꾸면 이 테마도 함께 바꾼다.

여백은 두 테마가 같다 — 좌우 12px, 로고와 글씨 사이 14px, 로고 20px. 공식 배포본(180×40)에서 잰 값이고 짐작으로 정하지 않는다.

G 로고 자체는 두 테마가 같은 파일이다.

**비율을 지키는 확대는 허용한다.** 가이드라인이 버튼 전체를 비율 그대로 키우는 것을 막지 않는다. 다만 **배율은 하나여야 한다** (MUST) — 항목마다 다른 배율로 반올림하면 그것은 확대가 아니라 규격 변경이다. **글씨만 키우는 것도 금지다.**

지금 배율은 **`16/14`**다. 사이트 본문이 16px인데 버튼만 14px이면 이 버튼 혼자 작아 보이므로 글씨를 본문에 맞췄고, 나머지 치수가 전부 같은 배율로 따라간다.

| 항목 | 규격 (1배) | 지금 (16/14배) |
|---|---|---|
| 글씨 / 줄간격 | 14 / 20 | 16 / 22.86 |
| 높이 | 40 | 45.71 |
| 로고 | 20 | 22.86 |
| 좌우 여백 | 12 | 13.71 |
| 로고–글씨 간격 | 14 | 16 |

**구현은 치수를 `em`으로 적는다** (MUST). 글씨 크기에 상대로 두면 배율이 코드에 한 번(글씨 크기)만 나오고 나머지는 저절로 따라간다. px로 적으면 항목마다 반올림이 갈려 위의 "배율은 하나" 규칙이 조용히 깨진다.

**로고를 표준 컬러 그대로 쓸 수 있는 배경은 light·dark·neutral뿐이다** (MUST). 테두리와 문구 없이 로고만 두는 형태도 쓰지 않는다.

### 로그인 상태 유지 방식

**신원 증명은 JWT, 인가 상태는 서버 세션이 담당한다** ([3-3 결정 12](3-3-DESIGN-DECISIONS.md#3-3-13-결정-12--인증은-jwt-인가-상태는-서버-세션으로-나눈다)).

| 담당 | 수단 | 저장 위치 |
|---|---|---|
| 이 요청이 **누구인지** | JWT (`sub`만) | `httpOnly` 쿠키 |
| 그 사람이 **지금 무엇을 할 수 있는지** (`role`, `status`) | 서버 세션 | RDS |

- JWT에 `role`과 `status`를 담지 않는다 (MUST). 담으면 정지·승인이 토큰 만료까지 반영되지 않아 §3-1-2와 [2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md)을 어긴다.
- **세션이 없으면 인증이 성립하지 않는다** (MUST). 서명이 유효한 JWT만으로는 통과하지 못한다.
- **세션과 JWT가 같은 사용자의 것인지 매 요청 확인한다** (MUST). 세션에 사용자 id를 저장하고 JWT의 `sub`와 대조해, 다르면 인증 실패로 처리하고 양쪽 쿠키를 폐기한다. 이 검증이 없으면 A의 JWT와 B의 세션을 함께 보내 A의 신원으로 B의 권한을 쓰는 조합이 성립한다.
- 토큰은 `httpOnly` 쿠키로만 주고받는다. `localStorage`에 저장하지 않는다 (MUST).
- **인증 쿠키에는 `Secure`를 붙인다** (MUST). `SameSite=Lax`는 교차 사이트 요청만 제한할 뿐 평문 HTTP 전송을 막지 못하므로 별개로 지정한다. 로컬 개발(HTTP)만 설정으로 예외를 둔다.
- 쿠키가 자동 전송되므로 **상태를 바꾸는 요청은 CSRF 토큰을 검증한다** (MUST). 쿠키·헤더 이름과 발급 경로는 [3-2 §3-2-3](3-2-DESIGN-CONTRACT.md)에 있다.
- 세션은 RDS에 저장한다 (MUST). ECS 태스크가 Fargate Spot이라 메모리에 두면 회수 시 전원 로그아웃된다.

**로그아웃은 세션을 지우는 것으로 성립한다** (MUST). 세션이 사라지면 쿠키에 남은 JWT는 더 이상 인증에 쓰이지 못한다. 이후 로그인 화면으로 이동한다.

### 클라이언트가 세션 상태를 바꾸는 근거

**상태를 알려주는 실패만 세션을 바꾼다** (MUST). "서버에 못 닿았다"에서 "로그아웃됐다"를 추론하지 않는다.

| 응답 | 클라이언트 |
|---|---|
| `401 UNAUTHENTICATED` | 비로그인으로 정리한다 — 서버가 명시적으로 말했다 |
| `403 PENDING_APPROVAL` / `403 SUSPENDED` | 해당 상태로 정리한다 |
| **`403 INACTIVE`** | **세션을 `INACTIVE`로 정리하되 내보내지 않는다** (2026-08-26, #228). 위 둘과 다르다 — 그 사람은 여전히 로그인 상태이고 공지·게시판을 계속 쓴다. **로그인 화면으로 보내면 안 된다** |
| **네트워크 오류·5xx·계약에 없는 코드** | **세션을 그대로 둔다.** 화면이 "다시 시도해 달라"를 보여준다 |

**모르는 `status` 값을 받으면 던지지 않는다** (MUST, 2026-08-26 리뷰). 오래 열어 둔 탭은 새 상태를 모르는 낡은 번들일 수 있다. **조용히 `ACTIVE`로 취급하지 않고** 비로그인으로 정리해 로그인 화면을 보여준다 — 새로 고치면 새 번들을 받는다. 값을 빠짐없이 가르게 해 두는 것은 **빌드를 깨서 새 상태를 못 보고 지나치지 않게 하려는 것**이지, 실행 중에 화면을 멈추려는 것이 아니다.

**이것은 위 표의 예외가 아니다.** 서버는 상태를 분명히 알려줬고 우리가 그 값을 모를 뿐이라, "상태를 알려주지 않은 실패"와 다르다. 그래서 세션을 그대로 두지 않고 정리한다. **최초 확인과 이용 중 갱신이 같게 동작해야 한다** (MUST) — 갱신 쪽만 호출부로 올리면 그 탭은 낡은 세션을 든 채 "다시 시도" 안내만 반복한다.

**이미 열려 있는 탭은 이 규칙으로도 못 고친다.** 그래서 이 관용은 **새 상태를 아는 배포보다 앞서, 따로** 나가야 한다 — 근거와 배포 순서는 [3-2의 API 공통 규약](3-2-DESIGN-CONTRACT.md)에 있다 (*"enum에 값을 더하는 것은 `v1` 안에서 하되, 배포 순서를 지킨다"*).

이 구분이 없으면 **네트워크가 잠깐 끊긴 것만으로 로그인 화면으로 튕긴다.** 승인을 기다리는 지원자가 대기 화면에서 "다시 확인"을 누르다 튕기면 다시 로그인해야 하고, 자기가 무엇을 잘못했는지 알 수 없다.

**최초 세션 확인은 이 규칙의 예외다.** 그때는 버릴 세션이 없다 — 어느 쪽으로도 정해지지 않으면 화면이 영영 "불러오는 중"에 갇힌다. 그래서 알 수 없는 실패면 비로그인으로 둔다. **이것은 "로그아웃됐다"는 판단이 아니라 "아직 로그인으로 확인된 바 없다"는 뜻이고**, 사용자는 로그인 화면에서 다시 시도할 수 있다. 이 둘을 화면에서 구분해야 할 필요가 생기면 그때 상태를 나누는 것을 결정으로 올린다.

### 상태 변경을 기존 세션에 반영한다

`role`이나 `status`가 세션에 들어 있으므로, 관리자가 값을 바꾸면 **그 회원의 기존 세션에도 즉시 반영해야 한다** (MUST). 어느 쪽이든 **다음 요청부터 새 값이 적용되어야 한다.**

**정지는 세션을 지우지 않고 `status`를 갱신한다** (MUST). 세션을 지우면 다음 요청이 `401 UNAUTHENTICATED`가 되어([5-TESTING T-30](5-TESTING.md#5-2-필수-테스트-사례)) 클라이언트가 정지인지 단순 만료인지 구별하지 못하고, **정지 직후의 그 화면에서** 안내를 띄울 수 없다 (§3-1-5의 경로 ②, [3-3 결정 12의 2026-08-11 노트](3-3-DESIGN-DECISIONS.md#3-3-13-결정-12--인증은-jwt-인가-상태는-서버-세션으로-나눈다)). 승인·권한 변경은 갱신과 무효화 중 무엇을 써도 된다 — 그쪽은 결과를 사용자에게 설명할 필요가 없다.

| 작업 | 반영되지 않으면 |
|---|---|
| 정지 (`ACTIVE` → `SUSPENDED`) | 정지된 회원이 계속 이용한다 — [2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md) 위반 |
| **승인 (`PENDING` → `ACTIVE`)** | 승인된 회원이 세션 만료까지 `403 PENDING_APPROVAL`에 갇힌다 — §3-1-6 위반 |
| 권한 변경 (`USER` ↔ `ADMIN`) | 회수된 관리자 권한이 계속 살아 있다 |
| **비활성화 (`ACTIVE` → `INACTIVE`)** | 지난 학기 부원이 세션 만료까지 자료를 계속 받아 간다 — [2-2 §2-2-3](2-2-OPERATOR-REQUIREMENTS.md#학기-전환--일괄-비활성화와-복구) 위반 (2026-08-26, #228) |
| 복구 (`INACTIVE` → `ACTIVE`) | 올라온 부원이 세션 만료까지 `403 INACTIVE`에 갇힌다 |

승인은 일괄 처리이므로([2-2 §2-2-2](2-2-OPERATOR-REQUIREMENTS.md)) 선택된 회원 전원의 세션을 함께 처리한다. **한 사람의 세션이 여럿일 수 있다** (PC·휴대폰). 전부 갱신한다 (MUST) — 하나라도 남기면 정지된 사람이 그 브라우저로 계속 쓴다.

**다섯 경우 모두 갱신 하나로 처리한다** (2026-08-13 #85, 2026-08-26 #228에서 비활성화·복구 추가). 정지가 갱신이어야 하므로 경로를 나눌 이유가 없고, 나누면 한쪽만 고쳐진다.

**세션은 `SPRING_SESSION.PRINCIPAL_NAME`으로 찾는다.** `SecurityContext`를 세션에 저장하지 않으므로 그 컬럼을 채워 줄 것이 없어, 로그인 시 사용자 id를 직접 넣는다(이메일이 아니라 숫자 id다 — 세션 테이블에 개인정보를 늘리지 않는다). 나머지 속성은 직렬화되어 값으로 조회할 수 없다.

**늦게 도착한 옛 값이 새 값을 덮지 않게 한다** (MUST). 행 잠금은 DB 변경만 직렬화한다 — 커밋과 함께 잠금이 풀리고 세션 저장은 그 뒤에 각자 일어나므로 **순서가 뒤집힐 수 있다.** 해제가 먼저 커밋된 뒤 세션 저장이 늦어지고 그 사이 정지가 커밋·저장까지 마치면, 뒤늦게 도착한 해제가 세션을 `ACTIVE`로 되돌린다. 세션에 계정 행의 낙관적 잠금 버전을 함께 두고 **더 낮은 버전으로는 덮어쓰지 않는다** — 나중 변경은 반드시 더 큰 값을 읽으므로(앞의 변경이 커밋될 때까지 행 잠금에 막힌다) 인스턴스가 여럿이어도 통한다. 같은 버전은 덮어쓴다: 상태가 바뀌지 않은 재요청이 갱신 실패의 복구 수단이기 때문이다.

**무엇을 쓸지는 갱신하는 쪽이 직접 읽는다** (MUST). 바뀐 값을 넘겨받으면 그것이 이미 지난 값일 수 있고, 앞뒤 갱신이 엇갈리면 옛 값이 새 값을 덮는다. **계정을 읽는 것부터 세션에 쓰는 것까지가 한 연산이다** — 그 사람의 계정 행을 잠근 채 읽고 쓰므로 동시에 도착한 갱신은 기다렸다가 같은 최신 값을 본다. 상태를 바꾸는 트랜잭션도 그 행을 잠그므로 갱신과 다음 변경의 순서도 함께 선다.

**세션 저장은 변경 트랜잭션이 끝난 뒤에 한다** (MUST). 트랜잭션 안에서 부르면 **DB가 되돌아가도 세션만 새 값으로 남고**(세션 저장소는 자기 트랜잭션으로 커밋한다), 커밋 콜백에서 부르면 **아직 반납되지 않은 커넥션 위에 커넥션을 겹쳐 잡는다** — 세션 저장소가 또 하나를 요구하므로 스레드마다 셋이 되어 풀이 마른다. 커밋이 끝나고 자원이 정리된 뒤에 부른다.

**로그인도 계정 행을 잠근 채 읽고, 세션이 저장소에 들어가는 것까지 그 잠금 안에서 끝낸다** (MUST, 2026-08-21, #127).

로그인이 계정을 읽어 세션을 채우는데, 그 세션이 저장소에 들어가는 것은 요청이 끝날 때다. 그 사이의 승인·정지는 **아직 없는 세션을 놓치고**, 뒤이어 저장된 세션이 옛 값을 들고 만료까지 남는다.

**한 번 더 읽는 것으로는 닫히지 않는다.** 창의 끝은 "다시 읽는 시점"이 아니라 **"세션이 실제로 저장되는 시점"** 이라, 조회를 늘리면 창이 조금 줄 뿐이다.

**저장한 뒤에 대조하는 것으로도 부족하다.** 세션은 응답이 커밋될 때 저장되는데, 응답이 나가면 **대조가 끝나기 전에 그 쿠키로 요청이 도착할 수 있다.** 그 순간 저장소에는 아직 옛 값이 있다.

그래서 **읽기부터 저장까지가 한 연산이다** — 위의 갱신 규칙과 같은 잠금, 같은 이유다. 상태를 바꾸는 쪽도 같은 행을 잠그고 세션 반영은 커밋한 뒤에 하므로, **어느 쪽이 먼저 잠그든 결과가 하나다.**

| 먼저 잠근 쪽 | 결과 |
|---|---|
| 로그인 | 상태 변경이 기다렸다 커밋하고, 그 뒤의 반영이 **이미 저장된** 세션을 찾는다 |
| 상태 변경 | 로그인이 기다렸다 **바뀐 값**을 읽어 세션에 담는다 |

**응답을 내보내는 호출이 잠금 안에 있어야 한다** (MUST). 그 호출을 받아 세션이 저장되기 때문이다 — 밖으로 빼면 잠금이 먼저 풀려 창이 도로 열린다.

**계정이 사라졌으면 세션을 만들지 않고 로그인을 실패시킨다** (MUST). 잠근 채 읽어 없으면 그 자리에서 끝낸다 — 아직 응답을 내보내기 전이라 실패로 되돌릴 수 있다. 이미 만들어진 세션 쪽은 갱신 경로가 맡는다: 계정을 찾지 못하면 그 사람의 세션을 **지운다.** 필터는 매 요청 `users`를 읽지 않으므로(결정 12) 남겨 두면 계정 없는 사람이 만료까지 인증된다 ([2-2 §2-2-4](2-2-OPERATOR-REQUIREMENTS.md#2-2-4-회원-제거) MUST).

**로그인 전후로 세션 id가 바뀐다** (MUST). 인가 요청이 쓰던 세션을 버리고 새로 만들기 때문이다 — 세션 고정 보호이자, 그 세션이 유령으로 남지 않게 하는 처리다.

> **배포 직후 30분** — 배포 시점에 살아 있던 세션은 `PRINCIPAL_NAME`이 비어 있어 찾을 수 없다. 세션 수명이 30분이라 자연히 사라지므로 마이그레이션은 필요 없지만, 그동안의 정지·승인은 그 세션들에 반영되지 않는다.

## 3-1-6 AUTH-04 신청·대기 화면

- **인증이 필요한 화면 중** `PENDING` 사용자가 접근 가능한 **유일한** 화면이다 (MUST). 공개 랜딩은 인증 영역 밖이라 이 제한의 대상이 아니다 (§3-1-2, §3-1-3).
- 신청서를 냈는지에 따라 한 화면이 두 모습을 가진다.

| 상태 | 화면 |
|---|---|
| 신청 전 (`applied_at IS NULL`) | 학번·학과를 입력하는 **신청 폼**. 이름·이메일은 구글 계정 값이 채워진 채 읽기 전용이다 (§3-1-4 ②) |
| 신청 후 (`applied_at` 있음) | 승인 대기 중임을 알리는 **안내**. 승인 전까지 신청 내용을 고칠 수 있다 |
| **신청 여부를 모름** | 폼도 안내도 띄우지 않는다 — 아래 참조 |

- 두 경우 모두 로그아웃 버튼을 제공한다.
- **두 경우 모두 탈퇴 진입점을 제공한다** (MUST, 2026-08-28, [#223](https://github.com/HackerKHU/hacker_HP/issues/223) — **`Post Launch`다**, [1-BACKGROUND §1-6](1-BACKGROUND.md#1-6-1차-출시-범위)). `PENDING`은 마이페이지를 볼 수 없으므로 **탈퇴가 화면에 드러나는 곳이 여기뿐이다.** 로그아웃과 나란히 두되 같은 무게로 두지 않는다 — 되돌릴 수 없는 조작이라 확인 창을 거친다 ([2-1 §2-1-9](2-1-USER-STORIES.md#2-1-9-마이페이지)).

**신청 여부를 모르는 상태가 실제로 존재한다** (MUST). 클라이언트가 `PENDING`임을 `403 PENDING_APPROVAL`로만 알아낸 경우다 — 보호 API가 그 코드로 막으면 세션은 "대기 중"이라는 것만 알고 `applied_at`은 모른다. **이때 기본값으로 폼을 띄우면 이미 신청한 사람이 다시 쓰게 된다.** 화면은 `GET /auth/me`를 다시 불러 신청 여부를 확인한 뒤에 무엇을 보일지 정하고, 그 사이에는 어느 쪽도 띄우지 않는다.

**승인은 이 화면에 저절로 반영되지 않는다.** 서버는 승인을 기존 세션에 즉시 반영하지만(§3-1-5), 대기 화면은 이미 그려져 있어 스스로 알지 못한다. 그래서 **"다시 확인" 버튼을 둔다** (MUST) — 누르면 `GET /auth/me`를 다시 불러 `ACTIVE`가 되었으면 그때 화면이 풀린다.

**자동 폴링은 하지 않는다.** 승인은 몇 분에서 며칠이 걸리는 일이라, 대기자 전원이 주기적으로 요청을 보내는 비용을 계속 낼 이유가 없다. 필요해지면 그때 결정으로 올린다.

**다만 화면이 "저절로 바뀌지 않는다"는 것을 사용자에게 알려야 한다** (MUST). 버튼만 두고 설명이 없으면 누를 이유를 모르고, 승인된 뒤에도 대기 화면에 머문다.
- 인증 API와 신청 API 외의 모든 API 호출은 `403 PENDING_APPROVAL`로 차단한다 (MUST). 프론트엔드는 이 코드를 감지해 이 화면으로 리다이렉트한다.
- 신청 API는 `PENDING` 전용이다. `ACTIVE`가 된 뒤에는 이 경로로 학번을 바꿀 수 없다 (MUST). 거부 코드는 `403 FORBIDDEN`이다 ([3-2 §3-2-7](3-2-DESIGN-CONTRACT.md), [5-TESTING T-50](5-TESTING.md#5-2-필수-테스트-사례)) — 코드가 정해져 있지 않으면 화면이 무엇을 보여줄지 정할 수 없다.

## 3-1-7 서버 재검증 원칙

- **모든 관리자 API는 서버 측에서 권한을 재검증한다** (MUST). UI 숨김만으로 권한을 통제하지 않는다.
- 본인이 마지막 남은 활성 관리자일 때는 본인 권한 회수·계정 삭제·본인 정지를 서버에서 차단한다 (MUST) — [2-2 §2-2-7](2-2-OPERATOR-REQUIREMENTS.md).
- 자료 수정·삭제의 "본인 것만" 조건도 서버에서 소유자를 확인한다 (MUST).

---
[← 이전: 운영자 요구사항](2-2-OPERATOR-REQUIREMENTS.md) · [다음: 계약(데이터·API) →](3-2-DESIGN-CONTRACT.md)
