/**
 * 화면의 정렬선. **헤더·본문·푸터·랜딩이 이 하나를 같이 쓴다** (#389).
 *
 * 헤더 로고와 본문 첫 글자와 푸터 주소가 한 줄로 서는 것이 이 값의 조건이다. 어긋나면
 * 화면을 오갈 때 내용이 좌우로 미끄러진다 — **그 어긋남은 이미 세 번 났다.** #247은
 * 헤더에 `px-4` 하나가 남아 8px, #258·#264는 페이지 길이에 따라 스크롤바가 생겼다
 * 없어지며 7.5px이었다. 매번 눈으로는 잘 안 보였고 원인도 헤더가 아닌 곳에 있었다.
 *
 * 네 번째는 **값이 네 벌이라는 사실**에서 올 자리였다. 한 곳만 고치면 나머지 셋은 조용히
 * 그대로다. `header-nav.ts`가 "`px-6`은 랜딩 본문·푸터와 같은 정렬선"이라고 말로 적어 둔
 * 것도 코드로는 그 사실을 붙잡을 수 없었기 때문이다. 이제 한 벌이다.
 *
 * **`px-6`은 좁은 화면에서도 줄이지 않는다** (#247, #249). 320px 뷰포트에서 실제 콘텐츠
 * 폭이 305px뿐이지만, 여기서 줄이면 헤더만 다른 정렬선을 갖게 된다.
 *
 * 폭 1152px을 고른 이유와 화면별 폭 규칙 다섯 종은 `apps/web/README.md`
 * "화면 폭과 여백"에 있다. **이 상수는 그중 가장 바깥(`<main>`) 하나만 정한다** — 읽는 글
 * (`max-w-2xl`)이나 짧은 폼(`max-w-sm`)은 화면이 그 안에서 고르는 값이라 여기 없다.
 */
export const PAGE_CONTAINER = 'mx-auto w-full max-w-[1152px] px-6'
